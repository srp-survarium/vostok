# Pattern index

One line per pattern: `- [title](file.md) — cN — tags — symptoms` (cN = confidence /10). Sorted by primary
`cpp:` tag so construct families cluster. Grep this file by tag (`cpp:if`,
`asm:sbb`, `topic:mangling`) or by symptom token, then read only the hits.
Authoring: new pattern = new file (schema in `../assembly_patterns.md`) + one
line here, same commit.

- [A `??__E<cc_static>` initializer looks argument-less: read the args out of the shipped `.data`](cc-static-args-from-data-section.md) — c9 — cpp:static cpp:ctor cpp:console-command | asm:mov asm:movss asm:call | topic:data-section topic:dynamic-initializer topic:recovery-technique — unpaired `dynamic initializer for '<cc_*>'`, note claims the command-NAME string is unrecoverable; m_name/m_command_type/m_serializable are constant-folded into .data, read them at obj+0x10/0x14/0x1D in survarium.exe (image base 0x10000); target cc_bool derives from cc_value<bool> and re-sets m_need_args in its own body

- [Rich-index exact evidence fills an objdiff score gap](rich-index-exact-fills-objdiff-gap.md) — c9 — cpp:comdat cpp:inline | topic:measurement topic:pairing topic:scoring-artifact — equal normalized streams, including uniquely PDB-proven ICF aliases at otherwise identical calls/jumps, establish function-scoped exactness when objdiff omits the score
- [ICF representative and optional `enum` spelling can hide an exact template body](icf-representative-enum-spelling.md) — c9 — cpp:comdat cpp:template cpp:enum | topic:fold-icf topic:pairing topic:measurement — same-unit equal bodies choose different ICF specialization names, demangled signatures differ only by optional `enum`, and differing call/jump names resolve through overlapping PDB alias sets; preserve both the exact identity and the consumed representative
- [Measure an asymmetric ICF alias without replacing the canonical representative](reviewed-cross-name-comdat-measurement.md) — c9 — cpp:comdat cpp:template | topic:fold-icf topic:pairing topic:measurement — when retail and base PDB prove a same-source specialization but base COFF selected another ICF name, rename only a disposable base-object copy for supplementary objdiff measurement; never steal the canonical representative, and never treat an unscored alignment as a match

- [A prebuilt third-party `.lib` supplies the COMDAT: base is unpaired AND compiled for a different ISA](prebuilt-lib-supplies-comdat.md) — c9 — cpp:comdat cpp:inline cpp:ctor | asm:fld1 asm:fldz asm:movss asm:xorps | topic:pairing topic:compiler-context topic:wall — target has a normal PDB record, base emits no delinked symbol yet the call site resolves; decode the call's E8 displacement and the rva has no record in binaries/rich/base/index.jsonl, the tree has a declaration but no definition, the mangled name lives in binaries.prebuilt/**/*.lib, and the linked body is x87 where the target is SSE; build-input gap, never anchor it
- [A whole objdiff unit that is 100% unpaired is a FILE-PLACEMENT bug, not a matching gap](inline-header-split-pairing.md) — c10 — cpp:header cpp:inline cpp:template | asm:none | topic:pairing topic:objdiff-units topic:file-layout — whole header unit unpaired, base_path ./dummy.obj, base emits same mangled symbols from <x>.h while target files them under <x>_inline.h; move the inline bodies, pure relocation
- [Speculative ASSERT in an inline ctor multiplies into every inline site](speculative-assert-inline-ctor.md) — c9 — cpp:assert cpp:inline cpp:ctor | asm:call | topic:assert-eater — eater bytes at every inlining consumer, ~0x10 per site, several consumers capped at once
- [Base inlines what target out-lines: a compiled-out ASSERT paid the target's inline budget](compiled-out-assert-pays-inline-budget.md) — c7 — cpp:assert cpp:inline | asm:call | topic:inline-vs-call topic:assert-eater topic:source-fidelity — old MSVC sizes the inline decision BEFORE optimization, so an assert that compiles out (constant-false guard, zero final bytes) still made the target's callee too big to inline; detect via empty/0-byte line entries at the callee body's start (not always an assert); restoring the assert flips the inline decision faithfully
- [Guarded eater WITH arg pushes = ASSERT_U / ASSERT_CMP_U / ASSERT_T_U](assert-u-eater.md) — c9 — cpp:assert cpp:macro | asm:push asm:call asm:test asm:jcc | topic:assert-eater — expression_eater, identity(false), push 0 before eater call, movzx test je, add esp, rep movsd
- [Mid-body call to a stripped/folded empty callee with a REAL source line (unmatchable)](empty-callee-mid-body.md) — c4 — cpp:assert | asm:call asm:lea | topic:fold-icf topic:inline-vs-call — mov byte[ebp-1],0 lea eax call mid-function, callee is a bare ret, arg in EAX no push, no symbol in either index
- [Empty-{} callee stub collapses the caller's body — match the call tree BOTTOM-UP](empty-callee-collapses-caller-body.md) — c8 — cpp:inline cpp:member | asm:call | topic:inline-vs-call topic:reachability topic:scoring-artifact — caller pairs but base folds N target stmts into 1 (rest TRGT_ONLY), only the loop header / non-stub call survives, low fuzzy on correct source; bodying a mid-tree fn can steal a sibling's objdiff pairing (37%->unpaired)
- [An inlined guard the callee's own COMDAT lacks belongs to a WRAPPER above it](inlined-guard-belongs-to-wrapper.md) — c9 — cpp:operator cpp:template cpp:inline cpp:const | asm:cmp asm:je | topic:inline-vs-call topic:mangling topic:structure-shape — inlined `cmp obj,src; je` before a clear()/+= pair, but the target's standalone callee COMDAT has NO guard; look one wrapper up; demangled `char const* const` param (mangles Q not P) = a by-value pcstr overload we never declared; call-site `lea` of a just-stored pointer = we bound by reference
- [Triaging a `sema` BRANCH-COUNT row: real source shape vs a core-header inline wall](branch-count-row-triage.md) — c9 — cpp:inline cpp:template cpp:operator | asm:call asm:jcc | topic:inline-vs-call topic:sema topic:triage — BRANCH-COUNT with a big block deficit; read the BASE block: ends in a call to a known small callee = inline wall (11 of 14 render/core rows), target blocks with a literal/call the base never emits = real bug; equal-count TOPOLOGY can be cross-jumping (backend::flush)
- [/GL re-inlines an out-of-line VOSTOK_*_API leaf into ~all callers: noinline+dllexport+Oy-off cure](gl-whole-program-leaf-reinline-noinline-cure.md) — c8 — cpp:inline cpp:call cpp:intrinsic | asm:call asm:lock asm:push-ebp | topic:inline-vs-call topic:whole-program topic:hack topic:frame-pointer — target out-lines interlocked_or/and (call ?interlocked_or@) base inlines lock or/and at ~all sites, standalone unpaired 15B custom-cc vs 25B std frame; cure = __declspec(noinline)+DISABLE_LINKER_GL(dllexport)+#pragma optimize("y",off) on decl+def
- [ASSERT eater's COMDAT-fold misname is scored MATCHED by report.json](assert-misname-scored-matched.md) — c8 — cpp:assert | asm:call | topic:scoring-artifact topic:fold-icf — dummy::nonnull vs finalize_impl, --view diff flags the eater call, N/M instructions equal line ~85-88%
- [TRGT_ONLY 0xc/0x12 row = compiled-out ASSERT - recover it](assert-eater-recover.md) — c10 — cpp:assert | asm:mov asm:lea asm:call | topic:assert-eater — TRGT_ONLY 0xc, TRGT_ONLY 0x12, mov byte ptr [ebp-N],0, lea eax, call empty_stub, finalize_impl, call_constructor_helper
- [`bool const` local IS codegen-visible: drops xor-before-sete, flips && temps to byte](bool-const-local-codegen.md) — c9 — cpp:bool cpp:const cpp:local | asm:sete asm:mov | topic:codegen-idiom topic:pdb-locals — xor ecx ecx before sete in base only, mov dword [ebp-N] 1/0 vs mov byte
- [A default constructor clearing seven bitfields emits `and byte, 0x80`](bitfield-default-ctor-mask.md) — c9 — cpp:bitfield cpp:ctor cpp:inline | asm:and asm:mov | topic:codegen-idiom topic:shared-header topic:source-fidelity — one-byte struct preserves only bit 7 at real construction sites; the same mask in a generic placement/copy helper proves the shared ctor initializer list even when the extracted PDB header prints an empty body
- [A flag `bool f = p && (mask)` emits branchy mov dl,1/xor dl,dl; `(mask)!=0` emits setne](ptr-guard-and-flag-vs-ne0.md) — c8 — cpp:bool cpp:if | asm:cmp asm:je asm:setne | topic:codegen-idiom topic:structure-shape — target builds flag via cmp ptr,0/je/mov dl,1/jmp/xor dl,dl + extra cmp ptr,<zero-reg> base lacks; restore the && NULL-guard
- [Inlined state getter: `cmp [m+off], K; sete` with NO temp slot = positive `return m_x == k;`](positive-getter-inline-sete.md) — c8 — cpp:bool cpp:inline cpp:return | asm:cmp asm:sete asm:movzx | topic:codegen-idiom — xor ecx ecx cmp dword [eax+11Ch] K sete cl movzx test je, no [ebp-N] bool store
- [`cmp [p],0; je; mov ecx,<3-byte fn>; test ecx,ecx; je` = smart-pointer SAFE-BOOL `if ( ptr )`](safe-bool-mov-const-test.md) — c9 — cpp:bool cpp:if cpp:smart-ptr | asm:mov asm:test asm:cmp | topic:codegen-idiom topic:condition-shape — mov reg imm32 of a folded `mov eax,[ecx]; ret` then a test that can never fail; absent = the source used `.c_ptr()` instead of the bare pointer
- [Two statement records around one `jcc` = an `if/else` assigning a pointer, NOT `x = a; if (c) x = b;`](pointer-select-two-line-records-is-if-else.md) — c8 — cpp:if cpp:else cpp:local cpp:pointer | asm:lea asm:je | topic:structure-shape topic:condition-shape — lea A / je short / lea B with NO jmp over the else arm; structure lists both leas as statements two source lines apart, none for the if; merged base statement reads TRGT_ONLY + SIZE +N
- [Lone 4-byte `mov byte[ebp-N],0` with no lea/call - origin UNRESOLVED (not an unused bool)](lone-byte-store-zero.md) — c2 — cpp:bool cpp:local cpp:const | asm:mov | topic:codegen-idiom topic:assert-eater — <0x4> statement, mov byte ptr [ebp-N],0 with no lea/call, no PDB local
- [A single `mov al,[f0] / or al,[f1] / ... / je` is `|`, not `||` - and the loads run in REVERSE source order](bitwise-or-flag-aggregate.md) — c9 — cpp:bool cpp:operator cpp:if | asm:or asm:mov asm:je | topic:codegen-idiom topic:structure-shape topic:condition-shape — target ORs N adjacent bool member loads into al with ONE je, base emits N cmp byte/jne; MSVC never folds || into |, so the source spelled |; loads come out reversed so read them bottom-up
- [bool -> u32 member store through neg/sbb/neg = a `? 1u : 0u` ternary](bool-store-ternary-boolize.md) — c8 — cpp:bool cpp:ternary cpp:member | asm:movzx asm:neg asm:sbb | topic:codegen-idiom — final store +0x6 in target, movzx neg sbb neg before the member store
- [`and X,mask; neg; sbb X,X; neg` = `bool b = ( val & MASK ) != 0;`](bool-from-mask-test.md) — c6 — cpp:bool | asm:and asm:neg asm:sbb | topic:codegen-idiom — and ecx 1 neg sbb ecx ecx neg, mask literal in the and operand
- [Bool added to an int through neg/sbb/neg = source wrote `+ ( b != 0 )`, not `+ b`](bool-addend-explicit-compare.md) — c9 — cpp:bool | asm:movzx asm:neg asm:sbb asm:add | topic:codegen-idiom — movzx neg sbb neg add, plain movzx add for the bare bool
- [`movzx; neg; sbb; neg` on a bool array index = `arr[ b != false ]`, not `arr[ b ]`](bool-index-explicit-compare.md) — c8 — cpp:bool | asm:movzx asm:neg asm:sbb | topic:codegen-idiom — full normalize before the index scale, bare movzx for plain bool index
- [The `s32 compare(a,b)` ordering family: `>` for the second test, STLport `std::min` for the loop bound](three-way-compare-family.md) — c8 — cpp:compare cpp:operator cpp:loop | asm:cmp asm:sbb asm:jbe asm:lea | topic:codegen-idiom topic:condition-shape — every cmp operand-swapped (jbe/ja vs jae/jb) over correct structure; branchless neg/sbb/and/add min vs lea/lea/mov eax,[eax] address-select; min in the for-condition recomputes each iteration (59%->31%)
- [A free fn mangled `?f@ns@@YA...` (not `??$f@...`) taking `T<X>` = friend DEFINED inside the class template](friend-in-class-template-nontemplate-mangling.md) — c9 — cpp:template cpp:friend cpp:operator | topic:mangling topic:structure-shape — target `?compare@render@vostok@@YAHABV?$res_xs@...` vs base `??$compare@...`, unpaired at identical size, body touches private members with no friend decl, N instantiations share one source-line range
- [`(T)config_value`: target keeps operator T out-of-line, base keeps cast_number](config-value-operator-cast.md) — c6 — cpp:cast cpp:operator | asm:call asm:movzx | topic:inline-vs-call — call binary_config_value::operator unsigned char vs call cast_number<unsigned char,unsigned __int64,unsigned int>
- [`static_cast_resource_ptr<P>( x.get_unmanaged_resource() )`: extra copy-construct + 8B frame](static-cast-resource-ptr-copy.md) — c7 — cpp:cast cpp:template | asm:call asm:lea asm:sub-esp | topic:inline-vs-call — sub esp 0BCh vs 0B4h, extra resource_ptr copy-construct call into a second slot
- [Redundant static_cast_resource_ptr value-loads the source; drop it when the param is the same underlying resource_ptr](redundant-static-cast-resource-ptr-value-load.md) — c8 — cpp:cast cpp:template cpp:member | asm:mov asm:lea asm:call | topic:inline-vs-call topic:convention — member resource_ptr to by-value param, base mov eax [this+off] push eax (value) vs target lea edx [this+off] push edx (address) into the copy/::set, ~43% over correct call, two typedef names of one resource_ptr type, drop cast -> 100%
- [Capture a reused member-array slot as a `T& ref = arr[idx];` so /Ox hoists its address to the prologue](reference-local-hoists-slot-address.md) — c8 — cpp:local cpp:member | asm:lea asm:mov | topic:pdb-locals topic:convention — target lea reg [this+idx*4+arr_off] once in prologue reused for all arr[idx] uses, base re-reads idx member each use, off-by-one TRGT_ONLY prologue stmt, bind a reference -> hoist matches, on_new_state 44->75% 12/13->13/13
- [/Od inlining-depth wall: target CALLS a small helper, base INLINES it (structure already matches)](od-helper-inline-depth-wall.md) — c7 — cpp:inline cpp:call cpp:helper | asm:call asm:sbb asm:strstr | topic:inline-vs-call topic:wall — STRUCTURE+locals MATCH but one stmt SIZE+/- from base inlining buffer_string::find/math::min(u32,u32)/operator<; not source-steerable, PARK
- [static_cast_checked is the project-wide cast idiom: extra temps / a `mov eax,[eax]; ret` call](static-cast-checked.md) — c9 — cpp:cast cpp:template | asm:mov asm:call | topic:codegen-idiom topic:fold-icf — cast statement 6+ bytes short, t1->t2->t3 temp chain, call bogus operator* that is mov eax [eax] ret, +0xc spill before strings::copy
- [`dest = src & 0xffff` masks through a 32-bit register; a `(u16)` cast does a direct 16-bit move](u16-mask-vs-cast.md) — c6 — cpp:cast | asm:and asm:mov | topic:codegen-idiom — mov eax and eax 0FFFFh mov [dst] ax <0xe> vs mov cx mov [dst] cx <0xa>
- [Typed pointer subtraction = `sub; cdq; idiv sizeof`](pointer-subtraction-idiv.md) — c9 — cpp:cast | asm:idiv asm:sub-esp | topic:codegen-idiom — sub eax cdq mov ecx sizeof idiv ecx
- [Cast-forward to current_state(): the chained one-liner is the 0-local form (named locals are phantom)](current-state-cast-forward-materialized-locals.md) — c7 — cpp:cast cpp:local cpp:return | asm:mov asm:call | topic:source-fidelity topic:codegen-idiom — base locals (2) state/current but target structure has NO locals block, SIZE -0x6 cast-forward, static_cast<DerivedState*>(current_state())->method()
- [A const method that assigns a member: the member is `mutable`](mutable-member-const-method.md) — c9 — cpp:const cpp:member | asm:call asm:mov | topic:structure-shape — C2678 binary '=' const left-hand operand, const method storing into this+off, @@IBE mangling
- [Sweep a module for access/CV-char mangling mismatches - each hit pairs a function for free](access-cv-char-mangling-sweep.md) — c9 — cpp:const cpp:member cpp:class | asm:none | topic:mangling topic:pairing topic:structure-shape — unpaired despite an identical-looking base fn, one-letter diff in the `@@<access><cv>E` group (QAE vs QBE, UBE vs EBE, AAE vs QAE); blank the group and intersect the rich indexes; fix = move the decl between access specifiers (+ friends) or drop/add const
- [Top-level const on a by-value pointer parameter gates pairing (QAV vs PAV)](param-pointer-const-mangling.md) — c9 — cpp:const | topic:mangling — QAV, PAV, QBV, PBV, not found in BASE index, unpaired with identical body
- [Top-level const on a value param: byte-neutral under /Od, NOT under optimization](top-level-const-value-param-od-vs-optimized.md) — c8 — cpp:const | topic:mangling topic:codegen-idiom — pdb_divergence fn-order `const T arg` vs `T arg` unpaired; mangling-neutral so adding const pairs them; FREE in /Od (game_core), apply to decl+def, skip east-const `T const x`; in optimized TUs const reshuffles codegen (regresses sibling fns same .cpp) -> revert there; mutating body = C3892 revert that one param
- [A `class` vs `struct` forward-decl mangles a param as PAV vs PAU (breaks the link)](class-vs-struct-forward-decl-mangling.md) — c8 — cpp:struct cpp:class | topic:mangling topic:buildability — LNK2001 unresolved external yet symbol in obj, PAU vs PAV one-letter mangle diff, class world vs struct world forward-decl
- [rva 0x03f210 is the universal empty-function fold target: a call there is whatever empty fn the source needs](fold-misname-empty-fn.md) — c9 — cpp:ctor cpp:assert | asm:call asm:mov | topic:fold-icf — call finalize_impl, call empty_stub, several unrelated empty functions at rva 0x03f210
- [A store before the first ctor body record belongs in the member initializer list](pre-body-ctor-store-is-init-list.md) — c9 — cpp:ctor cpp:init-list cpp:member | asm:mov | topic:structure-shape topic:line-records — unaddressed mov [this+member],0 before the first addressed body statement; the same store precedes that statement when the ctor is inlined
- [`lea &slot; call <misnamed ctor>; mov ecx,eax; call identity` = `float4x4( ).identity( )`](float4x4-identity-temp.md) — c6 — cpp:ctor cpp:float | asm:lea asm:call | topic:fold-icf topic:codegen-idiom — call stlp_std::allocator<char>::allocator<char> before float4x4::identity
- [Float-POD default ctor: sequential `movss [this+off], const` in declaration order](scalar-default-ctor-movss.md) — c8 — cpp:ctor cpp:init-list cpp:float | asm:movss asm:mov | topic:codegen-idiom — movss xmm0 [__real], per-member stores in offset order, gap in offset sequence
- [Value member with its own init-list ctor: sub-object `call` THEN inlined member stores](member-ctor-call-plus-inline-stores.md) — c8 — cpp:ctor cpp:init-list cpp:member | asm:call asm:movss asm:mov | topic:codegen-idiom topic:fold-icf — add ecx call misnamed ctor then explicit stores at the same member offset
- [Derived state ctor: base delegation + compiler-emitted vtable stores, EMPTY body](state-ctor-vtable-stores.md) — c9 — cpp:ctor cpp:init-list cpp:virtual | asm:call asm:mov | topic:codegen-idiom — mov [ecx] ??_7, mov [edx+18h] ??_7, vtable stores after base-ctor call
- [Recover an INLINE ctor body from its consumer expansions (never guess NULL)](inline-ctor-body-from-expansions.md) — c8 — cpp:ctor cpp:inline cpp:init-list | asm:mov asm:call | topic:inline-vs-call — header-inline ctor no standalone symbol, mov [reader+4] eax after folded buffer() call
- [An inline ctor pairs only once a non-inlining out-of-line caller is bodied](inline-ctor-pairs-when-root-caller-bodied.md) — c8 — cpp:ctor cpp:inline cpp:reachability | asm:call | topic:inline-vs-call topic:reachability topic:un-dce — ctor body already correct in *_inline.h but unpaired, no base COMDAT, target HAS standalone symbol; body the stub ROOT caller's new(p)T(...) to force it out-of-line (paired 2 ctors via new_animation 0.27->50%)
- [`tcp_packet packet( allocator )`: target CALLs the ctor, base inlines the field stores](tcp-packet-ctor-out-of-line.md) — c7 — cpp:ctor cpp:inline | asm:call asm:mov | topic:inline-vs-call topic:structure-shape — target push g_mt_allocator + call tcp_packet::tcp_packet, base inlines the 4 member stores, packet builders capped ~74-82% STRUCTURE MATCH, engine-wide de-inline knob
- [Chained-temporary parameters object = ONE statement (and ONE less local)](chained-temporary-params-object.md) — c8 — cpp:ctor cpp:local | asm:call | topic:structure-shape topic:pdb-locals — single big row ~0xa3 where base has params + setter chain + ctor rows, PDB lists NO local for the params
- [Builder/setter chain on ONE object: target wrote SEPARATE statements, not a fluent chain](builder-chain-separate-statements.md) — c8 — cpp:method cpp:inline | asm:lea asm:call | topic:structure-shape topic:source-fidelity — N tiny per-line `lea &obj; call obj::setter` rows vs base ONE collapsed row, each call reloads the SAME `lea [ebp-N]` not the prior return
- [Array-walk loop with 0 target locals = std::for_each (iterator lives in the inlined helper)](array-walk-zero-locals-is-for-each.md) — c9 — cpp:loop cpp:local cpp:template | asm:cmp asm:jmp asm:add | topic:inline-helper topic:source-fidelity — target 0 named locals, init-ptr cmp-end body add-ptr-sizeof jmp loop, raw for-loop adds +1 local; sibling fns std::for_each the same container
- [Inline accessor RETURN materializes a double-store the raw expression doesn't](inline-accessor-return-materializes-call-operand.md) — c8 — cpp:inline cpp:call cpp:temporary | asm:mov | topic:inline-helper topic:codegen-idiom — call/min operand with [ebp-A]->[ebp-B] double-store and NO call between, raw inline expr ~14% short; operand is an accessor return not the expression (counter-ex: by-value predicate made booby_trap worse)
- [Constructed-arg temp: inline T(NULL) (0 locals, 1 stmt) vs named T tmp; f(tmp) (+1 local, ctor-first)](inline-temp-vs-named-temp-ctor-order.md) — c7 — cpp:ctor cpp:local cpp:temporary | asm:push asm:call | topic:source-fidelity topic:arg-eval-order — target 0 locals + 1 stmt, SIZE -0x3 ctor reschedule, named form byte-100 but 2 stmts
- [Repeated accessor reloaded in base, CSE'd to ONE held pointer in target — cache the reference](cse-accessor-cached-reference.md) — c8 — cpp:reference cpp:accessor cpp:local | asm:mov asm:reg-alloc | topic:cse topic:pdb-locals — base re-derives `m_x.accessor()` per use (mov [..+18h]; mov [..+374h] repeated), target holds it once in a callee-saved reg across an intervening call, 0 named locals; cache `T& r = ...` (enregisters, no phantom local), 19%->70%
- [`cmp [X],0 / call iterate_keys<key_initializator> / cmp [X],1` = an inlined `command_line::key` guard](command-line-key-is-set-inlined.md) — c9 — cpp:if cpp:static cpp:global | asm:cmp asm:call asm:jcc | topic:codegen-idiom topic:data-recovery — cmp dword [G],0 jne, mov [G],1, call iterate_keys<key_initializator>, cmp [G],1 jne skip; recover name via `dynamic initializer for 's_x'` + .data pointers below m_type
- [The entry guard vanished from the callee - LTCG partial-inlined it into the caller](ltcg-partial-inlined-entry-guard.md) — c9 — cpp:if | asm:cmp asm:je | topic:inlining topic:ltcg topic:diagnosis — base callee has NO cmp though the source has the guard, is ~0xd short with an interleaved prologue; the caller shows `cmp <global>,0; je` right before the call
- [An unexplained entry guard over the whole body = a file-scope debug switch (cc_bool / command_line::key)](entry-guard-is-file-scope-debug-switch.md) — c9 — cpp:if cpp:global cpp:static | asm:cmp asm:je asm:call | topic:reconstruct-global topic:data-recovery — cmp byte ptr [<data addr>],0 je end as the FIRST statement, `dynamic initializer for 's_xxx'` in the target file dump, call iterate_keys<key_initializator>
- [`call single_threading_policy::increment<unsigned int>` where the target has `inc dword ptr [eax]`](refcount-policy-assert-blocks-inlining.md) — c9 — cpp:template cpp:assert cpp:inline | asm:call asm:inc asm:add | topic:inline-wall topic:codegen-idiom — base calls the refcount policy out of line at every intrusive_ptr assign because its ASSERT emits empty_stub; target inlines it; engine-wide SIZE wall, not a caller shape bug
- [A byte diff that flags no `j*` row is NOT proof the control flow matches](masked-diff-hides-branch-target.md) — c9 — cpp:if cpp:loop cpp:compare | asm:jcc asm:jmp | topic:tooling topic:condition-shape topic:structure-shape — `--view diff` all operand `~` rows and no branch row, or `~ je short 00000037h -> je short 000000E8h` (two address spaces); ask `vostok sema blocks --diff --lite` (destinations by BLOCK INDEX) first, NOT `branches --diff` (pairs positionally); ORDER-ONLY = isomorphic CFG and a first-divergence on a branchless block = uncontracted padding - do NOT "fix" the polarity
- [`cmp K1; je THEN / cmp K2; jle ELSE` fall-through = `x == K1 || x > K2` (disjoint, not >=)](two-compare-fallthrough-disjoint-condition.md) — c7 — cpp:if cpp:enum | asm:cmp asm:je asm:jle | topic:condition-shape — two compares diff constants, je-then jle-else fall-through-then, apparently-dead `> K2` clause
- [Derived ctor BODY re-assigns an inherited member the base ctor already set](derived-ctor-overwrites-inherited.md) — c8 — cpp:ctor cpp:member | asm:mov | topic:structure-shape — store at an inherited offset AFTER the init-list member stores, 0FFFFFFFDh
- [Derived class reads a base class's PRIVATE members directly = grant friend access](derived-reads-base-private-direct.md) — c9 — cpp:member cpp:inheritance | asm:mov asm:cmp | topic:inline-vs-call topic:structure-shape topic:buildability — direct [this+off] read of a base-private member where the out-of-line accessor would be a `call`, parked "private to base no inline getter", codegen-neutral `friend class Derived;` unlocks many fns at once
- [A single low-byte store into a multi-byte member = a single-FIELD write, not a multi-arg ctor](single-field-store-setter.md) — c6 — cpp:ctor cpp:member | asm:mov | topic:structure-shape — one byte store where source builds the whole object, mov byte[tmp] 64h
- [Copy ctor delegating to operator= + member-wise scalar operator=](copy-ctor-delegates-operator-assign.md) — c9 — cpp:ctor cpp:operator | asm:call asm:fld asm:fstp asm:cmp | topic:codegen-idiom — push other call operator= ret 4, cmp eax [ebp+8] je self-assign guard, fld/fstp pairs per member
- [fixed_string<N>("literal"): the 3-arg buffer_string base-ctor expansion vs a ctor `call`](fixed-string-ctor-inline.md) — c7 — cpp:ctor cpp:string cpp:template | asm:push asm:lea asm:call asm:mov | topic:inline-vs-call — mov [ebp-XX] N lea push &capacity, call buffer_string::buffer_string 3 args, ??_C@ literal push
- [`dest = s.substr(pos,n)` (by-value temp + memcpy) vs `s.substr(pos,n,&dest)` (in-place)](substr-byvalue-vs-outparam.md) — c8 — cpp:string cpp:template cpp:call | asm:lea asm:call asm:memcpy | topic:inline-vs-call topic:structure-shape — extra N-byte stack temp, call buffer_string::substr then a separate memcpy into dest, clear-then-copy (mov byte[dest],0)
- [Derived ctor: base-ctor `call` vs inlined init is decided by the BASE class declaration](base-ctor-call-vs-inlined-init.md) — c8 — cpp:ctor | asm:call asm:mov | topic:inline-vs-call topic:structure-shape — call Base::Base one call no args, inlined grand-base ctor + mov dword ptr [eax],0 vtable store
- [Delinker masks BASE address-range immediates but keeps the target's (unmatchable operands)](delinker-masks-base-immediates.md) — c5 — cpp:ctor | asm:mov | topic:tooling topic:scoring-artifact — mov ecx imm32 zeroed in delinked base obj only, % stuck with structure 0 diffs
- [LTCG dead-store elimination: an anchored-only ctor compiles EMPTY](ltcg-dse-empty-ctor.md) — c7 — cpp:ctor | topic:anchoring topic:codegen-idiom — ctor body with no member stores, push ebp mov ebp esp push ecx mov [ebp-4] ecx mov eax leave ret
- [Destruct-without-freeing helper = `obj.~T()`, NOT `delete &obj`](explicit-dtor-call-not-delete.md) — c8 — cpp:dtor cpp:new-delete | asm:call | topic:allocator — ~0x2c body vs ~0x50 with inlined operator delete, call_destructor helper
- [`push 0; call [vptr+0]` = explicit VIRTUAL dtor call (flag 0: destroy, don't free)](virtual-dtor-explicit-call.md) — c8 — cpp:dtor cpp:virtual | asm:push asm:call asm:mov | topic:codegen-idiom — push 0 mov edx [eax] call eax, 0xe destroy row vs 0x1b direct dtor + flag dance
- [Compiler-generated dtor epilogue: missing member-dtor `this` setup is ICF folding](compiler-dtor-icf-this-setup.md) — c6 — cpp:dtor | asm:add asm:call | topic:fold-icf — target add ecx 0xNN before each member ~T() call, base omits the add ecx setups
- [`math::pow(x, INT)` binds pow(float,int): target inlines the sign-dispatch, base calls](math-pow-int-inline.md) — c6 — cpp:float cpp:cast | asm:call asm:test | topic:inline-vs-call — mov edx 5 test jne call pow_impl inlined vs mov ecx 5 call vostok::math::pow
- [u32 difference to float: store low/zero high, `fild qword`, fdiv](u32-delta-to-float.md) — c9 — cpp:float cpp:cast | asm:fild asm:mov | topic:codegen-idiom — fild qword, zeroed high dword, fdiv [__real@447a0000]
- [Named float-const global vs literal at a `float const&` parameter](named-float-const-global.md) — c6 — cpp:float cpp:const | asm:push asm:movss asm:lea | topic:codegen-idiom — push <global> vs movss to a temp then lea/push of the temp
- [math::min/max xmm operand order reveals (and is steered by) SOURCE argument order](min-max-operand-order.md) — c9 — cpp:float cpp:operator | asm:movss asm:call | topic:codegen-idiom — movss xmm0/xmm1 swapped vs target, movss xmm1 xmm0 shuffle before call min

- [`math::max(a,b,c)` on floats binds the by-const-ref template and never inlines: retail spelled nested 2-arg max](three-arg-max-template-vs-nested-byvalue.md) — c8 — cpp:template cpp:float cpp:call | asm:call asm:comiss asm:fdiv | topic:inline-vs-call topic:overload-resolution topic:structure-shape — call vostok::math::max<float> with three pushed POINTERS, ??$max@M...ABM00@Z/??$max@M...ABM0@Z base-only symbols, x87 fdiv tail where target is all-SSE, return SPLIT into a BASE_ONLY second record; spell max(x, max(y,z)) to bind the by-value max(float,float)
- [Bare float literals 0.0f / 1.0f load as fldz / fld1](float-literal-fpu.md) — c9 — cpp:float | asm:fld asm:movss | topic:codegen-idiom — fldz, fld1, movss xmm [__real@...] when a named constant was used instead
- [`and 0x7FFFFFFF` through a stack slot = `math::abs(float)`; the compare polarity names the guard](float-abs-bit-mask-and-epsilon-polarity.md) — c9 — cpp:float cpp:if cpp:inline | asm:and asm:comiss asm:jbe | topic:codegen-idiom topic:condition-shape — movss [esp+N] / mov reg / and 7FFFFFFFh / movss back / comiss vs epsilon_5; jbe = `abs(a-b) > eps`, jb = `!is_similar`, jae = `is_zero`; the temp often lands in the dead incoming-arg slot
- [objdiff None from differing delinker constant-pool symbol names (body byte-identical)](none-constant-pool-names.md) — c6 — cpp:float | asm:movss | topic:scoring-artifact topic:fold-icf — structure-diff fully aligned 0 diffs yet scores None, same 0.0f reloc named differently per side
- [Counted loop exiting on `je` (not `jae`) = source wrote `i != N`, not `i < N`](loop-comparator-je-vs-jae.md) — c9 — cpp:for | asm:cmp asm:jcc | topic:codegen-idiom — cmp [i] N je .end vs jae .end
- [For-head merges: increment list, comma-operator counters, and both inits declared IN the for](for-head-statement-merges.md) — c9 — cpp:for | asm:jmp asm:cmp | topic:structure-shape topic:pdb-locals — one loop-head row = sum of while+increment+backjump rows, jmp short .test entry, BASE_ONLY ++counter row
- [Hoisted `.size()` with NO named local = a comma-declared for-init bound](for-init-comma-size-hoist.md) — c9 — cpp:for cpp:local | asm:cmp asm:sub asm:sar | topic:structure-shape topic:pdb-locals topic:cse — target computes v.size() once before the loop (cmp i,reg), base re-evals each iteration, NO i/count local; `for ( u32 i = 0, n = v.size(); i < n; ++i )`, split the for-init across lines to place size on its own line
- [`if (a && b)` materializing a THIRD bool temp = an inlined predicate, not a bare `&&`](and-chain-third-bool-temp.md) — c8 — cpp:if cpp:bool cpp:inline | asm:mov asm:movzx asm:test | topic:structure-shape — extra [ebp-0Ch] && result slot retested, sub esp 0Ch vs 08, per-member mov al round-trips
- [`call __lower_bound<...,bool (__cdecl*)(...)>` where the target inlines the binary search = OUR predicate is too big](trivial-predicate-unlocks-stl-inline.md) — c9 — cpp:template cpp:function-pointer cpp:stl | asm:call asm:cmp asm:sar | topic:inline-vs-call topic:stlport topic:structure-shape — base calls stlp_std::priv::__lower_bound, target open-codes mov ecx,eax/sar ecx,1/cmp [edx+ecx*8+K]/jae with the comparison inlined; fetch the predicate's own target body (19B = one clause), and check the scan loop's tail for a guard term we invented
- [If-condition materializing a bool (cmp/sete/movzx/test/je) = an INLINED bool helper, not `if(!x)`](inlined-bool-helper-in-condition.md) — c7 — cpp:if cpp:bool cpp:inline | asm:sete asm:movzx asm:test | topic:structure-shape — cmp [this+OFF] 0 sete dl movzx test je vs direct cmp jne
- [Smart-ptr returned by value in a `&&` chain: temp construct + safe-bool test + dtor](intrusive-ptr-byvalue-bool-chain.md) — c6 — cpp:if cpp:bool | asm:sete asm:cmp asm:call | topic:codegen-idiom — or dword [ebp-18h] 1 EH guard, intrusive_ptr::set then cmp 0 sete then ::dec, and 0FFFFFFFEh
- [`if ( T x = expr )` decl-in-if: ONE record covering decl + test, opening its own [n] block](decl-in-if-guard.md) — c8 — cpp:if cpp:local | asm:cmp asm:test asm:jcc | topic:pdb-locals topic:structure-shape — record covering init AND cmp/je, local listed in a [n] block opening at that statement, [1][2][3] consecutive depths
- [Init + double-compare in ONE statement = assignment-in-condition `if ( ( x = m ) == A || x > B )`](assignment-in-condition.md) — c8 — cpp:if cpp:local | asm:mov asm:cmp asm:jcc | topic:structure-shape topic:pdb-locals — one record with member load + temp store + two compares, no user local recorded, slot order swapped
- [A lone 2-byte TRGT_ONLY branch row after a guard = inverted guard + early `return;`](inverted-guard-early-return.md) — c9 — cpp:if cpp:return | asm:jmp asm:jcc | topic:structure-shape — TRGT_ONLY 0x2 bare branch with its own line, between guard test and body
- [Double join-`jmp` per return leaf = an else chain; flatten to early returns](early-returns-vs-else-join.md) — c8 — cpp:if cpp:return | asm:jmp | topic:structure-shape — jmp .13 jmp .5 double jump per leaf, scores None too divergent
- [Two identical `jmp .end` back-to-back = a returning if-block's `}` jumping OVER an else](else-block-double-jmp.md) — c8 — cpp:if cpp:return | asm:jmp | topic:structure-shape — +0x002 brace row sandwiched between if-body last statement and else statement, two jmp short .end
- [A fat arm calling the function's OWN bind + async op = a RETRY statement](async-retry-arm.md) — c8 — cpp:if cpp:template | asm:call asm:rep-movsd | topic:structure-shape — bind_t template args name the enclosing function, rep movsd 7 dwords endpoint deref, increment() row 0x8
- [Recover an INVERTED if/else from line records + branch sizes + jmp widths](inverted-if-else-recovery.md) — c8 — cpp:if | asm:jmp asm:jcc | topic:structure-shape — TRGT_ONLY pair 0x1a+0x5 after the if row, BASE_ONLY pair 0x2+0x1a after the then-block, equal stmt counts shifted
- [`else if` merges chain-jmp + test into ONE row; flat sequential `if`s split them](flat-ifs-vs-else-if.md) — c9 — cpp:if | asm:jmp asm:test | topic:structure-shape — mid-chain row test-sized too SMALL target-side, next row LOG-block-sized too BIG, ONLY-target row at the LOG line
- [rel32 brace-exit jmp gets its own line record; rel8 folds into the previous statement](rel8-rel32-brace-exit.md) — c6 — cpp:if | asm:jmp | topic:structure-shape topic:scoring-artifact — one TRGT_ONLY 0x5 row at an if-branch closing }, preceding base statement SIZE +0x5, 21-vs-20 stmts byte-identical
- [A multi-line `&&` if = ONE record at its LAST line; TWO records = NESTED ifs](nested-if-vs-multiline-and.md) — c8 — cpp:if | asm:test asm:jcc | topic:structure-shape topic:pdb-locals — condition split into two statements whose sizes SUM to the base's single if row, off-by-one stmt counts
- [Recovering `math::color_rgba( r, g, b, a )` from the inlined soft-float `math::floor` chain](softfloat-floor-color-rgba-recovery.md) — c9 — cpp:call cpp:float | asm:shr asm:sar asm:sbb | topic:color topic:inlining topic:constant-recovery — four ~0x90 blocks of pure integer bit twiddling (float stored then reloaded as bits, `sar 17h`/`and 0FFh`/`9Eh - exp`) feeding one packed u32; lanes a/r/g/b, constants are already x255
- [A multi-line statement's line record lands on its CLOSING `);`; carcass `<N>` gaps + record deltas recover the source layout line-for-line](multiline-statement-closing-line-record.md) — c8 — cpp:call cpp:array cpp:initializer | asm:none | topic:structure-shape topic:line-records — wide irregular gaps between records, last initializer element has NO record, delta from last element row to the call row = the call's line count
- [The rich index's per-function line map recovers a whole TU's source layout, not just its order](pdb-file-line-map-recovers-tu-layout.md) — c9 — cpp:file-layout cpp:macro | asm:push | topic:line-records topic:structure-shape topic:logging topic:data-recovery — unreachable __LINE__ immediate, every __LINE__/__FILE__ site off by a constant, need the extent of a compiled-out #ifndef MASTER_GOLD block, need the only file-scope room a no-dynamic-init global can occupy; one jq over `file` + `statements[].line` prints the whole TU's geometry
- [A gap in an /Ox line map is often a SUNK store, not compiled-out code - and you must not "fix" it by moving the statement](sunk-store-statement-loses-line-record.md) — c9 — cpp:local cpp:member cpp:assignment | asm:mov asm:inc | topic:line-records topic:optimized-target topic:structure-shape topic:scheduling — no record where a store/`= 0` initialiser must exist, structure-diff BASE_ONLY on a plain assignment the target never records, the store's mov/inc sits inside a LATER record's address range; place the statement in the gap BEFORE the absorbing record, never re-order to force a record merge (measured 97.40 -> 90.90)
- [objdiff None can mean "body too divergent", not only bad mangling - and REPEATED blocks force it at ~90% equal](none-body-too-divergent.md) — c7 — cpp:if cpp:macro | topic:scoring-artifact topic:logging — fuzzy_match_percent omitted, mangled name matches exactly, symbol present in base obj, objdiff-cli match_percent 0.0 while pdb_fetch --view diff reports most instructions equal (N repeated LOG_*/ASSERT expansions defeat the aligner)
- [Two trivial accessors in ONE `&&` can split inline-vs-call (one matches, one residual)](two-accessors-one-and-split.md) — c6 — cpp:inline cpp:if cpp:bool | asm:call asm:mov | topic:inline-vs-call — one getter call only on target side inside a && chain, [ebp-N] slot shift
- [Trivial header getter: target `call`s it, our /GL inlines the member read (wall)](inline-vs-call-trivial-getter.md) — c7 — cpp:inline cpp:member cpp:bool | asm:call asm:mov | topic:inline-vs-call — call is_aimed vs mov al [obj+488h], getter standalone in target index only, no inline keyword
- [Target calls a trivial accessor our header inlines: move the body to the .cpp (steerable)](outline-accessor-to-recover-call.md) — c9 — cpp:inline cpp:member | asm:call asm:sub-esp | topic:inline-vs-call — call accessor in target vs inlined mov ax [reg+off] in base, sub esp differs ~4, register cascade
- [Inline accessor leaves a +0xc call-result temp; direct member access does not](accessor-call-result-temp.md) — c8 — cpp:inline cpp:member | asm:mov | topic:inline-vs-call — mov ecx [member] mov [ebp-TEMP] ecx mov eax [ebp-TEMP], +0xc per accessor use
- [Single-TU anchor INLINES a now-real inline helper the target CALLs](single-tu-anchor-inlines-helper.md) — c7 — cpp:inline cpp:template | asm:call | topic:inline-vs-call topic:anchoring — target single call <helper> vs base inlines helper body at site, helper absent from base index, 0%
- [De-inlining a class-template accessor (intrusive_list::front) is a GLOBAL knob; target mixes inline-and-call per instantiation](template-accessor-deinline-global-knob.md) — c7 — cpp:template cpp:inline cpp:member | asm:call asm:mov | topic:inline-vs-call topic:convention — call bare-ret then call mov eax,[eax+8] ret with this in EAX, fsm states front family, sibling TUs stuck on one statement, cross-module 93-97% regressions after the flip; ALSO boost::function4/5::operator() (login-auth callback sites): target out-of-lines the COMDAT (5-byte call), base inlines empty/throw/get_vtable body (+0x3d/+0x49 per site), 47-90% handlers all STRUCTURE MATCH, de-inline = strip implicit-inline from function_template.hpp engine-wide
- [Same-line merges: ONE line-table statement covers everything on the line (size-sum tell)](same-line-statement-merges.md) — c9 — cpp:local cpp:assert cpp:if | asm:jmp | topic:pdb-locals topic:structure-shape — TRGT_ONLY/BASE_ONLY pair whose sizes ADD UP exactly to the other side's single row
- [Single-request `query_resources*` builds the request INLINE via `&create_request(...)`, not a named `request[]` array](single-request-query-inline-create-request.md) — c9 — cpp:local cpp:temporary cpp:array | asm:mov asm:lea asm:push | topic:pdb-locals topic:source-fidelity topic:structure-shape — target 0 named locals + 1 stmt, base phantom `requests[1]` + 2 stmts, request built inline on the frame, multi-element case KEEPS the array local
- [A `[idx*sizeof + symbol]` base load with a `PAY0<N>` mangled global = a 2D array `T g[M][N]`](flat-2d-array-global-indexing.md) — c8 — cpp:array cpp:global | asm:lea asm:cmp | topic:structure-shape topic:data-symbol — flat `lea [flat*8+?g_...@@3PAY09U...@A]`, row stride folded as `*N` earlier, `Y0<N>` pins inner dim, outer M from the guard bound, element bytes score as a separate data symbol
- [By-value temp built BEFORE the trailing arg pushes: hoist it to a NAMED local](byvalue-temp-named-local-hoist.md) — c9 — cpp:local cpp:ctor | asm:push asm:lea asm:call | topic:structure-shape — temp ctor before push this in target, inline rvalue pushes this first in base
- [Hoisting a temporary's `.c.xyz()` binds a float3 REFERENCE, not a full-matrix local](cxyz-reference-hoist.md) — c8 — cpp:local cpp:float cpp:const | asm:add asm:mov | topic:structure-shape — add eax offset then accessor call then mov [slot] eax pointer store, no full struct copy
- [Bone matrix index re-called per use: do NOT hoist `get_root_bones_count()`](matrix-index-no-hoist.md) — c8 — cpp:local cpp:member | asm:call asm:sub-esp | topic:structure-shape — repeated mov eax [m_skeleton] call get_root_bones_count per matrix site, shl edx 6
- [SIZE rows decomposing into 3-byte deltas = disp8-vs-disp32 frame noise](disp8-disp32-size-rows.md) — c8 — cpp:local | asm:mov | topic:pdb-locals topic:convention — SIZE rows +-0x6..0x12, slices identical except [ebp-8Ch] vs [ebp-80h], 3 bytes per access
- [N sequential `[1]` block-opens = N SEPARATE sibling braced scopes (not nesting)](sibling-braced-scopes.md) — c7 — cpp:local | asm:mov | topic:pdb-locals topic:structure-shape — depth resets to [1] each time never [2], slot-rename storm collapses when braced
- [Byte-identical body, `sub esp` off by 4-8 = /Od frame-slot allocation noise (stop at PARTIAL)](od-frame-slot-noise.md) — c8 — cpp:local | asm:sub-esp asm:mov | topic:convention topic:pdb-locals — sub esp 1Ch vs 18h, sub esp 38h vs 30h, saved-this slot shifted, all [ebp-N] shifted by a constant
- [LOG expansion: base schedules the boost::function1 ctor at the append site (target: block entry)](log-callback-ctor-schedule.md) — c6 — cpp:macro | asm:lea asm:call asm:push | topic:logging topic:convention — lea eax [fn_slot] call fn-ctor at block entry vs lea esi at append, guard-bit cleanups swapped
- [Unused-parameter eater: VOSTOK_UNREFERENCED_PARAMETER(S)](unreferenced-params-eater.md) — c9 — cpp:macro | asm:lea asm:push asm:call | topic:assert-eater — unreferenced_parameter_helper, (void)(&p), lone lea of a param slot, identity(false), rep movsd; UNGUARDED push;push;call;add esp (no guard, no push 0) = direct helper call NOT macro (guarded macro DCE-collapses to bare ret); class-ref arg passed by &address pointer-push
- [LOG_* __FILE__ residual is permanent on logging-heavy functions (path differs)](log-file-path-wall.md) — c9 — cpp:macro | asm:push | topic:logging — push path-string never matches, C:\survarium\sources vs Z:\home, functions capped 40-52%
- [LOG_* sites: pushed verbosity picks the macro; pushed __LINE__ pins the layout (do NOT pad)](log-line-verbosity-immediates.md) — c9 — cpp:macro | asm:push | topic:logging — push 2 push 3 push 4 verbosity twice, push 0ACh line immediate, __LINE__
- [0%/unpaired/None with plausible source = check the mangled join FIRST](mangled-join-failure.md) — c9 — cpp:member cpp:const | topic:mangling topic:scoring-artifact — 0% unpaired, scores None, not found in BASE index, @@EBE vs @@UBE, QAV vs PAV, whole unit at 0
- [boost::asio socket open/bind/endpoint idioms (all match from plain source)](asio-socket-idioms.md) — c8 — cpp:member cpp:ctor | asm:cmp asm:lea asm:rep-movsd | topic:codegen-idiom — cmp dword[socket+4] -1 setne, mov dword[ebp-4] 2 protocol temp, zero 7 dwords endpoint, sub esp 51Ch gap
- [`.i/.j/.k/.c.xyz()` folds: misnamed `finalize_impl` call, row offset in the `add`](xyz-fold-misname.md) — c9 — cpp:member cpp:float | asm:add asm:call asm:lea | topic:fold-icf — add eax 30h call finalize_impl, lea eax [mat] call finalize_impl, add 10h/20h
- [Thin forwarder: float/int arg in xmm0/eax vs spilled to stack = LTCG call-boundary residual](forwarder-arg-register-residual.md) — c7 — cpp:member cpp:float | asm:movss asm:fld asm:fstp | topic:convention — movss xmm0 [ebp+..] vs fld fstp [esp], sibling overloads 100% same shape
- [Private LTCG-inlined helper has NO base symbol: None / "not in base index" = STATE[INLINED]](inlined-helper-no-base-symbol.md) — c6 — cpp:member cpp:inline | topic:scoring-artifact topic:inline-vs-call — not found in BASE index, None, exists out-of-line in TARGET only, private AAE helper
- [`packet.append( field )`: scalar overloads push the VALUE, buffer/float3 push an ADDRESS](packet-append-overload.md) — c8 — cpp:member cpp:operator | asm:push asm:lea | topic:codegen-idiom — push eax value vs lea/push of a stack slot before append
- [A `this`-unused trivial member is FRAMELESS in the target, framed under /Od (None|PARTIAL)](frameless-this-unused-leaf.md) — c7 — cpp:member cpp:return | asm:fld asm:ret | topic:scoring-artifact topic:convention — target body d9 ee c3 fldz ret, b8 reloc c2 0400, base full frame 11+ bytes, fuzzy None
- [Non-virtual member call through a member reference = DIRECT `call`](nonvirtual-member-call-direct.md) — c9 — cpp:member cpp:virtual | asm:call asm:mov | topic:codegen-idiom — direct call survarium::Class::method, no vtable indirection
- [Member sub-object method call: `add ecx, <offset>` before the call](member-subobject-call-offset.md) — c9 — cpp:member | asm:add asm:call | topic:codegen-idiom — mov ecx this add ecx 14h call, no add for a member at offset 0
- [LTCG this-in-EAX frameless callee = the target kept it out-of-line; do NOT escape its address](eax-this-convention.md) — c4 — cpp:member | asm:call asm:mov | topic:inline-vs-call topic:convention — add eax 120h call reset, callee no push ebp reads [eax]/[eax+4], xorps frameless body
- [A carcass-anchor's SOLE call lets LTCG specialise the matched body's convention/args](anchor-sole-caller-convention.md) — c7 — cpp:member cpp:param | asm:ret asm:cmp asm:mov | topic:anchoring topic:convention — ret not ret 4, runtime param read as literal (cmp [m],0 where source says == id), param in reg not [esp+4], fix = runtime arg + member-fn-ptr volatile sink
- [A reachability anchor must CONSTRUCT + CALL with volatile-sourced args - keep() keeps the symbol but does NOT pair the body](anchor-construct-call-volatile-args.md) — c9 — cpp:member cpp:ctor | topic:anchoring topic:reachability topic:fold-icf — carcass fns unpaired despite an anchor, use_* absent from base index (count 0), keep() folds empty, literal arg lets the optimiser DCE the call; fix = construct+call routing every arg through a static volatile ptr
- [Anchor a non-virtual method by a guarded DIRECT CALL (not an address-take) to win the LTCG this-in-eax/esi/edi convention](anchor-direct-call-this-convention.md) — c9 — cpp:member | asm:mov asm:jmp asm:ret | topic:anchoring topic:convention topic:inline-vs-call topic:mangling — target reads this from eax/esi/edi not ecx, no push/pop this-reg, ret 4 arg method, member-fn-ptr sink scores 65-95% pure register cascade; fix = guarded direct call, or delete the sink once a real caller exists (breath_holding_params::load 97.26->100); private AAE/EAE methods also need target access + anchor friend
- [A write/read at `[ebp+arg]+0xNN` is a MEMBER of an argument struct, not a local](arg-struct-member-write.md) — c6 — cpp:member | asm:mov asm:add | topic:structure-shape — mov eax [ebp+8] add eax 0x1C store there, re-read by a later call
- [Per-call ref-return spill = uniform `[ebp-N]` shift, not a brace bug](per-call-ref-return-spill.md) — c6 — cpp:member | asm:mov asm:push | topic:inline-vs-call topic:pdb-locals — uniform slot deltas all the SAME constant, zero TRGT_ONLY/BASE_ONLY rows, frame +4 per spilled site
- [Single-`this`-spill prologue: `push ecx` (4-byte frame) vs `sub esp,0Ch`](single-spill-prologue.md) — c6 — cpp:member | asm:push asm:sub-esp | topic:convention — push ecx prologue vs sub esp 0Ch, this at [ebp-4] vs [ebp-0Ch], trailing alignment nops
- [Instance member scores None: access letter Q/A/I after @@ (2nd letter B = const this)](mangled-access-instance.md) — c9 — cpp:member | topic:mangling — scores None, unpaired, @@QBE, @@ABE, @@AAE, @@IAE, @@IBE, fuzzy None for a compiled fn, base obj @@QAE vs target @@IAE, top-level const on by-value param does NOT change mangling
- [A member fed to interlocked_* as the TARGET operand is threading::atomic32_type](interlocked-atomic32-member.md) — c8 — cpp:member | topic:structure-shape — COMPILE_ASSERT do_not_pass_NON_VOLATILE_values_to_INTERLOCKED_functions, PDB shows plain long
- [Carcass `// FUNCTION BODY[0xVA]` RVAs are +0x10000 off the rich index RVA](carcass-rva-offset.md) — c5 — cpp:member | topic:tooling — FUNCTION BODY[0xa72f0] vs index rva 0x972f0
- [Retail PDB type names are `__unDName` renderings — Wine's builtin msvcr90 spells them differently](pdb-names-undname-crt-wine.md) — c10 — cpp:template cpp:enum cpp:const | asm:none | topic:tooling topic:pdb-records topic:wine-environment topic:mangling — retail records `T<enum E,...>`/`const ` with trailing space, ours bare, uniformly (~1,700 vs 0); c1xx/c2/link render via msvcr90 __unDName, Wine's builtin differs; fix = native CRT into winsxs deadbeef dir + WINEDLLOVERRIDES=msvcr90=n (native_crt.py; overwrite wineboot's ~1MB stubs by size, clean rebuild to re-author compile-time records)
- [pdb_divergence `[member]` flag may be a STALE/DUPLICATE type record — confirm against the emitted asm](divergence-phantom-duplicate-type-record.md) — c8 — cpp:member cpp:struct cpp:template | asm:mov asm:cmp | topic:tooling topic:structure-shape topic:scoring-artifact — divergence `changed` row (`<u8>` vs `<u16>`, `2048` vs `8192`, `0xB20` vs `0xB28`) but base was asm-pinned; target dump has a `_1` sibling record; a value STORE width / ctor loop-bound in `pdb_fetch --view target` is the tie-breaker; network_core sequence_number/udp_match_client cluster all phantom; counter-ex particle_system_lod is a REAL single-record reorder; TWO phantom sources (target `_1` dup vs base STALE incremental-PDB record) — report.json is authoritative for "fix landed", pdb_divergence trustworthy only after a CLEAN RELINK
- [VOSTOK_FREE_IMPL vs VOSTOK_DELETE_IMPL: read the dealloc statement shape](free-vs-delete-impl.md) — c8 — cpp:new-delete cpp:macro | asm:call asm:cmp | topic:allocator — identical bytes per member regardless of triviality, if(p){free;p=0} shape, no element dtor
- [malloc_impl / delete_helper<> wrapper inline-vs-call (VOSTOK_NEW/DELETE_IMPL)](allocator-wrapper-inline.md) — c6 — cpp:new-delete cpp:macro | asm:call asm:push | topic:inline-vs-call topic:allocator — call malloc_impl vs call try_pop, call delete_helper vs delete_helper_impl with predicate bool, extra push 58h, frame +4
- [Cast a concrete global allocator to base_allocator& to keep malloc/free VIRTUAL](allocator-devirt-base-ref-cast.md) — c8 — cpp:cast cpp:new-delete cpp:member | asm:call asm:mov | topic:inline-vs-call topic:codegen-idiom topic:convention — target dispatches alloc through [g_mt_allocator+10h]/[+18h] vtable, base devirtualises to pt3malloc/pt3free + esi spill; pass static_cast<base_allocator&>(g_mt_allocator) so A=base_allocator stays virtual (no-op cast), create_world_bt 20->76% destroy_world 65->100%
- [`VOSTOK_DELETE_IMPL(g_allocator, resource)` one-liner: the strip_pointer wall (~31%); +INVERSE: gold inlines strip_pointer at an /Ox+/GL alloc site (base CALLs it), tell = base folds it as boost::get_pointer<weapon_user_animations_selector> before a virtual malloc; global __forceinline regresses ~170 fns so PARK](strip-pointer-delete-resource.md) — c7 — cpp:new-delete cpp:macro | asm:push asm:call asm:lea | topic:inline-vs-call topic:allocator — delete_helper two pushed args add esp 8 vs one arg add esp 4, ~31% with 1/1 stmts; boost::get_pointer fold-rep before mov edx,[eax];call [edx+10h] at VOSTOK_NEW/MALLOC_IMPL
- [NEW_ARRAY allocations are freed with VOSTOK_DELETE_ARRAY_IMPL (delete_array_helper_impl)](delete-array-impl.md) — c8 — cpp:new-delete cpp:macro | asm:push asm:lea asm:call | topic:allocator — call delete_array_helper_impl, lea edx [&ptr] push - takes the pointer's ADDRESS, dealloc row +0x14
- [Placement new on a mutable_buffer = `new ( buffer.c_ptr() ) T(...)`](placement-new-c-ptr.md) — c8 — cpp:new-delete | asm:call asm:cmp asm:push | topic:codegen-idiom — C2665 operator new overloads, call c_ptr misnamed operator*, post-new cmp [p] 0 je null guard
- [Allocator `new_X` / `delete_X` free-function helpers: the exact statement shapes](allocator-new-delete-helpers.md) — c9 — cpp:new-delete | asm:call | topic:allocator topic:structure-shape — cast-into-local + separate placement new, void* buffer temp, ptr = NULL
- [`circular_buffer<T>` ring-buffer member shapes (`new_item`, dtor)](circular-buffer-ring-shapes.md) — c9 — cpp:template cpp:member cpp:new-delete | asm:div asm:je asm:call | topic:structure-shape topic:codegen-idiom — lea x+x*2;shl 5 element stride, inc;xor edx;div max_count = next() remainder, placement-new je null guard on slot ptr, dtor drains via pop_tail + VOSTOK_FREE_IMPL(m_allocator,...) vtable[0x18] call_free
- [`member = T( args )`: construct temp, memberwise-copy data, destroy temp](assign-temp-object-member.md) — c8 — cpp:operator cpp:ctor | asm:lea asm:call asm:fld asm:fstp | topic:codegen-idiom topic:fold-icf — lea call T::T then fld/fstp copies skipping +0 vtable then lea ecx call dtor
- [float3 `a != b` folds to `!(operator==)` when the target has only operator==](float3-neq-folds-to-eq.md) — c6 — cpp:operator cpp:float | asm:call asm:jcc | topic:inline-vs-call — call operator== with inverted branch polarity vs call operator!=, no operator!= symbol in target
- [float3 dot `a | b`: per-call-site inline-vs-call; NEVER rewrite as dot_product](float3-dot-operator-inline.md) — c8 — cpp:operator cpp:float | asm:call asm:movss | topic:inline-vs-call — call vostok::math::operator| vs inlined mulss/addss chain, sub esp 0x24 vs 0x20
- [float4x4/float3 operator push order: first-push = RIGHT operand, hidden sret last](float4x4-operator-push-order.md) — c8 — cpp:operator cpp:float | asm:push asm:lea asm:rep-movsd | topic:codegen-idiom topic:convention — push push lea push edx call operator* add esp 0Ch, rep movsd 0x10 result chains
- [Post-increment returning by value needs a real `return` or LTCG dies with LNK1257](lnk1257-empty-byvalue-return.md) — c5 — cpp:operator cpp:return | topic:tooling — LNK1257 on an empty /* no source */ operator++(s32) body
- [`m_fn = value` (boost::function): target calls folded operator=, base may inline copy-swap-clear; +variant: assign_to inside a bind() ARG, +CRT/header-inline-wrapper siblings (strcat_s<260>, vostok::vsnprintf); +EXTREME-low-% tell (1-stmt setter at 7-9% that is STRUCTURE MATCH: function1/2 operator= inlined while function0 100%); +companion safe-bool split (`if(m_fn)` operator! idiom vs operator safe_bool conversion per-instantiation)](boost-function-assign-inline.md) — c6 — cpp:operator cpp:template cpp:bool | asm:call asm:lea asm:push asm:movzx asm:sbb | topic:inline-vs-call — one call folded operator= vs function ctor + swap + TWO call clear; OR target call function7::assign_to vs base inlined basic_vtable1::assign_to + movzx eax,al/stored_vtable, caller inline-budget driven; OR setter 7-9% STRUCTURE MATCH; OR call operator! + neg/sbb/not/and vs operator safe_bool + test eax,eax
- [animation::mixing `+` chains: keep the idiomatic spelling; the residual is a header gap](mixing-expression-operator-plus.md) — c8 — cpp:operator cpp:template | asm:call | topic:inline-vs-call topic:structure-shape — operator+ addition_lexeme cloned_in_buffer, expression::is_empty, caps ~83-85%; `result = result + get_X(...)` w/ TRGT_ONLY pair + scope-1 `expression` local = recover named-temp lexical block
- [Serial-number (RFC1982) operator< / operator<= / operator-](serial-number-operators.md) — c6 — cpp:operator | asm:cmp asm:jcc | topic:codegen-idiom — two-clause OR with +0x8000, jge vs jg one-byte diff, +0x10000 & 0x8000FFFF
- [Small-struct factory: eax:edx register-pair return / promoted convention vs base sret (wall)](small-struct-factory-abi.md) — c6 — cpp:return cpp:cast | asm:call asm:mov asm:push | topic:convention topic:fold-icf — mov [tmp] eax mov [tmp+4] edx copy chain, lea eax [esp+10Ch] sret, callee body is one ret byte, const_buffer::const_buffer bogus name
- [A fat early-`return;` row carries the inlined dtor of an in-scope local/by-value param](return-row-inlined-dtor.md) — c8 — cpp:return cpp:dtor | asm:jmp asm:call asm:cmp | topic:structure-shape topic:pdb-locals — ~0x26 return row, sp_counted_base::release on the return line, dtor walk then jmp epilogue
- [+0xC frame, byte-equal code = ELIDED named-return temp; the spelling is proven where the copy SURVIVES](named-return-temp-elision.md) — c5 — cpp:return cpp:local cpp:template | asm:sub-esp | topic:convention topic:inline-vs-call — frame exactly +0xC, instruction stream identical, only bottom-of-frame disp constants shift
- [Empty function body: the bare 5-byte /Od frame](empty-function-body.md) — c9 — cpp:return | asm:push asm:ret | topic:codegen-idiom — push ebp mov ebp esp pop ebp ret, 5 bytes
- [File-static console command (cc_bool/cc_float) + command_line::key: dynamic initializer, objdiff-UNSCORABLE](console-command-static-init.md) — c9 — cpp:static cpp:ctor | asm:push asm:call | topic:codegen-idiom topic:scoring-artifact — dynamic initializer for 's_*_cc'/'s_*', dynamic atexit destructor, ??__E/??__F, scores None, push atexit, protected_call protected_key_construct, command_line::key, global-scope static = no namespace prefix
- [Static DATA member access codes after @@: 0=private, 1=protected, 2=public](mangled-access-static-data.md) — c9 — cpp:static cpp:member | asm:mov | topic:mangling — @@0V, @@1V, @@2V, @@2PAV, @@0IA, mov ecx ?m_x@ reloc differs
- [Static member function access codes after @@: C=private, K=protected, S=public](mangled-access-static-fn.md) — c9 — cpp:static cpp:member | topic:mangling — @@CAI, @@KAI, @@SAI, scores None for a static member in the base obj
- [Emitting a DCE'd static helper: reconstruct its LTCG-inlined caller and anchor THAT](dced-static-helper-anchor.md) — c9 — cpp:static | topic:anchoring — target static with NO direct caller (only address-taken via boost::bind), our base never emits it
- [Report-unpaired function with a PLAIN (demangled-style) target name = a file STATIC](static-plain-name-pairing.md) — c9 — cpp:static | topic:mangling topic:anchoring — plain name in report.json (no ?@@ mangle), fuzzy None, survarium::foo listed undecorated, `static` -> `ns::f` vs anonymous namespace -> ``ns::`anonymous namespace'::f``, rich-index mangled-field sweep
- [One short helper name, several INDEPENDENT per-TU definitions with DIFFERENT bodies - attribute each COMDAT to its TU by RVA adjacency](per-tu-duplicated-static-helper.md) — c9 — cpp:static cpp:inline cpp:header | asm:call | topic:file-layout topic:pairing topic:data-recovery — same demangled name under several `file:` values with DIFFERENT sizes, a `.cpp` in `file:` means a definition IN that .cpp (so it cannot also include the header), header COMDAT's owning TU found by sorting the index by rva and reading its neighbours
- [Overloaded free function: delinker drops the param list, both overloads share one bare demangled name (unpairable)](overloaded-free-fn-demangled-name-unpairable.md) — c8 — cpp:overload cpp:static | topic:mangling topic:scoring-artifact topic:tooling — free-fn overloads under ONE bare demangled name (no ?@@, no params), every overload unpaired/None despite byte-correct source, base obj has ?fn@ns@@YA...@Z, verify by RVA diff not objdiff
- [Array brace-init: one literal per LINE = one statement per element](string-array-brace-init-lines.md) — c9 — cpp:string cpp:local | asm:mov | topic:pdb-locals topic:structure-shape — SIZE diff on a captions[N] line, N separate 0x7 statements vs one N*0x7, EMPTY only base rows
- [A 2-byte `jmp short` row = a closing brace `}` (case blocks, braced loop bodies); 3+ nested loops need K&R braces (brace on the for-line) or the closing rows fold](closing-brace-jmp-rows.md) — c8 — cpp:switch cpp:for | asm:jmp | topic:structure-shape — +0x002 carcass step, N trailing 0x2 rows after a loop body, base body row +2 per missing one; Allman vs K&R flips opening-vs-closing attribution at depth>=3
- [2-value case cluster = jl/jle range check + switch temp; unreachable `break;` still emits its jmp](switch-2value-cluster.md) — c8 — cpp:switch cpp:return | asm:cmp asm:jcc asm:jmp | topic:structure-shape — 4-row skeleton 0x14/0x4/0x2/0x2, switch temp copy, unreachable 2-byte jmp on its own line
- [Case whose body equals the default: `case 0: return X;`, NOT `case 0: break;`](switch-case0-return-not-break.md) — c8 — cpp:switch cpp:return | asm:cmp asm:jcc | topic:structure-shape — explicit cmp [v] 0 je to the shared return block, scores None with break
- [Switch jump-table bounds check (`cmp/ja`) tracks the `default:`: NODEFAULT() drops it](switch-default-nodefault.md) — c9 — cpp:switch | asm:cmp asm:jmp asm:jcc | topic:structure-shape — cmp [tmp] N ja default before jmp [table], head SIZE diff 0xa, BASE_ONLY 0x2 break row
- [MSVC /Od drops the case-compare for a case sharing the `default:` arm (non-steerable)](switch-redundant-case-compare.md) — c6 — cpp:switch | asm:cmp | topic:structure-shape topic:convention — target emits N compares incl. a redundant one, base N-1, +0x3 on the dispatch row
- [Jump-table switch: read the case order from the obj's own .text relocs](switch-jump-table-relocs.md) — c8 — cpp:switch | asm:jmp asm:movss | topic:structure-shape topic:tooling — jmp dword ptr [edx*4+table], trailing garbage after ret, add eax 6B00595Fh
- [NODEFAULT jump-table switch: diff FOOTER reads ~55-65% while report.json says 100](switch-footer-vs-report.md) — c8 — cpp:switch | asm:jmp | topic:scoring-artifact — (bad) instructions past ret, jmp .N vs jmp .N+1 label shift, footer % far below report.json
- [Template param by-value vs const&: mangled V?$ vs ABV?$ + caller temp-destruction shape](template-param-byvalue-vs-constref.md) — c9 — cpp:template cpp:const | asm:push asm:lea | topic:mangling topic:convention — V?$ vs ABV?$, push ecx mov esi esp, add esp 4 no dtor, lea ecx call intrusive_ptr::dec
- [Explicit-specialization DECLARATION does not stop MSVC8 inlining a visible template (negative result)](explicit-spec-decl-ineffective.md) — c4 — cpp:template cpp:inline | topic:inline-vs-call — template<> declaration added, % unchanged, operator+ still inlined
- [Trivial COMDAT template method: target `call`s it, base inlines (vectora::size)](inline-vs-call-template-comdat.md) — c6 — cpp:template cpp:member cpp:inline | asm:call asm:idiv | topic:inline-vs-call — call vectora<T>::size vs inlined [+4]-[+0] cdq idiv, imul edx 0x38 operator[]
- [An /Od module unit can carry an OPTIMIZED LTCG COMDAT - correct source, unpairable bytes](optimized-comdat-in-od-unit.md) — c7 — cpp:template cpp:member | asm:mov asm:movss | topic:convention topic:scoring-artifact — no push ebp frame, this in eax, flat unrolled stores, xmm movq pairs, args in edi/esi
- [A whole glue TU was compiled OPTIMIZED in gold while our base builds /Od (wall)](optimized-glue-tu-vs-od-base.md) — c8 — cpp:inline cpp:member cpp:wrapper | asm:mov asm:ret asm:call | topic:inline-vs-call topic:wall topic:convention — addressed bridge helper "0 stmts/1 byte", getter mov eax,[eax+8] ret, base +1 QUANTITY stmt per gfx() call, scaleform flash_value/flash_movie glue
- [CRTP packet<T> writer/reader need `friend` access to base_packet's private buffer](crtp-packet-friend-access.md) — c6 — cpp:template cpp:member | topic:structure-shape — private m_buffer/m_buffer_size access errors from packet/packet_reader/tcp_packet
- [A header fix that "does not take" = a STALE OTHER-MODULE COMDAT winning the link](stale-comdat-other-module.md) — c4 — cpp:template cpp:operator | topic:tooling — rebuilt objs carry new bytes but linked base shows the old compare, anchor TU never recompiled
- [`boost::bind(&Derived::virtual_method, ...)` ICF-folds onto a SIBLING class's bind<> rep](boost-bind-icf-sibling-fold.md) — c8 — cpp:template cpp:virtual | asm:call | topic:fold-icf — bind<> call named with a DIFFERENT class than the bound method, vcall'{36}' thunk, assign_to<bind_t<...>>
- [boost::bind args: by-VALUE vs boost::cref (reference_wrapper) - read the bind's listN type](boost-bind-cref.md) — c9 — cpp:template | asm:call asm:sub-esp | topic:codegen-idiom — call boost::addressof per arg, reference_wrapper<float3 const> in assign_to/bind_t, value<float3> struct copies
- [By-value boost::function argument: target copy-ctor (1 call) vs base default+assign_to_own (2)](boost-function-byvalue-copy.md) — c6 — cpp:template | asm:call asm:sub-esp | topic:inline-vs-call topic:convention — call function<>::function<> then call assign_to_own, sub esp 20h vs 14h, extra [ebp-10h] slot
- [asio completion binds use boost::asio::placeholders::error/bytes_transferred, not _1/_2](boost-asio-placeholders.md) — c8 — cpp:template | asm:mov asm:push asm:movzx | topic:codegen-idiom — mov eax [?error@...placeholders@asio@boost@@] vs movzx ecx byte[_1], ~94% head divergence
- [Real call sites keep r<T>/append standalone (the actual lever for template bodies)](real-call-sites-for-templates.md) — c6 — cpp:template | topic:anchoring topic:inline-vs-call — address-anchored template scores poorly, append statement silently dropped at call site
- [Template carcasses need member-fn-ADDRESS anchors (a call site inlines them away)](template-address-anchors.md) — c6 — cpp:template | topic:anchoring — 0 symbols for anchored template members, body inlined into the anchor
- [ICF fold names on intrusive_ptr helpers are NOISE; real type evidence is elsewhere](icf-fold-names-noise.md) — c8 — cpp:template | topic:fold-icf — intrusive_ptr<random type> ctors/operators applied to the same member, contradictory template args
- [An API-exported `template class intrusive_ptr<...>` de-inlines c_ptr/operator=/refcount in the BASE only](dllexport-intrusive-ptr-instantiation-deinlines-base.md) — c8 — cpp:template cpp:member | asm:call asm:inc asm:dec | topic:inlining topic:residual — faithful body over a ptr member stalls 25-70%, base CALLs c_ptr/operator=/safe-bool that the target inlines (cmp+raw-read+inline refcount), structure-diff clean
- [report.json "None" can mean PAIRED AT 0.0% (protobuf default-value omission)](none-paired-at-zero.md) — c6 — cpp:template | topic:scoring-artifact — fuzzy None but structure-diff resolves both sides, match_percent 0.0 in live diff
- [Module-only ninja_build does NOT relink: the base index is stale for new symbols](module-build-stale-index.md) — c5 — cpp:template | topic:tooling — freshly-anchored symbols missing from binaries/rich/base/index.jsonl
- [Ternary precedence trap: `a * b * c ? x : y` makes the whole product the condition](ternary-parens-precedence.md) — c8 — cpp:ternary cpp:float | asm:test asm:movzx | topic:structure-shape — target tests the bool ALONE then multiplies, base tests the float product
- [Ternary returned via a NAMED result local: extra slot + movss/movss/fld epilogue](ternary-named-result-local.md) — c8 — cpp:ternary cpp:return cpp:local cpp:float | asm:movss asm:fld | topic:pdb-locals — movss xmm0 [result] movss [ebp-N] xmm0 fld [ebp-N] epilogue, extra stack slot
- [`val != u32(-1) ? &obj : NULL` = `sub r,-1; neg; sbb r,r; lea; and`](ternary-pointer-or-null.md) — c8 — cpp:ternary | asm:neg asm:sbb asm:and asm:lea | topic:codegen-idiom — sub edx 0FFFFFFFFh neg sbb edx edx lea and edx eax push
- [Qualified call to an EMPTY base virtual: target keeps the call, base inlines the {} away](outline-empty-virtual.md) — c4 — cpp:virtual cpp:inline | asm:call | topic:inline-vs-call — + call Base::execute only on target side, ~3 bytes, empty body defined inline in header
- [Virtual call on a member reference: `mov edx,[obj]; mov eax,[edx+slot]; call eax`](virtual-call-through-vtable.md) — c9 — cpp:virtual cpp:member | asm:call asm:mov | topic:codegen-idiom — vtable slot load [edx+8Ch] call eax, slot loaded twice under /Od
- [A missing virtual in the BASE class shifts EVERY later vtable-offset by 4](missing-base-virtual-shifts-vtable-offsets.md) — c9 — cpp:virtual cpp:class | asm:call asm:mov | topic:structure-shape topic:vtable — constant +4*n delta on [edx+0x24]->[edx+0x28] across many callers, fix = insert missing virtual in base header + add override to every subclass
- [Virtual member access codes after @@: U=public, M=protected, E=private virtual](mangled-access-virtual.md) — c9 — cpp:virtual cpp:member | topic:mangling — @@UAE, @@MAE, @@EAE, @@EBE, scores None for a virtual override
- [`&Class::virtual_member` emits a vtable thunk, NOT the body (anchoring trap)](addressof-virtual-anchoring-trap.md) — c8 — cpp:virtual | topic:anchoring — zero Class::member symbols in obj despite anchor, C4716, LNK1257 on instantiating
- [Anchoring an abstract state subclass: override EVERY still-pure virtual](pure-virtual-anchor-overrides.md) — c7 — cpp:virtual | topic:anchoring — C2259 cannot instantiate abstract class, fsm_state pures
- [Read the unit's OWN recovered symbol for access letters, not the ICF fold rep](unit-own-symbol-vs-fold-rep.md) — c6 — cpp:virtual | topic:fold-icf topic:mangling — rich index shows an unrelated fold rep mangling, report.json lists the unit's own symbol
- [Address-taken member-pointer anchor emits a header COMDAT in the RIGHT unit](address-taken-anchor-emits-header-comdat.md) — c8 — cpp:inline cpp:member cpp:template | asm:call asm:mov | topic:anchoring topic:inline-vs-call topic:fold-icf — non-virtual header member absent from base index, plain anchor call inlined by /GL; plain-shape target pairs ~100%, custom-conv target pairs at 0%; report.json hides 0% pairs (protobuf zero omission)
- [`set(0)` after the zero-store = `resource_ptr( NULL )`, not `resource_ptr( )`](intrusive-ptr-null-ctor-vs-default.md) — c8 — cpp:ctor cpp:template | asm:push asm:call asm:lea | topic:structure-shape — mov dword [slot] 0 then push 0 lea ecx call intrusive_ptr::set, base 10 bytes short per site
- [Pin reconstructed resources class_id_enum values from cook-registration ctors](class-id-enum-pinning.md) — c8 — cpp:member cpp:ctor | asm:mov asm:push | topic:structure-shape — push 12Ch where the enum names 103h, unmanaged_cook ctor immediate pins the block start
- [Calling a thin inline accessor vs hand-inlining its body changes operand scheduling](inline-helper-vs-expanded-expression-scheduling.md) — c7 — cpp:member cpp:float | asm:cvtsi2ss asm:divss asm:call | topic:codegen-idiom topic:inline-vs-call — randF() member call defers the cvtsi2ss/divss past a sibling call vs hand-inlined float(randI())/maxF() pinning it; match the imprecise 1.2539185f literal not true sqrt(pi/2)
- [A recovered inline call site can preserve a standalone COMDAT](never-called-comdat-stripped-add-inlining-call.md) — c7 — cpp:member cpp:inline | asm:call | topic:anchoring topic:inline-vs-call — reconstruct a target-evidenced helper call instead of an open-coded body; inlining can preserve caller bytes and retain the standalone helper
- [A `convert_to_binder_X` switch maps device bits to unified binder keycodes](receiver-bit-to-binder-keycode-switch.md) — c8 — cpp:enum cpp:switch cpp:member | asm:movzx asm:jmp asm:mov-imm | topic:structure-shape topic:enum-pinning — sparse `1 << i` cases map receiver bits to binder-code immediates
- [A header's FILE PATH (incl. sources/ subdir) decides the objdiff unit - a wrong path leaves it TARGET_ONLY](header-location-drives-objdiff-unit.md) — c9 — cpp:inline cpp:member | topic:anchoring topic:tooling topic:scoring-artifact — unpaired despite correct bytes, base emits COMDAT in a different objdiff unit, rich-index path differs base vs target (sources/ subdir), git mv the header + fix include/vcproj, or declare-only in split header & define in the using header
- [A `return *(T*)NULL;` "buildability return" accessor stub collapses every inlined caller chain](null-stub-accessor-collapses-inlined-chain.md) — c9 — cpp:inline cpp:accessor | asm:mov-absolute | topic:ltcg topic:stub topic:reachability — base does mov ecx,[10h] / skips an indirection while target chains [esi+0A8h][ecx+94h][edx+10h]; one unmatched getter stubbed `return *(T*)NULL` const-props the null down the chain; fix the stub to its real one-liner
- [Stale cross-unit PARK notes expire; a type-completing include can newly pair a whole forward-declared subsystem](stale-park-recheck-and-type-completing-include.md) — c8 — cpp:include cpp:member | topic:reachability topic:park topic:fold-icf — a PARKED fn's cross-unit block (missing game member/accessor/method) had since landed in the carcass header; re-checking unblocked it; calling a method on a forward-declared member type needs the type-completing #include, which newly paired ~993 lobby UI fns (regressed 100->0 = null/dtor/vcall fold-rep churn, not real)
- [Runtime-loaded global vs const-fold, and FP reassociation of `x/180*pi*factor`](global-fov-const-fold-and-fp-reassoc.md) — c9 — cpp:global cpp:float cpp:fold cpp:console-command | asm:movss asm:mulss asm:fld | topic:codegen-idiom topic:fold-icf — base folds a const expr to one `__real@` slot where target loads `[?global@@3MA]`+separate `[pi]`/`[1/180]`; a cc_float binding `&global` defeats the fold (address escapes); group `((v/180)*pi)*factor` so MSVC keeps 1/180 and pi as separate mulss
- [Out-of-line method's standalone symbol is an ICF fold to a trivial body; the REAL body shows only inlined](outline-symbol-icf-folded-to-trivial-body.md) — c7 — cpp:member cpp:inline | asm:call asm:mov | topic:fold-icf topic:inline-vs-call — standalone Class::method is a 5-byte u8-setter (fold artifact) yet a cross-TU caller INLINES a much richer body (set/remove callback + boost::function assign); reconstruct from the inlined expansion, not the trivial standalone
- [Inlined matrix re-orthogonalisation = set up-row world-up + i = cross(j, k)](inlined-matrix-reorthogonalise-cross.md) — c8 — cpp:member cpp:inline cpp:assignment | asm:movss asm:mulss asm:subss asm:rep-movsd | topic:codegen-idiom topic:math-inline — double rep movsd 0x10 copying a float4x4 member, then 3 lone movss writing (0,0,1.0) in x,z,y order (j row = world-up), then a 6-mul/3-sub cross schedule reading k.y/k.z writing i.xyz only = `m.j.xyz()=float3(0,1,0); m.i.xyz()=cross_product(m.j.xyz(),m.k.xyz())`
- [A commented-out body block is live in the target; reconstruct it from a vtable-index chain + CRT `_*_l` locale macros](disabled-block-reconstruct-vtable-chain-crt-locale.md) — c10 — cpp:member cpp:virtual cpp:disabled-code | asm:call-vtable asm:and-103h | topic:reconstruction topic:layout-fix topic:crt-locale — target keeps a `//`-commented block LIVE (uses a member a layout fix just recovered); decode the virtual by vtable offset/4 against interface decl order (`[edx+0x24]`=9th virtual=get_keyboard, `[edx+4]`=1st=get_dik_name); `_isalpha_l` is a `<ctype.h>` MACRO (and eax,103h = _ALPHA), `_strlwr_s_l(buf,size,loc)` is the secure variant; 24.6%->100%

- [PDB enum drift shifts immediates and can alias generated switch cases](pdb-enum-drift-shifts-immediates-and-aliases.md) — c9 — cpp:enum cpp:switch cpp:template | asm:and asm:cmp asm:test | topic:layout-fix topic:generated-code — repeated power-of-two immediate shifts across otherwise matching callers point to an imported SDK enum version; restore the PDB enum once, then remove only later generated case arms made duplicate by target aliases

## Manual patterns (`manual/`)

`construct/idiom -> asm` mappings gathered by compiling minimal probes and by mining real
functions' shipped asm (`pdb_fetch`). Full index + reproduction recipe in
[`manual/INDEX.md`](manual/INDEX.md); opt level is per-TU (see
`manual/tu-optimization-level-od-vs-ox.md`). Greppable by tag like any line here.

- [if/else under /Od: the condition INVERTS, the then-block ends in a jmp over the else](manual/od-ifelse-inversion.md) — c9 — cpp:if cpp:return | asm:cmp asm:jcc asm:jmp | topic:codegen-idiom — cmp/jge skipping then, jmp over else, else-if = chained cmp/jne .next + jmp .end
- [Early-return guard `if(!c) return;` = inverted test jne .body + a bare jmp epilogue](manual/od-early-return-guard.md) — c9 — cpp:if cpp:return | asm:cmp asm:jcc asm:jmp | topic:structure-shape — cmp 0/jne .body/jmp .end pair, two branches where a wrapping-if has one je
- [The three /Od loop skeletons: for vs while vs do-while are distinguishable](manual/od-loop-skeletons.md) — c9 — cpp:for cpp:while cpp:do-while cpp:loop | asm:jmp asm:cmp asm:jcc | topic:structure-shape — jmp short .test entry, increment block above the test, test-at-top no entry jmp, body-first bottom-test non-inverted back-edge
- [`break`/`continue` = an inverted guard whose taken branch jmps to the loop exit / increment](manual/od-break-continue.md) — c8 — cpp:for cpp:while | asm:jmp asm:cmp asm:jcc | topic:structure-shape — cmp/jne .skip then jmp .end (break) or jmp .incr (continue)
- [`&&` and `||` short-circuit: && is all je .end; || flips the first to jne .body](manual/short-circuit-and-or.md) — c9 — cpp:if cpp:bool | asm:test asm:jcc | topic:codegen-idiom — two sequential test/je for &&, first operand test/jne to body for ||, second operand only evaluated when needed
- [Braces around a SINGLE statement are invisible — `if(c) s;` == `if(c){ s; }` byte-for-byte](manual/braces-single-statement-invisible.md) — c10 — cpp:if cpp:for cpp:while | asm:cmp asm:jmp | topic:wall topic:convention — identical bytes with and without braces, brace presence unrecoverable
- [A `{ }` block around a non-trivial-dtor local moves its call ~T to the block's }](manual/lexical-block-relocates-dtor.md) — c9 — cpp:dtor cpp:local | asm:call asm:lea | topic:structure-shape — call ~T emitted before a following statement, dtor-call position shifts with the brace
- [An expression-statement comma is invisible: `(e1, e2);` == `e1; e2;` byte-for-byte (a for-increment comma is NOT)](manual/comma-operator-invisible.md) — c10 — cpp:comma | asm:call asm:mov | topic:wall topic:convention — no distinguishing bytes between a discarded-value comma expression and the equivalent statement sequence; for-increment ops live in the increment block
- [cdecl vs thiscall at the call site: caller add esp,N + YA/SA vs mov ecx,this + no cleanup](manual/call-convention-cdecl-vs-thiscall.md) — c9 — cpp:call cpp:member cpp:static | asm:push asm:call asm:mov | topic:convention topic:mangling — add esp,N after the call, mov ecx,this before a member call, YA/SA vs QAE mangling
- [A qualified `obj->Base::f()` devirtualizes to a direct UAE thiscall (vs vtable dispatch)](manual/virtual-call-vtable-dispatch.md) — c8 — cpp:virtual cpp:call | asm:mov asm:call | topic:codegen-idiom — direct call ?f@..@UAE.. when qualified vs call reg after a double-deref of this, displacement = slot*4
- [A reference parameter and a pointer parameter generate IDENTICAL bodies — only the mangle differs](manual/reference-vs-pointer-param-mangling-only.md) — c9 — cpp:member cpp:const | asm:mov | topic:mangling topic:wall — T& and T* produce the same load-slot-then-deref body, AAH vs PAH is the sole difference
- [`_alloca`/`ALLOCA` = mov eax,size; call __alloca_probe_16; mov slot,esp — no matching free](manual/alloca-stack-probe.md) — c9 — cpp:local cpp:macro | asm:call asm:sub-esp asm:mov | topic:codegen-idiom topic:convention — call __alloca_probe_16, result pointer IS esp, lea esp,[ebp-N] out, no free call
- [`VOSTOK_NEW`/`VOSTOK_DELETE` family: strip_pointer feeding new_helper<T>::call / delete_helper<T>](manual/vostok-memory-macros.md) — c8 — cpp:new-delete cpp:template cpp:macro | asm:call asm:push | topic:codegen-idiom topic:convention — call strip_pointer then new_helper<T>::call then a null-guarded ctor; allocator pushed as OFFSET g_*_allocator
- [Placement `new (p) T` = a null-guarded ctor call, NO allocation; the placement operator is codeless](manual/placement-new-null-guarded-ctor.md) — c9 — cpp:new-delete cpp:ctor | asm:cmp asm:jcc asm:call | topic:codegen-idiom — cmp slot,0 / je over the ctor, mov ecx,slot / call ??0T, no operator new
- [Explicit `p->~T()` vs `delete p` (non-virtual): same skeleton, the flag immediate (0 vs 1) decides the free](manual/explicit-dtor-vs-delete-flag.md) — c8 — cpp:dtor cpp:new-delete | asm:call asm:and asm:jcc | topic:codegen-idiom — call ??1T then flag&1/je over operator delete; xor eax,eax (0) vs mov reg,1 (1)
- [`delete p` on a polymorphic type = push 1 + a virtual call to the ??_G scalar deleting destructor](manual/delete-polymorphic-scalar-deleting-dtor.md) — c8 — cpp:dtor cpp:virtual cpp:new-delete | asm:call asm:mov asm:push | topic:codegen-idiom topic:fold-icf — push 1 then vtable-dispatched call, callee ??_G/??_E, no site-level ??1/operator delete
- [`dst = c ? a : b` materializes a tv temp + a final copy (bigger frame); if/else stores direct](manual/ternary-temp-slot.md) — c9 — cpp:ternary cpp:local | asm:cmp asm:jcc asm:jmp asm:mov | topic:structure-shape topic:pdb-locals — tv<N> temp written in both arms then copied to dst, extra push ecx/sub esp, vs if/else direct stores
- [3+ operand `&&`/`||` chains and mixed precedence: the jump TARGETS encode the grouping](manual/short-circuit-chains-precedence.md) — c9 — cpp:if cpp:bool | asm:test asm:jcc | topic:structure-shape — N-1 jne .body + 1 je .end for ||, N je .end for &&, && group's failure routes to the next || operand
- [`inline` free fns and class-body members DO inline under /Od /Ob2 — only out-of-line defs emit a call](manual/inline-under-od-ob2.md) — c9 — cpp:inline cpp:member | asm:call asm:imul asm:mov | topic:inline-vs-call topic:codegen-idiom — small accessor/helper has NO call site, body spliced in; /Ob2 overrides /Od; LTCG inlines even more
- [Scalar float math = movss/addss/mulss on xmm, ONE component at a time, base ptr reloaded per component (/Od)](manual/scalar-float-sse2-ops.md) — c9 — cpp:float | asm:movss asm:addss asm:mulss | topic:codegen-idiom — f3 0f 10/58/59, base pointer reloaded per component, __fltused, __real@<hex>, float global @@3MA
- [Returning a struct by value: >8 bytes = hidden return-pointer (sret); <=8-byte POD = edx:eax](manual/struct-by-value-return-sret.md) — c8 — cpp:return cpp:member | asm:lea asm:push asm:call asm:mov | topic:convention — lea slot/push as extra arg + result via eax (sret), or mov eax/mov edx for an 8-byte POD

- [Optimization level is PER-TU (.vcproj Optimization 0 vs 3) — read the asm to know which world you're in](manual/tu-optimization-level-od-vs-ox.md) — c9 — cpp:for cpp:operator | asm:idiv asm:mul asm:add | topic:convention topic:ltcg — idiv vs magic-multiply, verbose iterator object vs register pointer, same source two codegens
- [`strings::equal(a,b)` is a register-args helper call (NO pushes); 1st arg->EAX, 2nd->ECX](manual/strings-equal-register-args.md) — c9 — cpp:string cpp:call | asm:call asm:mov asm:movzx asm:test | topic:codegen-idiom — mov eax literal/mov ecx other/call strings::equal/movzx/test/jcc, no push
- [`str.assignf("fmt", ...)` = cdecl varargs: push args R-to-L, push &fmt, push this(lea), call, add esp,N](manual/buffer-string-assignf-varargs.md) — c9 — cpp:string cpp:varargs | asm:push asm:call asm:lea asm:add | topic:codegen-idiom — push vararg/push format literal/lea this/call assignf/add esp,0Ch
- [`buffer_string` accessors inline: c_str()=mov eax,[eax], length()=m_end-m_begin](manual/buffer-string-accessors-inline.md) — c8 — cpp:string cpp:inline | asm:mov asm:sub asm:ret | topic:codegen-idiom topic:fold-icf — c_str this in EAX, length mov eax,[ecx+4];sub eax,[ecx], ICF-folded out-of-line copy
- [`if ( ptr )` = mov eax,[eax]; neg; sbb eax,eax; and eax,&c_ptr (safe-bool mask)](manual/intrusive-ptr-safe-bool-mask.md) — c8 — cpp:bool cpp:operator cpp:template | asm:neg asm:sbb asm:and | topic:codegen-idiom topic:fold-icf — neg/sbb/and against an ICF-folded c_ptr address
- [`ptr = other;` (operator=) = copy-and-swap: two call ::dec + one interlocked_increment + slot swap, ret 4](manual/intrusive-ptr-assign-copy-swap.md) — c8 — cpp:operator cpp:template | asm:call asm:mov | topic:codegen-idiom topic:inline-vs-call — mov [tmp],0/cmp/two dec calls/interlocked_increment/swap/ret 4
- [intrusive_ptr `a == b` / `a == raw` = mov [this]; xor eax,eax; cmp; sete al leaf](manual/intrusive-ptr-compare-sete-leaf.md) — c9 — cpp:operator cpp:bool cpp:template | asm:cmp asm:sete asm:xor | topic:codegen-idiom — xor eax/cmp/sete al, ret (self) or ret 4 (raw rhs)
- [intrusive_ptr operator->/operator* are a bare mov eax,[eax]; ret (ASSERT compiled out, ICF-folded)](manual/intrusive-ptr-deref-bare-load.md) — c9 — cpp:operator cpp:template | asm:mov | topic:codegen-idiom topic:fold-icf — 3-byte leaf, no ASSERT, folded across instantiations
- [float4x4::transform_position/direction = 3 scalar dot-products; the 1.f*e30 translation folds to a bare addss](manual/matrix-transform-per-component.md) — c9 — cpp:float cpp:operator | asm:movss asm:mulss asm:addss | topic:codegen-idiom — result.x = v.x*e00+v.y*e10+v.z*e20+1.f*e30, +e30 is a bare addss not mulss-by-1
- [cross_product/mul4x3 stage operands with movaps xmm,xmm (not base reloads) because source caches self=*this](manual/vector-op-movaps-staging.md) — c9 — cpp:float cpp:operator | asm:movaps asm:mulss asm:subss | topic:codegen-idiom — float3_pod self=*this; movaps register copies before each mulss/subss
- [length()/normalize() mix SSE + x87: SSE self-square sum, then call sqrtf + fld1;fdivrp reciprocal](manual/vector-length-normalize-x87.md) — c9 — cpp:float cpp:vector | asm:call asm:fld1 asm:fdivrp asm:mulss | topic:codegen-idiom — call sqrtf (not sqrtss), fld1/fdivrp reciprocal, mixed fmul/mulss
- [STLport `++it`: map/set = call _Rb_global::_M_increment (tree walk); list = mov reg,[node]](manual/stlport-tree-list-iterator-advance.md) — c9 — cpp:for cpp:iterator | asm:call asm:mov asm:push | topic:codegen-idiom topic:stlport — _M_increment call for tree, node->next load for list
- [STLport begin()/end() = an ICF-folded iterator copy-ctor CALL into a slot; the name is noise](manual/stlport-begin-end-iterator-ctor-fold.md) — c8 — cpp:iterator cpp:for | asm:call asm:cmp | topic:fold-icf topic:stlport — lea slot/call folded list1<...> ctor, then cmp/setne
- [Contiguous-iterator loop under /Ox: ++it = add reg,sizeof(T); end()-begin() count = reciprocal imul](manual/contiguous-iterator-add-sizeof.md) — c9 — cpp:iterator cpp:for | asm:add asm:imul | topic:codegen-idiom topic:ltcg — iterator is a register raw ptr, add sizeof stride, count via magic-div
- [/Od container loop re-derives end() EVERY turn and boolizes it != end (xor/cmp/setne/movzx/test/je)](manual/od-container-loop-boolized-compare.md) — c8 — cpp:for cpp:iterator | asm:setne asm:movzx asm:cmp | topic:codegen-idiom topic:stlport — ~7-row per-turn preamble re-taking end() + boolized compare
- [`(T)config["key"]` = call binary_config_value::operator[] then call operator T](manual/config-index-then-cast.md) — c9 — cpp:operator cpp:cast cpp:member | asm:call asm:movss asm:push | topic:codegen-idiom — push key/call operator[]/call operator float/store
- [A float3/by-ref config cast returns a POINTER + a member-wise 3-dword copy](manual/config-vector-cast-pointer-copy.md) — c8 — cpp:operator cpp:cast cpp:float | asm:call asm:mov | topic:codegen-idiom — operator[] then ICF-folded float3 cast, mov [dst]/[dst+4]/[dst+8]
- [`if ( cfg.value_exists("k") ) m = (T)cfg["k"]` looks the SAME key up TWICE](manual/config-value-exists-double-lookup.md) — c9 — cpp:operator cpp:if cpp:member | asm:call asm:test asm:jcc asm:push | topic:codegen-idiom — value_exists call/movzx/test/je then the same key pushed again for operator[]
- [Nested config["a"]["b"] = TWO chained out-of-line operator[] calls with mov ecx,eax between](manual/config-nested-index-chain.md) — c9 — cpp:operator cpp:cast cpp:member | asm:call asm:mov | topic:codegen-idiom — outer operator[] -> eax -> mov ecx,eax -> inner operator[] -> operator T
- [Integer / and % in a /Od TU = one cdq;idiv (or xor edx;div); quotient EAX, remainder EDX](manual/integer-div-mod-idiv-od.md) — c9 — cpp:operator | asm:idiv asm:div | topic:codegen-idiom — cdq;idiv signed / xor edx;div unsigned, even by a constant; paired /,% fold to one divide
- [Integer / or % by a CONSTANT in a /Ox TU = magic-number multiply (mov eax,<magic>; mul; shr), NO idiv](manual/integer-div-mod-magic-ox.md) — c9 — cpp:operator | asm:mul asm:shr asm:imul | topic:codegen-idiom — reciprocal magic, mul, shr edx, imul divisor back, sub for remainder
- [Signed vs unsigned comparison: the branch mnemonic IS the operand signedness (jl/jge vs jb/jae)](manual/signed-unsigned-compare-mnemonic.md) — c9 — cpp:if cpp:for | asm:cmp asm:jcc | topic:codegen-idiom topic:mangling — jl/jge/jg/jle signed vs jb/jae/ja/jbe unsigned, same source <
- [`static T x = f();` (function-local) = a first-call guard bit in the TU's $S flags word](manual/function-static-runtime-init-guard.md) — c9 — cpp:static cpp:local | asm:test asm:or asm:jcc | topic:codegen-idiom — test [$S],bit/jne/or [$S],bit then init once, value in the named static
- [`(this->*pmf)(args)` (single inheritance) = load pmf from slot, mov ecx,this, call eax](manual/pointer-to-member-fn-call.md) — c9 — cpp:member cpp:call | asm:call asm:mov | topic:codegen-idiom — mov edx,[obj+N]/mov eax,[edx+slot]/mov ecx,this/call eax, args pushed first
- [`class X : public A, public B` ctor = add ecx,sizeof(A) before B::B + one vftable store per base](manual/multiple-inheritance-this-adjust.md) — c8 — cpp:ctor cpp:inherit cpp:virtual | asm:add asm:call asm:mov | topic:codegen-idiom topic:structure-shape — call A::A/add ecx,sizeof(A)/call B::B/dual vftable stores; no thunks in this engine

### FSM / callbacks / wire-io / dispatch / threading (real-function mined)

- [`fsm::tick()` = guard, walk the transition list, dispatch finalize/initialize/execute through 4 vtable slots](manual/fsm-tick-transition-loop.md) — c9 — cpp:virtual cpp:while | asm:call asm:mov | topic:codegen-idiom topic:structure-shape — [edx+24h] next walk, boost::function0<bool> predicate, call [edx+0Ch]/[edx+4]/[edx+8]
- [`add_transition(from,to,boost::bind(...))` = bind temp -> function assign_to -> add_transition -> clear](manual/fsm-add-transition-bind-wiring.md) — c8 — cpp:template cpp:member | asm:call asm:push asm:lea | topic:codegen-idiom topic:structure-shape — call boost::bind/assign_to/push to,from/call add_transition/call clear
- [FSM consume: fsm.tick(); static_cast<Derived*>(current_state()); derived->virtual(dt)](manual/fsm-run-static-cast-dispatch.md) — c8 — cpp:virtual cpp:cast | asm:mov asm:call | topic:codegen-idiom — current_state inlined mov [fsm+10h], static_cast no-op temp chain, slot past the 4 pures
- [Empty-bodied derived state ctor: the vtable store is sunk into the new-expression, no standalone ctor symbol](manual/derived-state-vtable-store-at-new-site.md) — c9 — cpp:ctor cpp:virtual cpp:new-delete | asm:call asm:mov | topic:codegen-idiom topic:inline-vs-call — call Base::Base then mov [obj],??_7Derived@@6B@ at the new-site
- [Invoking a boost::function MEMBER = mov ecx,this; add ecx,<member_off>; call functionN::operator()](manual/boost-function-member-invoke.md) — c9 — cpp:call cpp:template | asm:add asm:mov asm:call | topic:codegen-idiom — add ecx,N member offset then call operator(), siblings differ only by N
- [`boost::functionN::operator()` body = empty-guard + bad_function_call throw + manager indirection (and eax,~1; call [eax+4])](manual/boost-function-operator-call-body.md) — c9 — cpp:template cpp:operator | asm:cmp asm:and asm:call | topic:codegen-idiom — cmp [esi],0/jne, bad_function_call thrown even w/ EH off, and 0FFFFFFFEh, call [eax+4], ret 4*N
- [`void_function_obj_invoker0<bind_t<...>>::invoke` = a frameless 3-insn tail thunk mov eax,[esp+4]; mov ecx,[eax]; jmp ecx](manual/boost-function-invoker-tail-thunk.md) — c9 — cpp:template | asm:mov asm:jmp | topic:fold-icf — read function_buffer&, load functor field 0, jmp; ICF-folded across instantiations
- [`boost::bind(&C::m, this)` functor build (sret) = pack mf code-ptr at +0 and bound this at +4](manual/boost-bind-functor-field-store.md) — c8 — cpp:template | asm:mov asm:lea | topic:codegen-idiom — sret bind_t in [ebp+8], mov [ecx],edx mf ptr then mov [edx+4],ecx bound this
- [`reader.r<T>()` inlined = load m_pointer(+4), deref, advance by sizeof(T), store back](manual/packet-reader-r-template.md) — c9 — cpp:template cpp:member | asm:mov asm:add asm:inc | topic:codegen-idiom topic:wire-io — mov [this+4]/deref/add 4 (or inc)/store back, then store to a target member
- [`reader.r<float>()` = integer load to a stack slot, then movss reload (GPR->XMM bounce)](manual/packet-reader-r-float-bounce.md) — c8 — cpp:template cpp:member cpp:float | asm:mov asm:movss | topic:codegen-idiom topic:wire-io — mov [esp],edx then movss xmm0,[esp], this in ECX
- [`reader.r_string<N>(buf)` = inlined u8 length-prefix read, memcpy, then NUL-terminate at buf[len]](manual/packet-reader-r-string.md) — c8 — cpp:template cpp:member cpp:string | asm:movzx asm:call asm:mov | topic:wire-io topic:codegen-idiom — length byte/memcpy/add m_pointer,len/byte ptr [len+dst],0
- [Event dispatch = build a stack functor holding the payload, then m_subscribers.for_each(functor) — never a raw loop](manual/listener-for-each-dispatch.md) — c9 — cpp:template cpp:member | asm:lea asm:call asm:push | topic:codegen-idiom topic:stlport — lea slot/push/call intrusive_list::for_each<void_predicate_ref<...callback_predicate>>
- [`intrusive_list::for_each` body = empty-guard, mutex_raii, then a save-next-BEFORE-pred-call loop](manual/intrusive-list-for-each-body.md) — c9 — cpp:for cpp:template cpp:iterator | asm:cmp asm:je asm:call | topic:codegen-idiom topic:stlport — cmp [list+24h],0 empty, mutex_raii, next=current->m_next before call, locals raii/current/next
- [A scoped-lock `mutex_raii raii(lock);` = ctor (acquire) at entry + dtor (release) at EVERY exit](manual/scoped-lock-raii.md) — c9 — cpp:dtor cpp:local cpp:ctor | asm:call asm:lea | topic:structure-shape topic:codegen-idiom — call mutex_raii/::lock at entry, call ::clear/::unlock at each return, one acquire / N releases
- [`threading::mutex` lock/unlock = Enter/LeaveCriticalSection; tiny unlock inlines as __imp__LeaveCriticalSection@4](manual/mutex-critical-section-lowering.md) — c9 — cpp:member | asm:call asm:push | topic:codegen-idiom topic:convention — call mutex::lock spins on TryEnterCriticalSection, LeaveCriticalSection with no matching Enter = inlined unlock
- [`threading::interlocked_*` = a lock-prefixed RMW: increment=lock xadd;inc, CAS=lock cmpxchg, exchange=bare xchg](manual/interlocked-lock-prefixed-rmw.md) — c9 — cpp:call | asm:lock asm:xadd asm:cmpxchg asm:xchg | topic:codegen-idiom — mov eax,1;lock xadd;inc eax (post-value), lock cmpxchg, xchg; call site keeps a call under /Od
- [Statement-LINE gaps recover byte-invisible source lines (R_ASSERT in an optimized TU)](statement-line-gap-recovers-zero-byte-statements.md) — c8 — cpp:assert cpp:structure | asm:none | topic:structure-shape topic:optimized-target topic:pdb-lines — MASTER_GOLD R_ASSERT folds to zero bytes AND zero line entries; the `{`-line to body-line gap still counts it (1=one-liner, 2=one-liner+assert, 11/13=expanded bind w/ 3/4 args); also sizes unwritten bodies and flags /OPT:REF-stripped sibling blocks (a 137-line jump)
- [`movss xmm,[const-pool]` where base emits `xorps` = the float literal is NOT 0.f](movss-const-pool-vs-xorps-nonzero-literal.md) — c9 — cpp:float cpp:ternary cpp:literal | asm:movss asm:xorps | topic:codegen-idiom topic:constant-recovery — statement 5 bytes short, base xorps xmm0,xmm0 vs target movss from a pool slot the delinker misnames (clear_value); cross-check the slot's other users to read the value (light-cook influence factors default 1.f not 0.f)
- [A module anchor cannot self-dispatch: an unreferenced .obj inside a .lib is never extracted](static-lib-anchor-needs-external-dispatch.md) — c9 — cpp:static-init cpp:anchor | topic:anchoring topic:reachability topic:link-model — anchor .obj built but grep -c anchor_<module> base index = 0 and nothing pairs; file-scope dynamic initializer never links from a static lib, wire it into game_core anchor.h/anchor.cpp IncludeAll; prefer address-take over call for already-bodied fns (pin forces stock __thiscall, caps 79-95%)
- [A truncated string literal is fully recoverable - the mangled name carries its length and a CRC](truncated-string-literal-crc-recovery.md) — c9 — cpp:string-literal cpp:assert | asm:push-immediate | topic:data-recovery topic:mangled-names topic:strings — `??_C@_0<len>@<hash>@<32-char prefix>@`: len = bytes INCLUDING nul in MSVC number encoding ('0'-'9' = N-1, else hex 'A'-'P'), hash = `~zlib.crc32(bytes+nul)` as 8x ('A'+nibble) MSB-first; verifies a guessed completion with certainty (recovered `"data->new_resource->shader_source == 0"` from `_0CH@BANAFJDE@`), meet-in-the-middle for a ~5-byte tail
