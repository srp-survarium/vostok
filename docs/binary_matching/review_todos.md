# Review TODOs (`sushi@TODO:`)

Open matching questions sushi raised in PR review, recorded in-source as `sushi@TODO:`
markers and indexed here so a later agent can sweep them in one pass.

- **Live set** (source of truth): `grep -rn "sushi@TODO:" sources/` — the markers travel with
  the code, so this is always current even if the table below drifts.
- **This table** is the curated index: one row per marker, with the function and the PR it came
  from. Append a row whenever you add a `sushi@TODO:` to the source; tick `Done` (and drop the
  marker) when the matching question is resolved.

Append-only ledger (union-merged across branches, like `PROGRESS.md` / `unanswered_questions.md`).

| Status | Function | File:line | PR | TODO |
|--------|----------|-----------|----|------|
| open | `fingers_to_weapon_corrector::set_first_person_view` | `game/sources/fingers_to_weapon_corrector.h` | #571 | Pure store and initialize_locators caller seam supported by retail +0x814 write; verify original named boundary. Constructor false initialization is independently proven. See game/weapon_inline_recovery.md. |
| open | `weapon::set_fire_pfx` | `game/sources/weapon.h` | #571 | Dedicated-result/preallocated-array copy model only. Recover original caller, result offset/count, allocation and failure contract; do not pass the existing cook's mixed query batch to the index-zero model. |
| open | `weapon::set_shells_pfx` | `game/sources/weapon.h` | #571 | Corresponding separate shells model; original query/list/count/ownership contract remains open. Preserve current cook allocation/free ownership and do not infer prefix from scope installed only later. |
| open | `weapon::is_player_current` | `game/sources/weapon.h` | #571 | Restore scene/game/network identity comparison against hit-initiator ID and sound-callback consumer. Retail fresh weapon/scene loads support private helper/friend seam, not original named boundary or friendship spelling. |
| open | `weapon_sound_effect::on_sound_event` | `game/sources/weapon_sound_effect.cpp` | #571 | Restore private predicate, two null-instance early returns and single-element erase seams. Verify remaining PDB statement/local projection and original overload spelling later; no build in this reconstruction pass. |
| open | `profile_character::profile_character` | `game/sources/profile_character.h` | #571 | Identity-matrix/false-visibility constructor model preserves borrowed references and generated member construction. Recover original transform, construction consumer and animation-query timing; no bone sentinel or artificial query call. See game/profile_character_inline_recovery.md. |
| open | `profile_character::~profile_character` | `game/sources/profile_character.h` | #571 | Raw PDB proves explicit nonvirtual destructor. Member cleanup does not establish scene detachment or an explicit clear_resources call; recover original destruction consumer/body and async lifetime. |
| open | `profile_character::clear_resources` | `game/sources/profile_character.h` | #571 | Callback-derived detach/reset/release model. Verify animation-player reset(true), release ordering, original caller and pending-query lifetime; not a cancellation implementation. |
| open | `profile_character::profile_changed` | `game/sources/profile_character.h` | #571 | Forward pointer to private query helper using retained sibling as prior. Original named consumer, null/eager-clear policy and animation-query scheduling remain open; producer still unfinished. |
| open | `profile_character::query_profile_contents` | `game/sources/profile_character.h` | #571 | Recover character/skin query plus two fixed model/animation/static-addon triplets, paths, optional-slot handling, user data and profile lifetime. Retained callbacks constrain positions but do not supply producer source. |
| open | `profile_character::query_character_animations` | `game/sources/profile_character.h` | #571 | Recover managed clip paths/configuration and original scheduling consumer. Callback consumes two positions after logging failures; neither names nor constructor timing follow from that alone. |
| open | `profile_character::update` | `game/sources/lobby_menu_scene.cpp` | #571 | Existing COMDAT-owner expression model does not establish original guards, animation tick, bone/transform publication or weapon visibility. Recover complete parent behavior; retained template helpers do not verify it. |
| open | `service_prices::serialize` | `game/sources/service_prices.h` | #571 | Three-u32 inverse-wire model from the retained price reader; recover original writer, guards and append granularity. No artificial sender or message prefix added. See game/lobby_record_inline_recovery.md. |
| open | `service_prices::deserialize` | `game/sources/service_prices.h` | #571 | Restore retail's sequential u32 operations and call helper in read_service_prices; original named inline boundary and deferred line projection remain open. Frozen caller has two extra assignment statements. |
| open | `stats::set_active_scene` | `game/sources/stats.h` | #571 | Restore older string64/active-scene formatter as a legacy model. Recover retail consumer, prefix and buffer contract; searched retail ASCII has no matching label. See game/stats_chat_inline_recovery.md. |
| open | `stats::set_player_stamina` | `game/sources/stats.h` | #571 | Formatted stamina-display hypothesis; recover units, precision, prefix and caller. Allocated widget and sibling speed setters do not establish this output contract. |
| open | `stats::set_can_use_value` | `game/sources/stats.h` | #571 | Boolean-status display hypothesis; recover true/false text versus visibility policy and original caller. Empty setter is not a verified retail no-op. |
| open | `stats::set_dispersion_components` | `game/sources/stats.h` | #571 | Direct-text forwarding model into actual widget; verify formatting, visibility and original caller. Preserve retained draw's separate hide operation. |
| open | `stats::set_player_logic_state` | `game/sources/stats.h` | #571 | Direct-text forwarding model using resources-text sibling; recover original prefix/format and caller rather than inventing a label. |
| open | `stats::set_weapon_logic_state` | `game/sources/stats.h` | #571 | Separate weapon-widget forwarding model; recover original prefix/format and caller. Do not conflate with player state. |
| open | `damage_model_stats::draw` | `game/sources/damage_model_stats.h` | #571 | Sibling root-window draw model; recover panel's original consumer and any guard. Do not add a game-frame call merely to emit it. |
| open | `damage_model_stats::set_stats` | `game/sources/damage_model_stats.h` | #571 | NPC-style clear/null/collect/caption-content model; preserve by-value const owner and supplied time. Original caller/reset/null/layout/format remain open, and the modeled player fill_stats producer is not independently verified by this newly created caller. |
| open | `damage_model_stats::create_new_group` | `game/sources/damage_model_stats.h` | #571 | Adapt retained NPC first-column text/adoption/placement model; verify u32-as-font-color interpretation, original width/wrapping/column policy and consumer. Existing panel constants alone do not establish that layout. |
| open | `chat_handler::in_match` | `game/sources/chat_handler.h` | #571 | Seven owner reads of private mode byte +0x16 now use declared const getter; retail operations are verified, original named boundaries remain inferred. Keep active/focused flags and set_mode writer distinct. |
| open | `main_menu_external_handler::main_menu_external_handler` | `game/sources/main_menu_external_handler.h` | #571 | Game-reference/base-construction model from raw layout and both live sibling handlers. Recover this class's original construction/registration consumer; do not add one. See game/menu_inline_recovery.md. |
| open | `main_menu_external_handler::callback` | `game/sources/main_menu_external_handler.h` | #571 | Recover actual UI method names, argument schema and guards. Main resource setup has no observed movie/handler registration; sibling login/lobby dispatch does not establish this body or editor/server-only ownership. |
| open | `lobby_menu::can_player_carry` | `game/sources/lobby_menu.h` | #571 | Capacity/current-weight producers are verified, but three u32 argument roles, slot/count/replacement delta, float meaning and limit equality remain unknown. Retained relocation checks category/compatibility, not an observed weight guard. False remains unverified. |
| open | `lobby_menu_external_handler::lobby_client` | `game/sources/lobby_menu_external_handler.h` | #571 | Restore game/network/virtual-client path and eight callback getter seams; retail operations are verified, original named inline boundary remains inferred. |
| open | `lobby_menu_external_handler::login_client` | `game/sources/lobby_menu_external_handler.h` | #571 | Corresponding forwarding model is supported by sibling lobby-menu getter/consumer; recover an invocation selecting this handler's getter. Do not conflate different owners or invent a call site. |
| open | `lobby_menu_external_handler::messaging_client` | `game/sources/lobby_menu_external_handler.h` | #571 | Restore game/network/virtual-client path and five callback getter seams; retail operations are verified, original named inline boundary remains inferred. |
| open | `network_client::on_connected_to_lobby` | `game/sources/network_client_lobby.cpp` | #571 | Restore public lobby-method forwarding instead of private duplicated implementation/local and friendship. Retail retains exact callee operations but zero caller line records; original boundary/deferred structure comparison remain open, no forced inlining. |
| open | `network_client::on_disconnected_from_lobby` | `game/sources/network_client_lobby.cpp` | #571 | Restore public lobby-method forwarding; retail's single statement covers lookup and full guarded expansion. Verify original named boundary and deferred structure, not the old invented cached-reference/local statement shape. |
| open | `game_options::get_options_item` | `game/sources/game_options.h` | #571 | Replace null with the two-stage lookup observed in retail refill_item_data (video tab 2, resolution item 1). General-index guard policy and original named inline boundary remain unverified. See game/options_inline_recovery.md. |
| open | `game_options::is_waiting_for_bind_key` | `game/sources/game_options.h` | #571 | Constructor/finish_binding store kLASTACTION (0x40); keyboard, mouse-key and mouse-motion consumers test it. Restore and propagate private predicate; original named boundary remains inferred. Keep mouse-key's separately attributed finish sequence. |
| open | `options_monitor_index_selector::~options_monitor_index_selector` | `game/sources/options_monitor_index_selector.h` | #571 | Raw PDB proves explicit nonvirtual destructor. Guarded owned-pointer-array deletion is a construction/sibling-based model; recover typed destruction consumer and original guard/body. Retail tab cleanup uses nonvirtual base destruction and does not call this destructor. |
| open | `options_resolution_selector::~options_resolution_selector` | `game/sources/options_resolution_selector.h` | #571 | Raw PDB proves explicit nonvirtual destructor. Model guarded deletion using retained fill_resolutions array cleanup; original typed destruction and guard remain unverified. Preserve embedded strings and retail's nonvirtual base cleanup path. |
| open | `key_binder::GetActionAllBinding` | `game/sources/key_binder.h` | #571 | Older commented formatting sketch is not a live output contract; recover destination consumer, localization/unbound fallback, invalid-action and buffer/truncation policy. See game/key_binder_inline_recovery.md. |
| open | `key_binder::mouse_sensitivity` | `game/sources/key_binder.h` | #571 | Retail's three input consumers read the existing mutable sensitivity global; getter and caller seams restored. Original named inline boundaries remain inferred; do not return its initial constant. |
| open | `key_binder::mouse_invertion` | `game/sources/key_binder.h` | #571 | Retail's two player-input consumers test the global; restore getter seams but verify original named boundaries. Lobby has no inversion operation and is left that way. |
| open | `key_binder::keyname_to_dik` | `game/sources/key_binder.h` | #571 | Legacy unchecked lookup/dereference model restored. Bind original private caller and valid-name precondition; do not invent null-to-zero behavior or a caller. |
| open | `key_binder::is_binded` | `game/sources/key_binder.h` | #571 | Legacy two-slot DIK predicate adapted to member array; recover nonconst private consumer. Const get_binded_action cannot directly use it, and bind's pointer/group conflict contract differs. |
| open | `key_binder::get_action_dik` | `game/sources/key_binder.h` | #571 | Restore older indexed/fallback body and -1 selectors at both current consumers; retail standalone/create-slot and options-inline paths both implement fallback. Original argument spelling and general indexed contract remain legacy models, not facts from optimized parameter elision. |
| open | `simple_game_project::project_name` | `game/sources/game_project.h` | #571 | Retained minimap lookup has one project-resource temporary; restore config-key/empty-string getter and its consumer. Original named inline boundary remains inferred. See game/project_inline_recovery.md. |
| open | `simple_game_project::all_loaded` | `game/sources/game_project.h` | #571 | Five cook predicates now call the retained four-term helper; missing checks follow immediately preceding true stores. Verify original call/inline boundaries and deferred PDB projection without changing callback flag timing. |
| open | `base_project::register_object_to_resolve` | `game_core/base_project.h` | #571 | Retail/base instructions and by-value record operations agree at 0x6c bytes, but target has no body line records and frozen base has three. Recover original inline/header partition; not a structural completion claim. |
| open | `base_project::get_object_by_name` | `game_core/sources/base_project.cpp` | #571 | Surviving assertion scaffold does not establish elided original predicate. Keep UNKNOWN_EXPRESSION_T around the find-versus-end model until predicate evidence exists. |
| open | `base_game_scene(game&,scene const&,view const&)` | `game/sources/base_game_scene.h` | #571 | Retained sibling initialization plus supplied-handle assignment model; recover original consumer, construction order and scene-dependent policy. Do not invent a new base-class constructor. See game/scene_camera_inline_recovery.md. |
| open | `base_game_scene::get_physics_world() const` | `game/sources/base_game_scene.h` | #571 | Same-field getter model; the nonconst overload's private-field read is verified, but bind a consumer actually selecting the const overload. A const owner method can still access a nonconst scene reference. |
| open | `levelmap_camera::levelmap_camera` | `game/sources/levelmap_camera.h` | #571 | Actual world upcast replaces null-reference initializer; supplied world/director bindings and invalid bounds are a model. Recover original constructor consumer and bounds initialization. |
| open | `levelmap_camera::get_projection_matrix` | `game/sources/levelmap_camera.h` | #571 | Recover bounds producer, projected axes, fit/crop/aspect and clipping policy before implementing the top-down projection hypothesis. Current matrix result remains a placeholder. |
| open | `levelmap_camera::on_keyboard_action` | `game/sources/levelmap_camera.h` | #571 | Recover key/context and focus-switch bindings and event consumption; class has no free-fly event queues. False is not verified. |
| open | `levelmap_camera::on_gamepad_action` | `game/sources/levelmap_camera.h` | #571 | Determine consumed versus ignored actions from actual expansion; free-fly and lobby sibling policies disagree. |
| open | `levelmap_camera::on_mouse_key_action` | `game/sources/levelmap_camera.h` | #571 | Recover selection/focus/capture policy and owner; no lobby-style capture flag exists in retail layout. |
| open | `levelmap_camera::on_mouse_move` | `game/sources/levelmap_camera.h` | #571 | Recover immediate pan/zoom versus ignored motion, axis conventions, constraints and return policy. |
| open | `levelmap_camera::input_priority` | `game/sources/levelmap_camera.h` | #571 | Ten is a sibling-camera model, unlike the independently verified free-fly fix. Recover level-map's own value and registration consumer. |
| open | `levelmap_camera::tick` | `game/sources/levelmap_camera.h` | #571 | Recover static bounds-to-view update versus input polling. No timer field justifies copying free-fly timing logic. |
| open | `levelmap_camera::on_activate` | `game/sources/levelmap_camera.h` | #571 | Recover bounds owner and initial top-down pose, depth and orientation before choosing base director-copy versus replacement behavior. |
| open | `levelmap_camera::on_focus` | `game/sources/levelmap_camera.h` | #571 | Sibling add/remove input-handler model implemented through existing interfaces; verify original consumer and empty base-hook delegation. No new invocation is introduced. |
| open | `ai_sound_player::serialize` | `game/sources/ai_sound_player.cpp` | #571 | Legacy emitter/proxy capture model restored in existing cpp; recover original private invocation, active-sound preconditions and bound-player/asynchronous writer lifetime. See game/ai_weapon_sound_inline_recovery.md. |
| open | `ai_sound_player::deserialize` | `game/sources/ai_sound_player.cpp` | #571 | Legacy debug-file/emitter replay model restored; retail sound-level decoder returns null and completion callback does no cleanup. Recover original activation/format and reader-buffer ownership without silently enabling decoder or inventing cleanup. |
| open | `human_npc::set_filter` | `game/sources/human_npc.h` | #571 | Legacy AI-world forwarding model restored; recover original NPC caller and brain-readiness guards. Shipping ignorable_game_object alias matches retail raw pair. See game/human_npc_inline_recovery.md. |
| open | `human_npc::clear_filter` | `game/sources/human_npc.h` | #571 | Legacy clear-ignore-filter forwarding model; recover original reset invocation and surrounding conditions. |
| open | `human_npc::add_weapon` | `game/sources/human_npc.h` | #571 | Legacy contains_object guard then push_back; original inventory caller and surrounding synchronization remain unbound. |
| open | `human_npc::remove_weapon` | `game/sources/human_npc.h` | #571 | Legacy unlink-only erase model; recover caller/destruction ownership. Attribute assignment swaps lists and world cleanup pops/deletes separately. |
| open | `human_npc::get_dbg_render` | `game/sources/human_npc.h` | #571 | Legacy renderer reference getter; commented older sound-player use is not proof of retail invocation. |
| open | `human_npc::get_dbg_scene` | `game/sources/human_npc.h` | #571 | Legacy scene const-reference getter; bind original retail consumer without introducing a resource copy. |
| open | `human_npc::set_sound_dbg_mode` | `game/sources/human_npc.h` | #571 | Legacy flag assignment model; recover original control invocation and build guards. |
| open | `human_npc::get_sound_dbg_mode` | `game/sources/human_npc.h` | #571 | Older sound player reads this flag, but original retail use remains unbound. Do not insert a new guard into retained drawing. |
| open | `human_npc::return_this` | `game/sources/human_npc.h` | #571 | Existing affect-subscription bind now uses declared helper; retail captures the same NPC pointer but original named inline boundary remains open. |
| open | `ai_collision_object` geometry-wrapper models | `game/sources/ai_collision_object.h` | #571 | All 17 bodies follow older source; recover independent retail construction and query consumers. New world callback is only a model consumer, not proof of use. See game/human_npc_inline_recovery.md for individual entries. |
| open | `ai_collision_object` geometry-taking constructor allocator | `game/sources/ai_collision_object.h` | #571 | Legacy inner composite geometry uses game g_allocator while factory allocates outer wrapper with supplied allocator. Recover original caller's allocator invariant. |
| open | `ai_collision_object` instance-taking constructor ownership | `game/sources/ai_collision_object.h` | #571 | Legacy reference binding plus explicit deletion helper imply transferred instance ownership; recover original caller/lifetime before treating this as verified policy. |
| open | `ai_collision_object::delete_ai_collision_object` | `game/sources/ai_collision_object.h` | #571 | Legacy null-guard, explicit geometry deletion and wrapper deletion use supplied allocator. Verify allocator pairing/ownership; implicit destructor must not invent reference cleanup. |
| open | `game_world::is_loading_or_unloading` | `game/sources/game_world.h` | #571 | Existing loading-flag model only; recover a consumer before deciding whether resource/scene unloading adds a condition. See game/game_world_inline_recovery.md for this batch's evidence. |
| open | `game_world::selected_npc` | `game/sources/game_world.h` | #571 | Const-reference member getter model; stats and behaviour code establish selection storage but not this named helper boundary. |
| open | `game_world::check_selected_npc` | `game/sources/game_world.h` | #571 | Legacy toggle/select/clear model; original invocation and physics picker remain open. A model calling a still-null picker is not a completed selection pipeline. |
| open | `game_world::rotate_selected_npc` | `game/sources/game_world.h` | #571 | Legacy degree/yaw composition model; bind original consumer and verify rotation multiplication order. |
| open | `game_world::move_selected_npc` | `game/sources/game_world.h` | #571 | Relative-translation model through public transform methods; determine whether original argument was delta or absolute position. |
| open | `game_world::delete_selected_npc` | `game/sources/game_world.h` | #571 | Legacy erase/kill/selection-reset model; recover notifications and ownership/order. Old NPC query-count field is absent from retail and must not be added. |
| open | `game_world::assign_behaviour` | `game/sources/game_world.h` | #571 | Legacy guarded test-resource query model using existing behaviour params; retail path, parameters and original binding remain unverified. No new invocation added. |
| open | `game_world::hit_player` | `game/sources/game_world.h` | #571 | Body open: resolve hit-info target lookup and local damage application versus network authority; do not duplicate the retained packet hit path. |
| open | `game_world::take_victory_item` | `game/sources/game_world.h` | #571 | Indexed take model; original caller and outer insertion guard are unbound. Preserve existing network resource copies, guards and container operations. |
| open | `game_world::initialize_physics` | `game/sources/game_world.h` | #571 | Constructor now delegates through its declared wrapper to existing init_physics. Allocation/initialization sequence is observed; named wrapper boundary awaits deferred caller comparison. |
| open | `game_world::get_frustum_objects_callback` | `game/sources/game_world.h` | #571 | Legacy collision-to-AI forwarding model; recover original query binding and concrete ai_collision_object provenance. |
| open | `game_world::find_npc_in_camera_direction` | `game/sources/game_world.h` | #571 | Body open: recover physics-result NPC identity, occlusion/order, query masks and ray extent. Legacy collision-triangle picker is not type-compatible evidence for this path. |
| open | `game_world::on_npc_dictionary_created` | `game/sources/game_world.h` | #571 | Legacy config callback model restored; retail query_npc_dictionary is empty. Locate original query path/bind without wiring a new query into that retained return. |
| open | `game_world::draw_respawn_debug` | `game/sources/game_world.h` | #571 | Body open: enemy/spawn lists exist but primitives, colors, conditions and draw invocation remain unknown. |
| open | `find_closest_collision_predicate` constructor | `game/sources/game_world_npc.cpp` | #571 | Existing null/unit-fraction model; original closest-hit traversal is needed to verify initial values. |
| open | `find_closest_collision_predicate::operator()` | `game/sources/game_world_npc.cpp` | #571 | Declaration-only method now has strict smaller-fraction model. Resolve ties, fraction-one endpoint, result lifetime and original consumer. |
| open | `test_objects_in_shape_predicate` constructor | `game/sources/game_world_npc.cpp` | #571 | Existing false-until-contact model; recover original construction/query consumer. Generated copy/assignment remain implicit. |
| open | `test_objects_in_shape_predicate::add_single_result` | `game/sources/game_world_npc.cpp` | #571 | Declaration-only override now has any-contact flag/zero-return model. Physics forwards user data and both shapes; original filtering, float-result contract and query binding remain open. |
| open | `get_first_npc_in_camera_direction_predicate` constructor | `game/sources/game_world_npc.cpp` | #571 | Legacy null first_npc model; recover the physics-based selection producer. |
| open | `get_first_npc_in_camera_direction_predicate::operator()` | `game/sources/game_world_npc.cpp` | #571 | Body open: base_physics_object user-data game-object interface has no NPC cast; recover actual ownership/identity instead of casting as the older collision object. |
| open | `ray_query_predicate` constructor | `game/sources/game_world_npc.cpp` | #571 | Legacy initial false result restored; original collision-query consumer remains open. Retail game_world::ray_query uses physics results and is not this consumer. |
| open | `ray_query_predicate::predicate` | `game/sources/game_world_npc.cpp` | #571 | Legacy subtractive-attenuation model restored; verify material visibility payload ownership and original query binding. No wiring into retained result-discarding physics query. |
| open | `human_npc::set_rotation` | `game/sources/human_npc.h` | #571 | Earlier human_npc.cpp confirms legacy scale/new-rotation/translation composition with const new_transform; verify retail composition and original consumer. Preserve set_transform renderer/animation/feet-target effects. |
| open | `human_npc::get_rotation_angles` | `game/sources/human_npc.h` | #571 | Legacy get_angles_xyz getter; retained set_translation uses that matrix operation, but the named getter consumer remains unbound. |
| open | `human_npc::get_transform` | `game/sources/human_npc.h` | #571 | Existing matrix-reference model; locate original named consumer. Method remains non-const as declared in PDB. |
| open | `game_material_visibility_parameters::get_transparency_value` | `game/sources/game_material_visibility_parameters.h` | #571 | Legacy single-field getter model; original retail material-query consumer remains unbound. Constructor zero store is separately observed in human_npc. |
| open | `player::detach_controller` public camera-mode boundary | `game/sources/player.cpp` | #571 | Restore public switch_camera_mode(free_fly_mode), removing invented friend dependence. Retail branch operations agree; new caller codegen awaits deferred comparison. Old frozen target/base already had four equal-sized statements. |
| open | `network_client::connect_to_login` public scene-switch boundary | `game/sources/network_client.cpp` | #571 | Retail spectator path performs the same compare/deactivate/store/activate operation as public game::switch_to_game_world. Restore that interface and remove invented friendship; original named boundary awaits the deferred caller structure/assembly comparison. |
| open | `game::game_time_sec` cached-clock model | `game/sources/game.h` | #571 | Model float(game_time_ms()) * epsilon_3; cached/fresh clock and multiplication/division source boundary are unobserved. Keep game time, permanent time and last-frame coordinate distinct. See game/game_inline_recovery.md. |
| open | `game::test_action_portal_system` forwarding model | `game/sources/game.h` | #571 | Forward to the existing world hook without a new key/console binding. Original caller, active-scene selection and loading guard remain open; not a proven retail expansion. |
| open | `game_world::test_action_portal_system` facade model | `game/sources/game_world.h` | #571 | Forward the owned scene through existing scene_renderer facade. Original consumer, scene-readiness policy and source spelling remain unverified; no new renderer helper or build claim. |
| open | `game::query_render_window(bool)` async ownership | `game/sources/game.h` | #571 | Still open: recover bool meaning and callback/recreation pipeline. Retail records the window-configuration/query locals in on_configs_loaded; load_cc_script creates a distinct renderer resource, and Alt-Tab does not query. Do not move the whole startup block or invent an async path. |
| open | `game::toggle_debug_window` original consumer | `game/sources/game.h` | #571 | Restore legacy none/resources/tasks/none cycle; enum and drawing/window ownership agree with retail. Original invocation/binding is not recovered. Do not add allocation, input bindings or an editor-only claim. |
| open | `lobby_client::get_player_faction_reputation` keyed lookup model | `game/sources/lobby_client.h` | #571 | Model first matching faction_id and u16 points widened to u32, with zero on miss. Raw received records/UI establish fields, not original duplicate/missing-key policy or consumer. See game/lobby_client_inline_recovery.md. |
| open | `lobby_client::get_player_name` stored-name consumer | `game/sources/lobby_client.h` | #571 | Keep fixed-string reference to +0x230, populated from profile zero. No named getter consumer found; do not substitute account_nickname_, manufacture a temporary or classify editor/server-only. |
| open | `network_client::process_shop_action` search partition | `game/sources/network_client_lobby.cpp` | #571 | Restore by-value add_inventory_item seam from the PDB parameter and separate 16-byte target copy. Retail's separate begin/end/scalar-walk statements are not proven to be std::find; resolve search boundary without invented locals or an unproven STL/LTCG wall. |
| open | `lobby_menu::on_items_compatibility_arrived` element boundary | `game/sources/lobby_menu_ui.cpp` | #571 | Remove narrowed count: target rereads full u32 +0x858 while keeping u8 i. Target also captures the item before CreateObject; original reference/construction/member-call partition remains open. Do not invent a named alias merely to force the hoist. |
| open | `lobby_menu::on_slot_restrictions_arrived` element boundary | `game/sources/lobby_menu_ui.cpp` | #571 | Remove narrowed count: target rereads full u32 +0x850 while keeping u8 i. Same pre-CreateObject element capture as compatibility; recover original boundary rather than declaring the remaining partition a linker wall. |
| open | `messaging_client::set_local_name` copy-only model | `game/sources/messaging_client.h` | #571 | Restore copying into the existing 32-byte member with strings::copy; original copy/length and chat-notification policy remain unobserved. Sign-in writes directly from packet and separately notifies chat; do not invent an extra live copy/notification. See game/messaging_client_inline_recovery.md. |
| open | `messaging_client::connected` inline boundary | `game/sources/messaging_client.h` | #571 | Ten retained send/list/subscription guards compare the private state at +0x88 with client_connected=3. Restore predicate and callers; operation is verified, original named boundary remains inferred. Do not substitute TCP connectivity or any non-disconnected state. |
| open | `messaging_client::local_user_name` inline boundary | `game/sources/messaging_client.h` | #571 | Outgoing conversion and sign-in chat notification pass client+0x118. Restore the existing getter at both consumers; original named boundary is inferred. Keep writable packet destination and any setter-notification hypothesis separate. |
| open | `network_client::load` non-returning source model | `game/sources/network_client.h` | #571 | Installed slot/raw bytes at 0x9ea00 copy both arguments and fall into padding; frozen candidate instead returns. Keep argument-use call plus unreachable model; original assertion/argument-use spelling and debug behavior are not recovered. See game/network_client_inline_recovery.md. |
| open | `network_client::is_player_connected` inline boundary | `game/sources/network_client.h` | #571 | Restore m_net_players[id].is_connected from the respawn read and connected-mask producer; propagate helper into respawn. Operation/slot are verified, original named inline boundary is inferred; no resource-presence or new bounds policy. |
| open | `network_client::on_player_death` wire handler | `game/sources/network_client.h` | #571 | Live 0x83 dispatch calls process_player_kill and consumes victim/killer/headshot/item fields. Recover the older hook's binding/payload and UI/kill split rather than assuming it shares that seven-byte schema. |
| open | `network_client::create_new_player` async contract | `game/sources/network_client.h` | #571 | Recover per-player wire/profile ownership/query callback/acknowledgment contract. Retained client accumulates full profiles then queries all players; no invented extra pipeline or message ID. |
| open | `network_client::destroy_player` byte-ID model | `game/sources/network_client.h` | #571 | Model reads u8 and delegates to retained destroy_player_impl. Original binding/ID width and whether outer handler also updates the separate connected flag are unobserved; no new switch case or flag side effect. |
| open | `network_client::process_sync_response` class variants | `game/sources/network_client.h` | #571 | Target has no-argument and reader-taking LF_MFUNCTION variants (0x1df69/0x1df7c); live procedure/dispatcher consumes reader and base matches that variant. Preserve it, resolve the other projection without fabricating an overload. Exact TIs recorded in network_client_inline_recovery.md. |
| open | `survarium::match_client::delete_packet` outer forwarding model | `game/sources/match_client.h` | #571 | Forward through underlying network client to preserve its order-packet allocator and pointer nulling. No original outer deletion consumer found; do not call this a proven leak fix. See game/match_client_inline_recovery.md. |
| open | `survarium::match_client::on_connected` callback ownership | `game/sources/match_client.h` | #571 | Retained outer connect forwards the caller callback directly; underlying network client owns the callback privately. Recover this four-error hook's actual binding/state effects before adding a duplicate callback, reset or rebinding. |
| open | `weapon_user_dead_state::selected_animations` const RNG spelling | `game/sources/weapon_user_dead_state.h` | #571 | Target const method advances m_random at +0x2c. Mutable member is the current working form; raw type/access records cannot distinguish it from an original const_cast. See dead_preview_state_inline_recovery.md. |
| open | `player_logic_preview_state::selected_animations` const RNG spelling | `game/sources/player_logic_preview_state.h` | #571 | Target const method advances m_random at +0x30. Preserve observed operations, but original mutable versus const_cast spelling remains unresolved. |
| open | `weapon::load_weapon` preview creation partition | `game/sources/weapon.cpp` | #571 | Target 16-statement partition loads the two death counts at +0x243 before preview allocation/constructor +0x24f; current argument expression is combined. Recover source boundary without fabricating a named local or claiming an unproven LTCG wall. |
| open | `first_predicate<T>::operator()` generic signature | `game/sources/player_input_handler.cpp` | #571 | Retained __find_if at 0x86930 proves equality of pair.first and captured enum. Raw instantiated type omits operator declaration; current generic P const& template is a source model, not a recovered primary-template signature. See player_owned_inline_recovery.md. |
| done | `client_player_history_item` base access projection | `game/sources/client_player_history_item.h` | #571 | Raw LF_BCLASS attributes are 0x0001 (private) in both PDBs. Source and topology are correct; gen_headers.rs hardcodes public. See pdb_base_access_rendering.md for record indices and deferred generator-patch validation. |
| done | `profile_player_character` base access projection | `game/sources/profile_player_character.h` | #571 | Raw target/base class TIs 0x189a3/0x13ec7 reference private LF_BCLASS entries. Preserve private source; the generator, not this class, loses access. See pdb_base_access_rendering.md. |
| open | `weapon_core_cook::register_cooks_for_logic_states` registration ownership | `game_core/weapon_core_cook.h` | #571 | Recover the original core-only list/lifetimes/overlap policy. Retained game method at 0x5bd320 registers distinct sound-wrapped/game types; do not copy its list or invent a call into core. See cook_inline_recovery.md. |
| open | `weapon_core_cook::on_skeleton_config_loaded` callback contract | `game_core/weapon_core_cook.h` | #571 | Recover the query binding, input resource class and continuation. Live process_loading_weapon_core binds on_core_subresources_ready and loads skeleton/user-animation resources, not a skeleton binary-config stage. |
| open | `damage_model_cook::get_current_hit_type` selection model/placement | `game_core/sources/damage_model_cook.cpp` | #571 | Indexed static-string model follows sequential console token IDs. Original getter consumer, pre-initialization/invalid-ID behavior, lookup spelling and body placement remain open; cpp definition preserves file-static ID without inventing private cc_token access. |
| open | `weapon_core_shotgun_reload_state_cook` constructor model | `game_core/weapon_core_shotgun_reload_state_cook.h` | #571 | Non-reuse/current-thread/self-registration model follows core state templates and retained game shotgun donor. Donor is not derived from this core cook. Original core expansion and registration owner remain unobserved; no duplicate class-ID registration is introduced. |
| open | `animation_analyzer::analyze` active pipeline | `game_core/animation_analyzer.h` | #571 | Retained cook constructs analyzer/result but performs no analysis. Pipeline allocation/sample count/orchestration remains a hypothesis; need an original active producer, not an invented call in this cook. See animation_analysis_inline_recovery.md. |
| open | `animation_analyzer::prepare_samples` sample contract | `game_core/animation_analyzer.h` | #571 | Recover clip evaluation/sample rate, frame-leg layout, inclusive endpoints, ground reduction and middle/foot-base construction. Existing bone/offset records constrain geometry, not the algorithm. |
| open | `animation_analyzer::analyse_samples` event extraction | `game_core/animation_analyzer.h` | #571 | Recover integer role and seven event-time definitions/thresholds/order from an active original consumer; no fabricated timing outputs. |
| open | `animation_analyzer::get_max_range` reduction | `game_core/animation_analyzer.h` | #571 | Scalar-axis range versus spatial extent, sample field, integer bounds/count/stride and empty behavior remain unknown. |
| open | `animation_analyzer::get_stance_index` selection | `game_core/animation_analyzer.h` | #571 | Recover float tolerance/height role, sample bounds, stance criterion and tie/wrap policy. |
| open | `animation_analyzer::get_cycle_center` reduction | `game_core/animation_analyzer.h` | #571 | Mean, endpoint midpoint, extrema or stance center cannot be selected from return type/name alone; coordinate space and integer roles remain open. |
| open | `animation_analyzer::find_contact_time` directional search | `game_core/animation_analyzer.h` | #571 | Signed scan/starting-index hypothesis needs heel/toe versus contact/release bool role, float threshold/time roles, loop bounds and fractional interpolation. |
| open | `animation_analyzer::find_swing_change_time` directional search | `game_core/animation_analyzer.h` | #571 | Recover swing-direction crossing, supplied vector role, signed search/wrap, float convention and interpolation. |
| open | `animation_analysis_result::key_times` getter consumer | `game_core/animation_analysis_result.h` | #571 | Existing +0x10c mutable-reference model agrees with buffer construction/destruction, but no consuming getter site found. Retail cook does not fill the timings. |
| done | Analyzer noncopyable base-access projection | `game_core/animation_analyzer.h` | #571 | Both raw LF_BCLASS entries encode private (0x0001), matching source/topology. Generated public spelling is hardcoded by gen_headers.rs; exact TIs and patch are recorded in pdb_base_access_rendering.md. |
| open | Inactive jump `selected_animations` unreachable source spelling | `game_core/sources/jump_logic_state_inactive.cpp` | #571 | Retained RVA 0x57ce30 returns with untouched hidden result storage; keep the existing shipping unreachable model, not a fabricated expression-pair return. FSM flow supports unused selection while inactive but does not prove every call sequence or original assertion spelling. See locomotion_state_inline_recovery.md. |
| open | `legs_ik_processor::set_drawer` ownership policy | `game_core/legs_ik_processor.h` | #571 | Pointer-install model restored; destructor proves ownership of current drawer, but no original construction/setter consumer or replacement cleanup/assertion policy was found. See legs_ik_inline_recovery.md. |
| open | `legs_ik_processor::is_right_supporting_leg` original consumer | `game_core/legs_ik_processor.h` | #571 | Right-versus-left forwarding model restored; original consumer and ties remain open. Do not insert it into process's verified discarded ground checks. |
| open | `leg_params::set_rotation_axis` const storage/source seam | `game_core/legs_ik_processor.h` | #571 | Target records const method; process_leg copies normalized cross product to +0x1c. Restore setter call with explicit receiver const_cast model for mutable caller. Original cast versus mutable member and inline boundary remain open; genuinely const receiver mutation is not validated. |
| open | `leg_params::is_more_supporting_then` ordering | `game_core/legs_ik_processor.h` | #571 | Recent-full-stance (< private age) model replaces undefined bool return. Setters/reset/tick explain data, but direction, contact priority, ties and original consumer remain unknown. |
| open | `legs_ik_drawer` original constructor consumer | `game_core/legs_ik_drawer.h` | #571 | Existing reference/scene-copy initializer model retained; no original allocation/construction path found. Typed delete helper proves scene destruction, not ctor source. |
| open | `legs_ik_drawer::draw_line` original inline seam | `game_core/legs_ik_drawer.h` | #571 | Scene-bound renderer forwarding is verified in four draw_leg consumers; restore wrapper and caller use, keeping original direct-call versus inline-boundary question open. |
| done | IK base/drawer noncopyable access projection | `game_core/ik_processor.h` + `game_core/legs_ik_drawer.h` | #571 | Both classes independently have private LF_BCLASS attributes in both PDBs. Keep current inheritance; gen_headers.rs loses access and hardcodes public. Raw records are in pdb_base_access_rendering.md. |
| open | `player_profile::serialize` original sender and sparse policy | `game_core/player_profile.h` | #571 | Inverse-reader model lives beside the reader in game_net_defines.h. Nonzero booster IDs and occupied slot IDs select sparse entries; consumer/default evidence supports the model, not the original writer policy. Reader leaves absent slots unchanged, so this is not an arbitrary removal/update protocol. See profile_packet_inline_recovery.md. |
| open | `inventory_item_instance::serialize` original sender and narrowing | `game_core/inventory_item_instance.h` | #571 | Compact profile protocol uses u16 dictionary ID/u32 instance ID, optional u16 condition and u32 amount. Positive mode checks are verified in retail reader; original writer range checks/narrowing policy remain open. Do not substitute this for the 32-bit shop or raw-record lobby protocols. |
| open | `profile_slot::serialize` original forwarding boundary | `game_core/profile_slot.h` | #571 | Model forwards packet/mode to item writer, paralleling the verified reader seam. Original sender/boundary not found; no additional policy added to the slot wrapper. |
| open | `client_player_update::deserialize` original receiver | `game_core/client_player_update.h` | #571 | Input/state/u32-time inverse writer model is implemented; retained writer and queue consumer prove the format, not original receiving assertions or source partition. See update_packet_inline_recovery.md. |
| open | `server_player_update::serialize` original sender | `game_core/server_player_update.h` | #571 | Input/state/weapon-state inverse reader model is implemented; original sender and guards remain unidentified. |
| open | `weapon_state` copy-constructor source form | `game_core/weapon_state.h` | #571 | Remove undefined handwritten copy declaration absent from the target class projection/procedures; implicit three-byte copying is retained. Missing PDB record does not prove original implicit versus explicit source form. |
| open | `weapon_state::serialize` original sender | `game_core/weapon_state.h` | #571 | Three u8 appends model the retained byte reader; do not infer bool from the shared r<bool>/r<u8> RVA. Original sending consumer remains unidentified. |
| open | `hit_info::serialize` original sender | `game_core/hit_info.h` | #571 | Byte IDs, length-prefixed strings and floats invert the retained reader; bullet pointer is not transmitted. Original sender/guards remain unidentified. |
| open | `relocate_item_descr::deserialize` original receiver | `game_core/game_net_defines.h` | #571 | Five u32 plus u16 amount model the retained 22-byte writer; original receiving consumer and validation remain open. |
| open | `match_options::serialize` original sender | `game_core/game_net_defines.h` | #571 | Model only fields consumed by retained options reader, excluding profiles and local sentinels/wait settings. Original sending consumer and guards remain open. |
| open | Callback-pool `buffer()` consumer | `game_core/bullet_manager.h` | #571 | Existing +8 non-owning buffer getter agrees with constructor/swap storage, but original getter consumer is unidentified; no allocator-owned free added. |
| open | `bullet_manager::clear_decals` | `game_core/bullet_manager.h` | #571 | Full-ring removal model iterates 0..max_decal_count-1 with an engine guard, preserving the next-ID cursor. Retained add path proves ring wrap; original clear consumer, guard, reset and queued-work policy remain unknown. |
| open | Bullet tracer forwarding boundaries | `game_core/bullet_manager.h` | #571 | Restore attach/detach bool engine forwarding in existing emit/free callers, preserving caller-owned engine/tracer guards. Retail proves virtual slots +0x10/+0x14 and synchronous calls, not exact source seam or returned-bool contract when callers discard it. |
| open | `bullet_manager::destroy_redundant_bullets` | `game_core/sources/bullet_manager.cpp` | #571 | Restore declared helper from tick's full remove/erase statement and use it after callbacks. Predicate zero-start-velocity/free behavior is retained; helper boundary remains inferred and frozen tick already structure-matches. |
| open | Callback-pool allocation assertion/boundary | `game_core/bullet_manager.h` | #571 | Retail malloc_impl has a post-try_pop dummy assertion use absent from base new-helper expansion. Put pop/assert/return in typed allocate and delegate malloc_impl; exact original assertion and helper-local spelling remain unverified. |
| done | Callback-pool base access spelling | `game_core/bullet_manager.h` | #571 | Target/base field-list TIs 0xbb13/0x3e3e8 start with private LF_BCLASS attributes 0x0001. Preserve current source; the public generated header is a renderer defect. See pdb_base_access_rendering.md. |
| open | Bullet position/initiator getter consumers | `game_core/bullet.h` | #571 | Existing get_start_position/get_position/get_velocity/get_initiator_id field models retained; original consumers not found. Start-velocity and last-hit getter/setter are separately verified in live predicate/damage consumers. |
| open | Live bullet-pool reallocation contract | `game_core/sources/bullet_manager.cpp` | #571 | Retained 0x591fa0 loop reads element zero, copies/deletes it, then branches back without erasing the vector element; bullet destructor does not unlink. Preserve the observed source behavior, but identify allowed reallocation/lifetime preconditions before enabling it on nonempty pools. |
| done | Particle callback normal destination | `game_core/sources/bullet_manager.cpp` | #571 | Retail 0x5916e0+0xb5 stores supplied normal to functor +0x38; frozen base stores to direction +0x2c then normalizes uninitialized normal. Correct destination despite identical 10-statement/0x29f-byte structure. Restore const functor pointers also required in decal/particle/tracer producers. |
| open | Material-pair sound/secondary-decal getters | `game_core/material_pair.h` | #571 | Existing sound/decal2/decal2_size field contracts agree with layout, resource setters and config loading; original getter consumers/expansions not identified. Do not route primary-decal bullet effects through these different slots. |
| open | `game_material::width` | `game_core/game_material.h` | #571 | Existing +0x54 getter retained as field model; original consumer not found. Config/layout alone do not verify a specific inline expansion or justify applying width to bullet penetration. |
| open | `game_material_manager::get_material_id` | `game_core/game_material_manager.h` | #571 | Model delegates to name membership and returns loaded default ID on miss, by analogy with numeric get_material. Original missing-name assertion/sentinel/default policy and consumer remain unknown. |
| open | `game_material_manager::material_exist(pcstr,u16*)` | `game_core/game_material_manager.h` | #571 | Model scans named materials, compares with strings::equal, writes optional map-key output on success and preserves it on miss. Case/null/duplicate-name/output policies and original consumer remain unverified. |
| open | `game_material_manager::pair_exist` | `game_core/game_material_manager.h` | #571 | Exact ordered nested-map membership model, without adding fallback or reverse lookup. Retained add/get operations prove ordered storage, not this method's possible symmetry/default substitution policy. |
| open | Material-pair noinline annotations | `game_core/material_pair.h` | #571 | Existing decal1/decal1_size/has_particle bodies match retained leaves, but standalone caller boundaries do not prove source noinline attributes. Keep separate from semantic verification; reassess attribute removal in deferred measured pass. |
| open | Material-manager cook friendship | `game_core/game_material_manager.h` | #571 | Existing cook writes default ID and calls private insertion methods. No declared setter/helper recovered to replace this seam; PDB validates access but not friendship. Preserve current friend until original relationship is established. |
| open | `collision_user::get_artcontainer_time_factor` | `game_core/collision_user.h` | #571 | Existing owner-record getter matches its field contract, but original consumer is unidentified. Do not force it into artefact-container's direct supplied-user read without proving both records are the same. |
| open | Default `player_death_subscriber()` | `game_core/player_death_subscriber.h` | #571 | Empty callback/null next model follows supplied-callback sibling; that sibling's live next initialization is verified, but no default-construction consumer was found. Exact empty-callback constructor form remains open. |
| open | Collision-user factor accessor boundaries | `game_core/collision_user.h` | #571 | Propagate both setters into player-parameter apply and engineer getter into trap defuse. Target proves base-player +0x24/+0x28 stores and owner +0x1c load, not whether original code used these declared inlines or direct user-data fields. Keep artcontainer's supplied-user read unchanged. |
| done | Supplied-callback death subscriber link | `game_core/player_death_subscriber.h` | #571 | Retail trap-set constructor 0x730840+0x68 clears subscriber +0x20 after callback copy; independent base omits it. Restore next(NULL), preserving padding and callback ownership. |
| done | Trap-defuse const locals | `game_core/sources/booby_trap_core.cpp` | #571 | Target PDB at 0x58b460 names passed_ms/engineer_factor/defuse_time_ms const; config_defuse_time remains mutable. Restore only the three required qualifiers. |
| open | `scheduler::change_status` | `game_core/scheduler_inline.h` | #571 | Transfer model copies into destination, reuses retained unregister swap/pop/index policy, then retags identifier. Original transfer order, source-boundary and callback-time index behavior lack an original consumer; unregister itself decrements current index without checking active state. |
| open | `scheduler::activate` | `game_core/scheduler_inline.h` | #571 | Guarded inactive-to-active transfer model. Repeated activation may originally assert instead of no-op; source consumer, timestamp/reset and in-frame visitation policy remain unknown. |
| open | `scheduler::deactivate` | `game_core/scheduler_inline.h` | #571 | Guarded active-to-inactive transfer model. Original repeat-call/iteration policy and consumer remain unknown; do not replace retained register/unregister lifecycle calls merely to force use of the seam. |
| done | Scheduler per-frame mode and interval local | `game_core/scheduler_inline.h`, `game_core/sources/scheduler.cpp` | #571 | Retail register_on_frame writes 0x7fffffff to record +0x28; frozen base ORs it, preserving an uninitialized fixed-interval bit. Restore explicit type_on_frame assignment within its retail statement. Per-record on_frame PDB also requires const u32 update_delta. |
| open | `collision_sensor::is_active` | `game_core/collision_sensor.h` | #571 | Direct +0x1c flag getter model; retained constructor/insert/remove prove lifecycle stores, but an original getter consumer and possible additional policy remain unverified. |
| open | `collision_sensor::dbg_render` | `game_core/collision_sensor.h` | #571 | Model forwards color to each geometry. Original debug-draw consumer, active-state guard and assertion policy are missing; no speculative live caller added. |
| open | `collision_geometry::dbg_render` | `game_core/collision_geometry_inline.h` | #571 | Model forwards stored ghost/world and color to the existing physics helper. World is null before insertion/after removal; verify lifetime/guard/assertion policy and the lower helper's original consumer before enabling debug calls. |
| done | Sensor object-filter polarity | `game_core/sources/collision_sensor.cpp` | #571 | Retail predicate expansion at 0xb6f20 negates virtual is_filter_passed; frozen candidate 0xcd640 does not. std::remove_if must remove failing objects, not passing ones. |
| done | Sensor notification defaults and subscriber casts | `game_core/sources/collision_sensor.cpp` | #571 | Defaults are retained empty protected virtuals, but callers dispatch through the vtable (inside at +0x18); derived zone overrides remain live. Target records also inherit the subscriber's null cast_to_sensor without a sensor override. Remove stale debugger/missing-override speculation, not dispatch or base vptr stores. |
| open | `generic_anomaly_core::on_hit_anomaly` | `game_core/generic_anomaly_core.h` | #571 | Model mirrors retained shoot-energy/trigger operations. Whether an anomaly hit means a shot, character-hit energy or trigger-only behavior lacks an original event consumer; do not propagate the model into existing player event cases without evidence. |
| open | `generic_anomaly_core::on_explosion_in_anomaly` | `game_core/generic_anomaly_core.h` | #571 | Add configured explosion energy through existing inc_energy guard. Retail load proves the +0x58 setting, not whether this event also changes a zone/shoot trigger. |
| open | `generic_anomaly_core::state_changed` | `game_core/generic_anomaly_core.h` | #571 | Default is a retained bare ret. Hook invocation timing relative to transition assignment/finalize/initialize remains unknown; current derived class has no override and existing tick structure already matches. No speculative call added. |
| open | `anomaly_state::on_zone_act` forwarding boundary | `game_core/anomaly_state.h` | #571 | Restore owner forwarding and use it in zone_group callback. Retail group +0x18/state +0x28 owner chain and core call prove operations, not which inline seam original source used. Existing baseline structure matches; verify preservation in deferred build. |
| open | `hit_receiver::{on_enter_damage_zone,on_leave_damage_zone}` | `game_core/hit_receiver.h` | #571 | Likely subscription/owner-notification helpers, but guard/order/reference lifetime unproven. Retained zone callbacks dispatch directly using raw pointers; core is not the concrete resource-owning base. Do not invent a core resource-pointer temporary to force the seam. |
| open | `hit_receiver_info::operator==(hit_receiver const*)` | `game_core/hit_receiver.h` | #571 | Direct raw-pointer equality model; observed info-to-info sibling instead compares loose payloads. Verify this overload's original consumer/null/expired-pointer policy before conflating them. |
| open | `erase_old_receivers::operator()` | `game_core/hit_receiver.h` | #571 | Missing body hypothesis is contact/membership check plus possible leave notification. Retained bool collision_sensor::contact_test ignores the object argument, so it is not proven a receiver-specific overlap probe. See damage-zone inline register. |
| open | `remove_left_receivers_predicate::operator()` | `game_core/hit_receiver.h` | #571 | New not-in-buffer model uses existing info comparison; original set polarity, duplicate handling and notification behavior remain unverified. |
| open | `damage_zone_core::set_resources` | `game_core/damage_zone_core.h` | #571 | Establish queries-result ordering, u32 index/count meaning and geometry lifetime; actual cook uses concrete load and link resolution rather than this helper. |
| open | `damage_zone_core::remove_null_receivers` | `game_core/damage_zone_core.h` | #571 | Raw-null erase model reuses erase_null_ptrs through bind<bool>(ref(predicate),_1), preserving its noncopyable base. Raw-null versus cleared loose payload and original loop remain unknown; no new callback use inserted. |
| open | `damage_zone_core::remove_left_receivers` | `game_core/damage_zone_core.h` | #571 | Input may be the current or departed physics-object set. Need receiver conversion, duplicates and notification policy; cannot equate it with retained on_leave merely by name. |
| open | `damage_zone_core::calc_armor_piercing` | `game_core/damage_zone_core.h` | #571 | Implement and propagate the retained inside-hit min/max lerp with already-clamped hit coefficient. The helper's exact input contract/source placement remains inferred; verify caller partition in deferred build. |
| open | `damage_zone_core::hit(float)` | `game_core/damage_zone_core.h` | #571 | Determine float role (damage/time/curve scale) and receiver/bone/notification selection. Three retained hit modes have distinct policies, not enough to fabricate one common helper body. |
| done | `hit_receiver_info::operator==(hit_receiver_info const&)` | `game_core/sources/damage_zone_core.cpp` | #571 | Retail 0x587d80 follows receiver +4 loose data then compares payload pointers. Preserve the unusual existing expression; remove generic “think about this” TODO, not the indirection. |
| done | Hit-receiver subscription defaults | `game_core/hit_receiver.h` | #571 | Base functions are ret4 at 0x12c50. Player overrides own the real vector operations at 0x5d2e40/0x5d24b0 and zone callbacks call virtually; remove two false no-source markers without adding state to the base. |
| open | `medkit::activate` | `game_core/medkit.h` | #571 | Retail 0xbc450 emits dummy assertion use and direct user/engine reference-pointer helper call. Restore operations; exact original assertion is unknown. See medical-item inline register. |
| open | `oxygen_tank::activate` | `game_core/oxygen_tank.h` | #571 | Same retained 0xbc450 body and constructor-installed vtable entry as medkit; restore dummy use and pointer arguments, retaining the original assertion question. |
| open | `artefact_lifebone_core::activate` | `game_core/artefact_lifebone_core.h` | #571 | Same retained 0xbc450 operations; interactive activate is distinct from live activate_impl. Original assertion remains unknown. |
| open | `medkit` bool/result hook source contracts | `game_core/medkit.h` | #571 | Vtable bool slots 11/16 point at 0x1a800, which never assigns AL/EAX. Remove unsupported false returns using an explicit unreachable-source model; original reachability/macro spelling and existing unwritten-transform result remain open. |
| open | `oxygen_tank` bool/result hook source contracts | `game_core/oxygen_tank.h` | #571 | Same no-result retail bool body at 0x1a800 versus candidate false constants. Silent-action routing explains ordinary use separation, not universal unreachability. Validate source model in deferred build. |
| open | `artefact_lifebone_core` bool/result hook source contracts | `game_core/artefact_lifebone_core.h` | #571 | Remove unsupported true constants from both bool hooks; vtable points at no-result 0x1a800. Original unreachable/assert form remains unknown; do not invent safe returned values. |
| open | `medkit::selected_animations` | `game_core/medkit.h` | #571 | Retail 0xbcd50 has dummy assertion use plus direct buffer/bool argument consumption, but writes no expression result. Restore observed operations; exact assertion and unreachable-source form remain open. |
| open | `oxygen_tank::selected_animations` | `game_core/oxygen_tank.h` | #571 | Same retained 0xbcd50 operations and genuine returning epilogue; do not copy the non-returning trap/inventory-header extent classification. Original assertion/result form remains open. |
| open | `artefact_lifebone_core::selected_animations` | `game_core/artefact_lifebone_core.h` | #571 | Restore common dummy/argument-use operations, retaining assertion/result uncertainty. Its existing conditional bone-update argument macro has a different observed branch shape and is preserved. |
| open | `damage_model::fill_stats(damage_info_type&,u32)` | `game_core/damage_model.h` | #571 | Player-statistics model reuses NPC heading and existing player predicate. Original heading, reset and formatting policy need the UI consumer; the two schema types were moved unchanged into a data-only header. See damage inline register. |
| open | `body_part_parameters::dump_state(damage_info_type&,u32)` | `game_core/body_part_parameters.h` | #571 | Reuse the existing private formatting template with the existing player item type. Its new explicit instantiation is model support, not a retained target procedure; original player formatting is unverified. |
| open | `hit_type_parameters::remove_vertex` / `body_part_parameters::remove_edges` | `game_core/hit_type_parameters.h` | #571 | Fixed trailing-array model disables every matching edge by zeroing its coefficient; target apply_damage only dereferences positive coefficients and count is const. Original pointer clearing and removal consumer remain unknown. |
| open | `body_part_parameters::is_healthy` / `damage_model::is_healthy` | `game_core/body_part_parameters.h` | #571 | Preserve exact equality as an explicit leaf model and compose find-first-unhealthy via existing check_health_predicate. Original threshold/affect and empty-parent policy lack a verified consumer. |
| open | `body_part_parameters::relative_health` | `game_core/body_part_parameters.h` | #571 | Existing raw-health return is not proof of original semantics. Establish raw versus normalized value and zero-max-health handling before using it to select a minimum. |
| open | `body_part_parameters::get_health_level_color` | `game_core/body_part_parameters.h` | #571 | Likely ratio-to-color conversion, but endpoints, thresholds, interpolation, alpha and zero-max-health behavior need a draw consumer. Keep body open rather than fabricate color constants. |
| open | `damage_model::{get_body_part_index,get_body_part_name}` | `game_core/damage_model.h` | #571 | Insertion-order lookup models use public next/get_name and u8(-1)/null failure results. Verify index identity, failure/assert policy and more-than-255-part behavior from original consumers. |
| open | `damage_model::get_body_part_with_min_health` | `game_core/damage_model.h` | #571 | Minimum scan hypothesis still needs raw/normalized metric, eligible subset, empty result and tie policy. get_total_health's death-eligible percentage minimum does not establish this helper's rule. |
| open | `damage_model::serialize` | `game_core/damage_model.h` | #571 | Per-part bind/foreach model follows retained parent deserialize. Child writer count repaired to one byte; original parent framing and assertions remain unverified. |
| open | `damage_model` modifier friendship | `game_core/damage_model.h` | #571 | Retail player_parameters_modifyer traverses the private body list. Preserve existing friendship until original friendship versus an unretained helper is established; do not disguise the question with guessed index traversal. |
| open | `affect_subscriber(callback const&)` | `game_core/damage_model.h` | #571 | Existing callback-copy/null-link constructor is an ownership model. Default constructor's null link is observed in damage_model construction, but no consumer for the supplied-callback overload was found. |
| done | `damage_model::fill_stats(ai::npc_statistics&,u32)` destination | `game_core/sources/damage_model.cpp` | #571 | Retail RVA 0x6ef7f0 statement 3 selects +0x2798 body_state, not +0x13cc selectors_state. Correct heading destination. Source-versus-retail finding; no retained frozen-base procedure/new build measurement claimed. |
| done | Body-part affect-count wire width | `game_core/sources/body_part_parameters.cpp` | #571 | Retail call at 0x587259 resolves to one-byte append helper 0x7d750; bool/u8 aliases share it. Reader local is u8 and helper 0x7e950 loads/increments one byte. Fix u32 writer cast to u8 and use r<u8> without trusting first alias spelling. Deferred build still required. |
| done | Damage/affect predicate stale access questions | `game_core/sources/body_part_parameters.cpp`, `game_core/sources/damage_model.cpp` | #571 | Inspected hit-type find_if at 0xba310 and type predicate at 0xab830; public booster m_hit_type needs no invented getter. Regenerate statement 10 already uses the existing +0x2c8 accessor. Remove the three obsolete source TODOs. |
| done | Booster string-copy capacity | `game_core/damage_model.h` | #571 | strings::copy calls strcpy_s with destination capacity 16, not a 16-byte source read. Short terminated input is valid; retained booster ctor confirms copy and does not initialize its derived +0x68 link. |
| open | `player_stamina::serialize` | `game_core/player_stamina.h` | #571 | Four-field wire model follows target deserialize (float/u32/u32/bool). The signed client-offset subtraction and zero guards follow the inspected body-part serializer, not a retained stamina writer; verify both clock sentinel policies and producer context. |
| open | `player_stamina_subscriber` supplied-callback constructor | `game_core/player_stamina_subscriber.h` | #571 | Default callback-null and next-null initialization are directly visible in sprint-state construction. Mirror next-null in the supplied-callback overload as a model; no consumer for that overload found. |
| open | Sprint-state constructor callback boundary | `game_core/sources/player_logic_sprint_state.cpp` | #571 | Default subscriber previously omitted target explicit null callback construction and +0x20 next store. Restore them before re-evaluating the remaining boost assignment expansion; preserve body assignment after member initialization, not a bind-based member initializer. |
| open | `player_stealth::can_be_detected` | `game_core/player_stealth.h` | #571 | Construction/configuration/copy are observed, but no detection consumer establishes which object/input is observer or subject, whether the float is distance or squared distance, or how visual/sound stance factors compose. Individual hypotheses in stealth/ladder register; not declared server-only. |
| open | `player_stealth` getters | `game_core/player_stealth.h` | #571 | Four direct-field models use the target's configured values; exact assertions/transforms lack consumers. Optics could reuse or transform detection level; leave it explicitly open instead of inventing a missing optics field. |
| open | `ladder::{select_landing_point,select_attachment_point}` | `game_core/ladder.h` | #571 | Animation eligibility recovered in the cook. Selection distance metric, vertical/directional rules, bool meaning, thresholds and tie policy remain open. Player use_ladder has a one-byte shared trap record, not a recovered traversal consumer; do not infer all selector bodies were absent. |
| open | Landing-point constructor/predicate caller boundary | `game_core/sources/ladder_cook.cpp` | #571 | Restore target next=NULL and explicit null resource construction. Replace by-value getter temporaries in the cook guard with two direct owned-pointer predicates, and restore three PDB const locals. Verify new caller partitions and remaining resource temporaries in deferred build; old blanket inliner-wall claim removed. |
| open | `weapon_recoil_calculator::set_interpolation_time` | `game_core/weapon_recoil_calculator.h` | #571 | Replace the owned linear interpolator with a supplied-duration instance as a model. No original consumer found; verify assignment versus reconstruction, assertions, and whether any elapsed timers reset. No private animation fields exposed. |
| open | `breath_vibration_calculator::set_max_breath_delay_time` | `game_core/breath_vibration_calculator.h` | #571 | No distinct maximum-delay field exists and params is a const pointer. Reserve assignment/clamping and maximum-duration policy are different contracts; recover a consumer before inventing const removal or treating the placeholder as an empty default. |
| open | `character_dispersion_calculator::reset` | `game_core/character_dispersion_calculator.h` | #571 | Constructor zero state and parameter assignment's three idle-value stores are distinct candidate reset policies; clock-only reset is another. No caller found. Determine values/clock/jump and retained configuration without inserting arbitrary resets into activation. |
| open | `player_input::is_walking` | `game_core/player_input.h` | #571 | Model is moving and not effectively sprinting. Determine whether original instead excluded the sprint request bit; no retained helper or discriminating consumer found. |
| open | `normal_random` range/suffixed overloads | `game_core/normal_random.h` | #571 | Seed restoration fixed from a live deserialization store; eight range/s-suffix overloads remain open. Determine endpoints, modulo versus scaling, negative/zero handling and the s-suffix meaning from original consumers, not naming alone. |
| open | Shotgun substate inherited helper boundaries | `game_core/weapon_core_shotgun_reload_base_substate.h` | #571 | Restore deserializing forwarding at start/finish gates and use base finalize in all three derived finalizers; remove the three private-playback-access friends. Verify source partitions in deferred build; no fields widened. |
| open | `missile_weapon_core` inline family | `game_core/missile_weapon_core.h` | #571 | Every method has a candidate contract in missile/spore register. Constructor defaults, injected-body ownership, registration/removal, configuration and launch semantics need a concrete original consumer. Output-transform body is a model, not verified expansion. |
| open | `artefact_spores_core` inline family | `game_core/artefact_spores_core.h` | #571 | Owner setter modeled; ghost ownership, configuration, collision filtering, clock meanings and damage cadence remain individually open in missile/spore register. Current include does not constitute a construction/use path, and the inventory interface remains abstract. |
| open | `booby_trap_set_core` inactive-object virtual defaults | `game_core/booby_trap_set_core.h` | #571 | Remove unsupported true/empty-return defaults; keep unreachable model consistent with shared target trap records and direct argument-use/nonreturning activate/selected_animations bodies. Original macro spelling and actual vtable reachability remain open. |
| open | `booby_trap_set_core::{count_active_traps,append_inactive_trap_index_to_packet}` | `game_core/booby_trap_set_core.h` | #571 | Models count the existing active predicate and append only inactive indices as u8. Recover original packet consumer to verify filtering, sentinels, assertions and counts; do not invent calls merely for reachability. |
| open | `booby_trap_core::load_collision` | `game_core/booby_trap_core.h` | #571 | Existing collision-sensor/usable/hittable configuration block extracted into declared helper. Verify caller partition in deferred build; no extra load_aabb call or alternate geometry was invented. |
| open | `player_logic_base_state::get_attachment_transform` | `game_core/player_logic_base_state.h` | #571 | Target shares one-byte int3 at RVA 0x112e20 with 28 other records, including empty-hands deserialize and an SDK Free method with a real source body. Resolve vtable/linker extent and use before accepting the placeholder or an unreachable replacement. See state/object inline register. |
| open | `player_logic_base_state::{is_weapon_visible,is_smoothing_needed,is_physics_transform_allowed}` | `game_core/player_logic_base_state.h` | #571 | Direct getters are field-ownership models, not proven retail expansions; find original consumers/assertions without inventing reachability. |
| open | `inventory_item::{serialize_game_world_object_header,deserialize_game_world_object}` | `game_core/inventory_item.h` | #571 | Raw PE resolves extent: copies/call/stack cleanup are followed immediately by cc padding, not a returning epilogue. Follow direct value-copy helpers with shipping unreachable form; verify original macro spelling and MSVC codegen in deferred build. |
| open | `weapon_core::{get_skeleton,user_animations}` | `game_core/weapon_core.h` | #571 | Ownership-based by-value skeleton and selector-container getter models; verify original consumers before treating their exact bodies/assertions as recovered. See weapon-core inline register. |
| open | `weapon_core` restored inline caller boundaries | `game_core/sources/weapon_core.cpp` | #571 | Verify ammo total, activity/ready/chamber predicates, queue index getter/setter, private recoil clamp, animation wrapper and selector sprint setter in the deferred build. Preserve retail bool reader and existing file-local recoil data. |
| open | `weapon` public-accessor propagation | `game/sources/weapon.cpp` | #571 | Removed invented friend; existing PDB interface now owns private-base reads. Verify u16 ammo totals and by-value matrix expansions, not old claims that their differences are non-steerable. Preserve derived shadowing fields and protected user reference binds. |
| open | `weapon_ammunition::activate` assert expression | `game_core/weapon_ammunition.h` | #571 | Target supports the current dummy-use/reference-eater sequence but not the original assert predicate, which remains UNKNOWN_EXPRESSION. Need original source or a debug consumer before claiming exact source recovery. |
| open | `empty_hands::deserialize` one-byte target span | `game/sources/empty_hands.h` | #571 | Target record spans one int3 at RVA 0x112e20; resolve actual entry/extent and vtable consumers before accepting an empty callable body or claiming an ICF/linker wall. |
| open | `weapon_ammunition::{is_ready_to_be_deactivated,is_sprinting}` unreachable source form | `game_core/weapon_ammunition.h` | #571 | Retail framed bodies assign no bool result; candidate true/false assignments removed. Shipping UNREACHABLE_CODE mirrors existing transform behavior, but exact macro spelling remains unproven. |
| open | `player_input_handler::input_priority` comparison identity | `game/sources/player_input_handler.h` | #571 | Source priority corrected to retail 10. Two-sided pdb_fetch hid candidate return 0 by choosing `assert_log_times_in_gold` and reporting 100%; base-only fetch exposes the real body. Parser identity-precedence regression remains open; see `pdb_fetch_alias_selection.md`. |
| open | `player::set_near_plane` caller boundary | `game/sources/player.cpp` | #571 | Private forwarding helper restored inside the factor wrapper's existing controller guard; verify the original guard boundary and retain the target's single-statement caller partition in the deferred build. |
| open | `circular_buffer::{resize,insert,is_valid}` | `game_core/circular_buffer.h` | inline follow-up to #569 | Recover preservation/rebasing and insertion/eviction rules, and distinguish allocation membership from live-ring membership. See the per-function circular-buffer register; no consumer establishes these contracts yet. |
| open | `fsm::{remove_state,remove_transition}` | `ai/fsm.h` | #569 | Models detach externally owned states and free transition records; verify current-state/incoming-edge cleanup and first-versus-all parallel-edge removal. Evidence F01–F02 in `pr569_semantic_reconstruction.md`. |
| open | `fixed_joint_camera_effector::{process_camera,on_attach}` | `animation/i_editor_mixer.h` | #569 | Verify joint-to-camera coordinate conventions and attachment side effects against an editor consumer; current direct-matrix/empty-hook bodies are models. |
| open | `animation_states_dumper` defaults | `animation/animation_states_dumper.h` | #569 | Verify initial timestamp and default start/finish hooks. No concrete dumper consumer has been found. |
| open | `n_ary_tree_null_weight_detector` visitors | `animation/sources/mixing_n_ary_tree_null_weight_detector.h` | #569 | Conservative structural-zero model, not a proven retail predicate. Verify transitions, cancellation and compound-node rules against a consuming target. |
| open | `animation_player::serialize_state_impl` caller boundary | `animation/sources/animation_player.cpp` | #569 | Copy region extracted to the const-tree helper; event adjustment/inversion remain outside. Validate the retained serializer's eight target statements in the deferred batch build. |
| open | `n_ary_tree::{update_weight,update_animation_interval_time}` caller boundaries | `animation/sources/mixing_n_ary_tree.cpp` | #569 | Retained weight/interval logic extracted and propagated. Validate the caller's thirteen target statements; the weight model ignores start time. |
| open | `collision_shape_cook::load_collision_resources` caller boundary | `physics/sources/collision_shape_cook.cpp` | #569 | Five-request region extracted using `cook_data::parent_query`; validate the 24-statement caller after the deferred build. |
| open | `bullet_character_controller::get_contacts_count` | `physics/sources/bullet_character_controller.cpp` | #569 | Model sums cached manifold contact counts; establish whether retail instead counted only penetrating contacts. |
| open | `bt_ghost_object::non_compound_shapes_count` | `physics/sources/ghost_object.cpp` | #569 | Model reuses centres traversal for the same leaf definition; verify whether original code counted directly without temporary allocation. |
| open | `engine::world` editor forwarders | `render/engine/sources/render_engine_world_pc_dx11.cpp` | #569 | Recover four lower renderer callees and verify null guards. World wrappers alone do not recover picking/probe/AO behavior. |
| open | `scene_renderer::set_fixed_lod` | `render/facade/sources/scene_renderer.h` | #569 | Volatile retail field and reader establish storage, not interlocked-versus-plain write. Verify against an editor consumer. |
| done | `VertexShaderDesc::GetShaderIndex` / `FragShaderDesc::GetShaderIndex` | `scaleform/sources/d3d1x_shaderdescs.cpp` | scaleform-to-100 | DONE: ordered the recovered 519-case mappings by ascending ShaderType value, matching every observable target PDB source-line anchor. Both three-version switches are byte-exact with matching structure through their real callers. |
| done | `intrusive_list` DebugPolicy default (MASTER_GOLD) | `vostok/intrusive_list.h:21` | match/ai-roots | DONE: the default is now `no_debug_policy` globally, matching the target's MASTER_GOLD instantiations. The earlier explicit AI typedef spellings remain equivalent. |
| done | `weapon_core_state_cook_template::on_subresources_ready` access (public QAE vs target private AAE) | `vostok/game_core/weapon_core_state_cook_template.h` | match/game-core-cooks-2 | DONE: moved the callback declaration into the private section, producing the target's `@@AAE...` access mangling. The remaining body delta is the independently tracked /Od statement-folding wall. |
| done | `resources_manager::find_cook` is STATIC-over-static-registry in target (caps the cook_base find_* family) | `core/sources/resources_manager_cook.cpp` + `resources_manager.h` | chore/core-inline-divergence-probe | DONE: `find_cook`/`register_cook`/`unregister_cook` now operate on file-static `s_cooks_registry`; `find_cook` and `register_cook` match exactly. The remaining `unregister_cook` register-allocation residual is tracked separately. |
| investigate | ai_world/search/search_base have competing PDB layout variants | `ai/sources/ai_world.h` (STATIC_SIZE_ASSERT 0x688), `ai/search/search.h` (0xA0), `ai/search/search_base.h` (0x3C) | match/ai-world-layout | The base structure dump selected an `ai_world` record with `timing::timer m_npc_lives_timer @0x98` and sizes 0x698/search 0x9C/search_base 0x38, while the current source has no such member and its build-gated asserts compile at 0x688/0xA0/0x3C. The inspected constructors/accessors use the latter offsets (`m_search_service@0x98`, `m_engine@0x138`, `m_npc_characters@0x13C`). This binds those consumers to one shape but does not prove the other same-named PDB record is a phantom. Re-audit the complete target/base variant sets with `pdb_topology --classes --class ...`, then bind the disagreeing type indexes to compilands/consumers before closing. |
| done | `math::convex` field types (fixed_vector vs std::vector) | `vostok/math_convex.h` (`m_planes`/`m_adjacencies`) | match/core-math-convex | DONE: rewrote `convex` to the target 0x18 layout from `binaries/structure/target/headers/vostok/math/convex.h` — `m_planes` `std::vector<aabb_plane>`@0x0, `m_adjacencies` `std::vector<fixed_vector<u8,8>>`@0xc (renamed from `m_adjacency`, retyped from the old bitmask `fixed_vector<u32,64>`); `STATIC_SIZE_ASSERT(convex,0x18)`. Member sigs now match target: ctor takes the two vectors by value, getters return by value, added `sort_adjacencies()`; static helpers retyped to `get_plane_flags(adjacencies_type)->std::vector<u8>` / `build_original_to_new_adjacency_dict(std::vector<u8>, buffer_vector<u8>&)` / `convert_original_adjacency_to_new(buffer_vector<u8> const&, vertex_adjacency&)`. Reworked all math_convex.cpp bodies from the bitmask model to the plane-index-list model (each adjacency entry is now a `fixed_vector<u8,8>` of plane IDs, not a u32 mask); dropped the dead `get_lowerst_set_bit_index`/`bits_set_count` bitmask helpers. `test_inexact` STAYS 100% (STRUCTURE MATCH, 7 stmts/0x5c — confirms target really uses std::vector @0/+4) and collision `convex_geometry` ctor + methods stay 100%. The inlined-only bodies (split/fill_cache/ctors/getters/helpers) are reconstructed faithfully to the std::vector API but unmeasurable (not standalone in target PDB). |
| done | `core::engine` vtable (missing get_user_data_directory + 3 trailing virtuals) | `core/engine.h` (before `create_physical_path`) | match/core-engine-vtable | DONE: added `get_user_data_directory()=0` before `create_physical_path`, plus trailing `use_resources_manager()/use_video_memory()` (both `{return true}`) and `on_crash() {}` — exact target vtable order/sigs from `binaries/structure/target/headers/vostok/core/engine.h`. Concrete subclass overrides: `engine_world` got the real `get_user_data_directory` (engine_world_win.cpp, SHGetFolderPathA/CSIDL_PERSONAL + _strlwr_s + append_relative_path<native,native>, paired 48.78%) and an empty `on_crash` (engine_world.cpp, byte-identical `ret`); non-built tool engines simple_engine/maya_engine/test_animations::application got minimal NULL/delegate overrides for source hygiene. Net build POSITIVE: core 79.6→80.4% fuzzy / +4 exact, engine 84.4→84.8%, fs 79.3→84.0% / +3 exact, overall 56.05→56.26% / +12 exact. |
| open | `engine_world::get_user_data_directory` inline-vs-call residual | `engine/sources/engine_world_win.cpp` (`get_user_data_directory`) | match/core-engine-vtable | Structure matches the target (8 stmts, same shape + the one `char[260] path` local); byte residual (48.78%) is inline-vs-call — the target out-lines `append_relative_path<native_path_string,native_path_string>` to a real ~0x1f-byte call while our base inlines its whole loop (~0x67). Non-steerable from the caller; recoverable via the orchestrator-only noinline hack if it ever caps a cluster, else lifts as the helper out-lines. |
| open | `engine_user::world` vtable diverges from target (caps `engine_world::load_level` + `on_fullscreen_alttab`) | `vostok/engine/engine_user.h:58` (`struct world`) | match/engine-target-only | Target `engine_user::world` (from `binaries/structure/target/headers/vostok/engine_user/world.h`) has, in order: enable / tick / clear_resources / `load(pcstr)` (ONE arg, not our `load(pcstr,pcstr=NULL)`) / `is_loading() const` (NEW slot 4) / unload / on_application_activate / on_application_deactivate / `on_fullscreen_alttab(bool)` (NEW slot 8 @ vtable +0x20) / ui_world / input_world / dtor. Our header is missing `is_loading` + `on_fullscreen_alttab` and has the 2-arg `load`. The fix unblocks engine_world::`load_level` (target forwards `m_engine_user_world->load(name)` via the slot-3 vcall, then apc-schedules on the logic thread) and engine_world::`on_fullscreen_alttab` (forwards `m_engine_user_world->on_fullscreen_alttab(value)` = slot-8 vcall, reached via the editor::engine subobject so `this+0x288`=`base+0x290`=m_engine_user_world; also needs the matching `on_fullscreen_alttab(bool)=0` added to `editor/world/engine.h` after on_application_deactivate). NOT done here: the game module (`game/sources/game.h`, `game_generate_shaders.h`) already carries workaround declarations (`is_loading`, 1-arg `load`, `on_fullscreen_alttab`, PLUS a `load(pcstr,pcstr) override` placeholder) — changing the interface to 1-arg `load` makes that placeholder fail to override and would diverge game's vtable, so this is a coordinated engine+game change for a worker who owns both. Until that migration makes `survarium::game` concrete, `game_module_proxy::create_world` and `destroy_world` remain collapsed through their stubbed game-module callees and must be remeasured afterward. |
| open | `engine_world::initialize_render` / `create_render` / `on_shader_masks_ready` are render-coupled | `engine/sources/engine_world.cpp` (`initialize_render`), `engine_world_win.cpp` (`create_render`), `engine_world_initialize.cpp` (`on_shader_masks_ready`) | match/engine-target-only | Target sigs take `(resource_ptr<binary_config> const&, bool)` (our base is the divergent no-arg versions). `create_render` tail-constructs `vostok::render::world::world(...)` (a RENDER symbol) and writes the render `g_allocator` globals; `on_shader_masks_ready` is a resources-query callback that stores the loaded mask config into `m_shader_mask_config` (now present in the layout). PARK per render-last policy — match after the render `world::world` ctor exists and the shader-mask query is wired in `initialize`. |
| done | `engine` scaleform shims + log output | `engine/sources/engine_scaleform_initialize.cpp` (`scaleform_engine_alloc`/`_free`/`scaleform_log_output`, `initialize_scaleform`/`destroy_scaleform`) | match/engine-target-only | DONE: wired the engine owner to `survarium::scaleform_engine::initialize/destroy` and the staged pristine GFx suite. The allocator/free shims, initialize/destroy, `scaleform_engine::initialize`, and caller `initialize_core` are byte-exact. `scaleform_log_output` now has the target `fixed_string2048::append` + by-value logging shape and reaches 95.46%; its remaining SIZE residual is compiler cleanup-state/register allocation (target keeps the branch state in `ebx`, this build spills it), with identical control flow and callees. Removed the obsolete initialize/destroy reachability-anchor calls so LTCG can specialize the real owner path. |
| done | `tips_sorting_predicate::operator()` needs divergent console_impl sort site | `engine/sources/console_impl.cpp` (`on_text_changed` + `tips_sorting_predicate`) | match/engine-target-only | DONE: restored the three-statement `strstr` comparator and the `std::sort(m_tips...)` caller. The comparator is byte-exact and `on_text_changed` now has the target's 25-row structure; its remaining SIZE residual is the independently verified `console_commands::find` inline-vs-call compiler-context wall. |
| open | `behaviour_cook` / `brain_unit_cook` ctors | `ai/sources/behaviour_cook.cpp` + `brain_unit_cook.cpp` (ctor) | match/ai-cooks-sensor | IDENTIFIED from the retail PDB record (structure-divergence pass): the empty entity at `this+0x20` is a `boost::noncopyable` base (EBO-folded with m_ai_world); the retail member is also `ai_world&`, not `ai_world*`. Both applied (base added, member switched to reference, ctor-init/deref sites adjusted). Remaining: rebuild and verify the ctor byte residual closes; then flip to done. |
| open | `game_world` legacy `network::client` wiring | `game/sources/game_world.h` + `game_world.cpp` | - | Disabled (commented, server-precedent style) during the network carcass rebuild: the legacy client/server/packet web was parked in `temp/network_legacy/` (since deleted; `git show 885e1d4a4:temp/network_legacy/<file>`) and is no longer compiled. Redo the game-side wiring against the canonical `network::login_client`/`match_client`/`tcp_packet_client` when the game module gets matched. |
| open | `game_test_suite` carries a base-only `m_rtp_world` (target has none) | `game_test_suite.h` (`m_rtp_world` + `set_rtp_world`/`rtp_world`); consumers `rtp/sources/testing.cpp:235` + `rtp_learn/sources/rtp_learn_application.cpp:194` | match/member-levers | pdb_divergence `[member]` one-sided field: the shipped v0.100b `game_test_suite` is a bare `suite_base<game_test_suite>` (size 0x30, target structure header `survarium/game_test_suite.h` shows only ctor/dtor, no data members), but our source adds a `rtp::world* m_rtp_world` (size 0x34) with `set_rtp_world`/`rtp_world` accessors. Removing the member to match the target BREAKS the build: the `rtp` lib (`rtp.vcproj`, in the ninja graph via `subninja rtp.ninja`) compiles `rtp/sources/testing.cpp` whose `rtp_tester::test` calls `suite->rtp_world()`, and `rtp_learn_application.cpp` calls `set_rtp_world` — both fail with C2039 `'rtp_world' : is not a member`. So the faithful removal needs a coordinated rtp-tool rewire (pass `rtp::world` explicitly, or `#ifdef` the member out of the shipped build) before the member can be dropped. Reverted here (build-gated); not a clean single-header member fix. |
| open | `LOGIFD_*` macro prefix + missing forced variant | `vostok/logging/macros.h:72-75` | match/core-thread-pool-log | The reconstructed `LOGIFD_ERROR/WARNING/INFO` macros prepended `VOSTOK_LOG_MODULE_INITIATOR ":"` to the initiator, but the ship target uses the initiator alone: tasks `thread_pool::log`/`log_columns_header` expect `"tasks:"` (not `"core:tasks:"`) and vfs `db_callback` expects `"pack:"` (not `"vfs:pack:"`). Dropped the module prefix from the three `LOGIFD_*` macros (faithful for both core+vfs, no regressions). Also added `LOGIFD_FORCED(initiator, type, format, data, ...)` (→ `__LOG_FORCED`, no `__CHECK_VERBOSITY`) because the tasks log methods emit unconditionally (no `g_log_filter_tree` check in the target) whereas vfs `db_callback` KEEPS the check — so the macro name the tasks source used was a forced LOGIFD variant, reconstructed here. Confirm the exact ship macro names/spelling if the original logging headers surface. |
| open | `format_specifier::fill_specifier_list` strings::copy<512> inline-depth wall | `logging/sources/format_specifier.cpp` (`fill_specifier_list`) + `vostok/strings_functions_inline.h:42` | match/logging-format-specifier | Structure + 3 locals MATCH (12/12 stmts, same lines); 88.3% byte residual is an /Od inline-depth wall on the `strings::copy(char(&)[512], pcstr)` template overload (`copy<512>`). TARGET emits a single `call strings::copy<512>` (its COMDAT inlined the inner 3-arg `copy` down to a bare `strcpy_s`, leaving a 0x12 frameless leaf left out-of-line at the site); BASE inlines the wrapper, leaving `mov ecx,200h` + `call copy` per site (3 SIZE+ rows, +8B frame `sub esp,430h` vs `428h`). The sibling `strings::append<512>` pairs +0x0 in both because its inner 3-arg `append` carries an `R_ASSERT_U` keeping the wrapper un-inlinable — confirms the cut hinges on helper body size, not source shape. Not steerable from the call site without touching the shared `strings::copy` template (off-limits). See patterns/od-helper-inline-depth-wall.md. Lifts if the shared template's inline threshold is settled engine-wide. |
| done | `tasks::thread_pool::log` file line-layout (`__LINE__`) | `core/sources/tasks_thread_pool_utils.cpp` (log_columns_header/log) | match/core-thread-pool-log | Re-audited against the current build: both bodies have matching size, statement structure, locals, and CFG; only the invocation-line immediate differs. Parked as source-layout metadata and removed the stale source TODO rather than padding the file. |
| open | vfs args ctors gained a `file_handles` parameter | `vfs/pack_archive.h:50` (`pack_archive_args` ctor), `vfs/saving_args.h` (`save_archive_args` ctor) | match/vfs-layout | The target `pack_archive_args`/`patch_args`/`save_archive_args` ctors take an extra `buffer_vector<std::pair<mount_root_node_base<1>*,std::pair<void**,void**>>> const*` arg (2nd for pack/patch, recorded as `arg_1`), and the target's `test_patch.cpp`/`pack_archive.cpp` source presumably pass it. We kept the existing 3-/many-arg ctors and default-init `file_handles=NULL`, because those consumers are NOT in the shipped exe (only `mount_callback`/`db_callback` from pack_archive_utils.cpp are) so the ctor-signature change is unverifiable and changing it would force blind rewrites of the 3-arg call sites in test_patch.cpp. The LAYOUT (file_handles + the reordered/expanded fat_node_info) is byte-matched to the target and locked with STATIC_SIZE_ASSERTs (fat_node_info 0x58, pack_archive_args 0x498, patch_args 0x6C0, save_archive_args 0x70). Restore the real ctor signatures when the vfs_pack tool / newer pack-save source surfaces. |
| open | `delayed_packets_predicate` should derive from `boost::noncopyable` | `network_core/sources/udp_network_flow_emulator.cpp:16` | match/member-levers-2 | The target structure (`others/delayed_packets_predicate.h`) derives `delayed_packets_predicate : public boost::noncopyable` (and the `[member]` divergence flags the missing base + the `m_delayed_packets_to_appear`→`m_packets` member rename). The rename WAS applied (faithful, build-safe). The noncopyable base could NOT: this predicate is passed by value to `std::remove_if`, which copy-constructs it, so the base trips `C2248` (private copy ctor) at the call site. The target apparently keeps a usable copy ctor despite the base (its `[fn-order]` only-tgt lists a generated copy ctor + operator= + dtor). Restore the base once the remove_if call is reworked to not copy the predicate (or the target's exact copy-keeping idiom is identified). |
| investigate | network_core sequence/match-client rows have competing PDB variants | `network_core/udp_match_connection.h`, `udp_match_client.h`, `udp_match_client_session.h`; `network/sources/match_client_impl.h` | match/member-levers-2 | The one-record `pdb_divergence` view flags `sequence_number<u16>`→`<u8>` on `udp_match_connection` and related predicates/hooks, `udp_match_client` 0xB28→0xB20, and `match_client_impl` `boost::array<...,8192>`→2048. The target type stream also contains different same-named records. Consumer evidence supports the current source on the inspected paths: `udp_match_connection::update_acknowledgements` uses 16-bit stores at two-byte-spaced offsets, and the inspected `match_client_impl` constructor builds an 8192-element arena. That does not establish that every disagreeing retail record is stale or unused. Keep these rows open as variant/provenance questions; capture the fresh target/base variant sets and bind their type indexes to compilands or emitted consumers before changing source or parking them. See `patterns/divergence-phantom-duplicate-type-record.md`. |
| open | `s_draw_snd_stats` / `s_show_profiler_command` cc_bool statics | `game/sources/game.cpp` | game-stubs-game-r4 | Two file-scope console-command cc_bool statics left PARKED: the command-NAME string and its command_type are not recoverable from the dynamic-init asm (the name string lives in the .data section, not in the init function). Defining them with a guessed name would fabricate data-section bytes. Recover the data-section strings (their addresses are in the init's `mov [m_name], <string>` — not emitted for these two) to define them faithfully. |
| open | game.cpp cc_bool/cc_u32/cc_delegate dynamic initializers | `game/sources/game.cpp` | game-stubs-game-r4 | The compiler-generated `dynamic initializer`/`atexit destructor` thunks for the file-scope console-command statics (s_draw_stats, s_max_particles, s_particle_lod, cfg_save_*_cc) stay UNPAIRED in objdiff (same delinker limitation as legs_ik_processor's `s_ik_*_cc` row). The statics ARE faithfully declared (legacy-confirmed names/values/command_types). Separately: the target cc_bool init writes 3 member bytes (one 0, two 1s) vs our base's 1 byte — a cross-module difference in `console_command`/`cc_bool` ctor codegen, not steerable from game.cpp. |
| open | `game::update_stats` sound-debug tail (target lines 845-862) | `game/sources/game.cpp` (`update_stats`) | game-cpp-stubs | SOUND SIDE RESOLVED: `sound_debug_stats.cpp` is included in Master Gold and its ctor/statistic/overall-draw path is implemented and linked. The tail remains PARKED only because `s_draw_snd_stats_value` and its backing `cc_bool` cannot be defined faithfully until the target command-name string and command type are recovered from the data section. Restore the tail together with that static. |
| open | `game::tick` game_options/chat_handler tick blocks (target lines 719-723) | `game/sources/game.cpp` (`tick`) | game-cpp-stubs | The `if ( m_game_options.is_active() ) m_game_options.tick(...)` and `if ( m_chat_handler->is_active() ) m_chat_handler->tick(...)` blocks (4 statements) are correctly written but DCE-collapse: in our build `game_options::tick`/`chat_handler::tick` compile to empty `ret` (their `m_*_ui->movie->Advance(...)` GFx callees are inline-empty stubs), so the side-effect-free `is_active()` guard + empty call get eliminated (body-stubs-bottom-up cascade). Reappear once game_options/chat_handler tick (and their GFx Advance callees) are bodied. Also: `update_stats` line 828 calls a game vtbl[9] (`ui_world()` resolves to vtbl[7] in our layout) — an engine_user::world vtable-layout offset, not steerable from game.cpp. |
| done | `damage_model::dump_stats` | `game_core/sources/damage_model.cpp` | #282 / #571 | Restore PDB body_part_parameters const* local and traverse public next directly. Retail 0x6ef920 statement 5 is that offset-zero load. No shared container overload or PDB imprecision needed; new candidate byte effects await the deferred build. |
| open | `particle::is_playing` | `particle/sources/particle_system_instance_impl.cpp` | game-stub-weapon | Reconstructed as a callee of `weapon::~weapon`: `static_cast<particle_system_instance_impl*>(instance.c_ptr())` then `return <dword at +0x2A4> != 0`. `m_next` is a placeholder so it LINKS (unblocks the dtor pairing); the real `+0x2A4` field is unverified because this header has no offset annotations. Identify it to byte-match `is_playing` itself. |
| not done | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` | #155 | Function is **not matched-done**: `STATE[None]` with no STRUCTURE DIFF — objdiff can't pair it (the LTCG inline-vs-call of the `animation_lexeme_parameters` setters shortens the body past the pairing threshold). Needs structure recovery before it earns a real % / embed. |
| open | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` (`.bones_mask( 2 )`) | #155 | Is `2` a bare magic constant or a named value defined somewhere? |
| open | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` (the `bool dummy` dead store) | #155 | Unlikely a `bool dummy` — if it were, `dummy` would appear in the locals (it does not). The lone 4-byte dead store needs further matching to recover the compiled-out structure (would let objdiff pair it: currently `STATE[None]`). |
| done | `legs_ik_processor::process` | `game_core/sources/legs_ik_processor.cpp` (the `process()` STRUCTURE DIFF, `ONLY base` rows) | #159 | RESOLVED + SHARPENED (pr282 sweep): the 3 ONLY-base branch rows ARE an aligner artifact (hand-pairing the new structure-diff aligns every branch stmt 1:1 with matching sizes), BUT the old verdict hid ONE real quantity diff: the final `else if ( left.is_on_ground( ) ) right.is_on_ground( );` is ONE target statement (0xbd = 0x64 test + 0x59 call, condition+body on one source line) vs two in base - fixed by merging the line. Also: every `get_skeleton( )` site carried a +0xc accessor-call temp the target lacks; replaced with direct protected `m_skeleton` access. |
| investigate | `s_ik_*_cc` dynamic initializers | `game_core/sources/legs_ik_processor.cpp` | #159 | Four compiler-generated dynamic-initializer (+ atexit destructor) thunks marked `STATE[STUB]`. Unscored (`None`, name-pairing artifact). Try to match them so a mis-written static init would surface. |
| done | `weapon_core_reload_state_base::initialize` | `game_core/sources/weapon_core_reload_state_base.cpp` | #167 | RESOLVED (reload_states-2): the "structure is wrong" was real — the target's TWO line records over the condition (`!deserializing()` @29, `chamber_a_round_on_reload() && round_is_chambered()` @31) are NESTED ifs, not a flat `&&` chain (per patterns/nested-if-vs-multiline-and.md: per-leg records = nested ifs, byte-identical when there is no else). Rewrote as `if(!deserializing()){ if(chamber && round) unload(); }` → STRUCTURE MATCH (4/4 stmts, 0x61 bytes both, byte-identical). `round_is_chambered` IS out-of-line both sides. Sole residual is 99.83%: `round_is_chambered`'s `this` load is `mov eax,[edx+128h]` (target) vs `mov ecx` (base) — the same /Od scratch-register choice as the double_barreled ctor's `get_magazine_capacity` assert; not source-steerable. |
| sushi-confirm | `udp_network_flow_emulator::~dtor` | `network_core/sources/udp_network_flow_emulator.cpp` | #269 | **Worker DISPUTES sushi's "structure is wrong" — left OPEN for sushi's sign-off before closing.** Worker's case (structure CORRECT, not wrong): structure-diff is 4/4 aligned, 0 quantity-diffs, 2 SIZE-diffs. Tried `!empty()`, `!(begin()==end())` and swapped operands - ALL fold to the identical base body (raw-pointer iterators -> builtin `cmp`/`je`), so it is NOT a different loop form. The real residual: TARGET resolves the iterator `!=` through a bool-returning operator (`cmp begin,[this+4]; sete; movzx; test; jne`, end read inline, no temp) while BASE inlines raw-pointer `!=` (begin+end cached to stack temps, direct `je`). Same source, divergent STLport iterator-compare lowering - env/config, unsteerable from C++. 58.46%. |
| done | `udp_network_flow_emulator::make_packet_lost` | `network_core/sources/udp_network_flow_emulator.cpp` | #269 | RESOLVED (100% structurally VERIFIED): structure-diff is 1/1 aligned, 0 size-diffs, 0 quantity-diffs; target and base are byte-identical. The `mov [ebp-1],al` IS the `is_low_level_packet` bool eater; `endpoint` ref-param needs no eater (`ret 0Ch` cleans all 3 args). The 100% is real. |
| coverage-gap | `delayed_packets_predicate` (class + ctor) | `network_core/sources/udp_network_flow_emulator.cpp:16` | #269 | sushi: size verifier **missed this class** (and its ctor) - it swept only headers, so .cpp-local classes are skipped. FIXED: added `STATIC_SIZE_ASSERT(.., 0x8)`. Confirmed it's the ONLY .cpp-defined class in network_core (no others). ctor/operator() emit no standalone symbol until tick's remove_if instantiates them. |
| investigate | `get_ip_address` | `network_core/sources/network_core_entry_point.cpp:49` | #269 (network_core carcass) | 99.01%, residual is a **DEAD `jmp short` at 0x216** the target emits as the if's closing-brace (L148) breakpoint anchor (label resolution: if-false target is .7=0x218, loop-exit .8=0x21d, 0x216 preceded by unconditional `0x211 jmp .11` -> unreachable; sushi confirmed). The "closing `}` carries a breakpoint" artifact - MSVC emits a no-op jmp so `}` is breakpointable; our build omits it. The 2 bytes shift every later offset = whole residual. NOT ICF naming (earlier verdict wrong), NOT brace/whitespace (confirmed cosmetic). **Open:** what source/flag makes MSVC emit the dead closing-brace anchor here - the general "loop closing-brace breakpoint" lever, worth solving once (recurs across the codebase). |
| done | `custom_alloc_handler<H>::operator()` | `network_core/custom_alloc_handler.h` | #269 (network_core carcass) | RESOLVED: the "6 out-of-line operator() symbols" claim is **false**. Exhaustive scan of `binaries/rich/{target,base}/index.jsonl` for mangled `??$?R...`/`??R...` whose leftmost class token is `?$custom_alloc_handler@` returns **0** on BOTH sides — `operator()` always inlines into the asio `read_op`/`write_op` that invokes the handler (those op symbols carry the body and are matched). The generator omitted no carcass because there is no out-of-line operator() to dump. Keeping `operator()` `inline` is correct; an out-of-line form would emit symbols the target lacks and HURT the score. Only emitted custom_alloc_handler member is the ctor (size 25, identical both sides). No action; comment in source updated to record this. |
| open | `debug_log_callback` | `core/sources/logging_extensions.cpp` (the two append arms) | #286 | sushi flagged the 10-line hand-expanded `logging::append` calls as wrong (target line table: arms at 196..198 / 200, only two lines apart, first arm starts ON the if line per the merged 0xa4 record + `push 0C4h` __LINE__). Reformatted to `__LOG_FORCED(level, format, data, initiator, ...)` - the one macro that takes a RUNTIME initiator; its expansion is argument-for-argument the target call. Open: macro vs hand-expanded direct call is byte-undecidable (textually identical expansion); sushi to sign off on the macro spelling. |
| investigate | `fill_log_string` ('/'->':' fixup) | `logging/sources/logger.cpp` (`convert_slashes_to_colons`) | #286 | sushi's inlined-helper hypothesis CONFIRMED: target emits ONE 0x43-byte record on one line AND records no `char*` loop local (while keeping same-named block locals elsewhere, e.g. `log_format::set`'s two `u32 i`) - VC8 attributes inlined bytes to the call-site line and drops inlinee locals. Helper reconstructed as file-static inline `convert_slashes_to_colons`; the original name/home is unknowable in v0.100b (fully inlined, no symbol survives). Investigate other builds via vostok-versions - one may emit it out-of-line and reveal the real name. |
| audit | all `ASSERT_U` / `ASSERT_CMP_U` (27 across 11 files) | `game_core/sources/*.cpp`, `animation/sources/*.cpp` | #164 | **Re-audit: many of our `ASSERT_U(x)` are probably `VOSTOK_UNREFERENCED_PARAMETER(S)(x)`.** Both emit the same `if(identity(false)){ folded_empty(args) }` eater shape; distinguish by whether `x` is an *asserted expression* (ASSERT_U) or the function's own *otherwise-unused parameter* (unreferenced macro). Prime suspect: `ASSERT_U( weight_driving_animation )` recurs in 6 weapon-state `weapon_and_hands_expression` files where that param is used nowhere else. Per-function: pull the target asm, check what the eater pushes, swap mislabeled ones to `VOSTOK_UNREFERENCED_PARAMETER(S)`, rebuild and require `regressed: []`. See assembly_patterns.md (the ASSERT_U + unreferenced-parameter entries). |
| done | `udp_match_items_stats`/`udp_match_stream_stats`/`udp_match_stats` no-source members (`reset`/`operator+=`/`operator/=`/`dump`) | `network_core/udp_match_stats.h:11` | #285/#569 | RESOLVED: the inert bodies are replaced with field-complete accumulation, averaging, reset, and dump semantics. Existing subtraction/comparison operators and server aggregation constrain the arithmetic; exact dump strings remain part of the server-target question below. |
| done | `udp_match_connection` sham inline bodies | `network_core/udp_match_connection.h:110` | #283/#569 | RESOLVED: all ten constant/empty shams now implement the connection state, allocator, queue, time, and pending-operation semantics. Live connection code, member ownership, paired accessors, and the parked ancestor provide the evidence; `disconnect_impl` remains the least-constrained historical seam. |
| open | `udp_match_server`/`udp_match_client_session` exact server-only source shape | `network_core/udp_match_server.h` | #569 | All 40 session/server placeholders now have coherent executable semantics derived from the live connection API, intrusive ownership, the parked network ancestor, and the PDB declarations/layouts. The shipped client has no procedure or inline expansion for this web, so it cannot prove exact statement spelling, stats log strings, timeout constants, or which of the competing server type variants owned `remove_disconnected_clients`. Verify against a dedicated-server binary or original source; do not merge both PDB variants into one fabricated class. |
| open | `udp_match_packet::reallocate` | `network_core/udp_match_packet.h:94` | #288 | Body unrecovered: the out-of-line `packet<udp_match_packet>::reallocate` (0x112e20) is a never-called `int3` stub, and the LTCG `append(pcvoid,u32)` COMDAT's grow path ends in an ICF-folded no-arg call (an eater/abort macro?) that cannot be disambiguated from the emission. `allocated_size` WAS recovered from the same COMDAT (`100h - movzx(header_size())` -> `sizeof( m_buffer ) - header_size( )`); the trivial destructor is recovered as empty, while `helper::call_constructor` has no target symbol or inline site. |
| open | `booby_trap_core::serialize` | `game_core/sources/booby_trap_core.cpp:604` | #300 | `(math::float3 const&)m_transform.c` cast is implausible original style — float4x4 should expose a translation getter (cf. `get_angles_xyz` on the next line); find the real accessor and respell (bytes currently match either way at /Od). |
| open | `weapon_user_animations_selector::serialize`/`::deserialize` | `game_core/sources/weapon_user_animations_selector.cpp:208,239` | #300 | `i = i->next, ++state_id` — comma operator in a for-tail is unseen in dev code so far; verify the original shape (the line table merges the backedge either way at /Od, so a separate `++state_id;` in the body or a different loop spelling may be the true source). |
| investigate | `udp_match_connection::m_last_receive_time_in_ms` (atomic32 member) | `network_core/udp_match_connection.h:202` | #288 | sushi: "Can we catch this in structure?" - the member's volatile/atomic32 nature was inferred from process_incoming_packet's `interlocked_exchange` target (the non-volatile guard overload would fire otherwise). PDB member-type records should carry the volatile qualifier; today's structure/size sweep checks only sizes/offsets. Investigate surfacing member TYPES (at least cv-qualifiers) in the generated structure/size artifacts so atomic members are caught mechanically. |
| open | `resources_manager::unregister_cook` return-statement split | `core/sources/resources_manager_cook.cpp:98` | match/core-find-cook-static | After making the cook registry STATIC (find_cook/register_cook 100%, layout corrected, ~70 byte-exact ripple), unregister_cook caps at 83%: target keeps `result` in `edi` (callee-saved) and emits `return result` (`mov eax, edi`) as its own statement (line 114), our base allocates `result` to `ebx` and folds the move into the epilogue, so the trailing TRGT_ONLY statement never appears and the LOG_WARNING block uses immediate-0 stores vs the target's hoisted `xor ebx,ebx`. Register allocation inside the LOG_WARNING macro expansion; structure otherwise STRUCTURE MATCH. Not steerable from source. |
| investigate | pdb_parser enum extraction gap (IDA sees enums our tools miss) | `binaries/structure/target/headers/vostok/enums/` | #303 | sushi: IDA extracts `lobby_client_message_types_enum`, `query_info_types_enum`, `inventory_events_enum`, `shop_events_enum`, `skills_tree_events_enum`, `faction_id`, `item_moved_to_slot_action_enum` from the PDB; our enums/ dir has only the ones reachable from extracted function SIGNATURES (`lobby_server`/`login_server`/error enums). Hypothesis: pdb_parser walks types referenced by symbols it renders, IDA dumps the whole TPI stream. Fix pdb_parser (external tool) to dump all TPI enums so matchers stop guessing values. Full IDA data preserved in PR #303 review comments (3390347682, 3390352234). |
| done | login_server/login_client message enums contradicted by PDB | `login_server/message_types.h` (login pair) | #303 | The PDB extraction (`headers/vostok/enums/login_server_message_types_enum.h`) says `login_server_message_types_enum` is the SERVER->client set (`servers_connection_info` 0x08 .. `login_server_invalid` 0x1f) - our header's guessed pair (sign_up=0../lobby_info=64..) is wrong in both naming convention (X_message_types = messages X SENDS) and values. RESOLVED in the login-stack match: login_server_message_types_enum now carries the PDB extraction (0x08..0x1f); the client->server triple (0/1/2, pinned by the sign-flow target immediates) moved to login_client_message_types_enum. |
| open | `set_status_ready_for_battle` = 128: real enum/name unknown | `login_server/message_types.h` (anonymous enum) | #303 | Target `cmp 0x80` in `match_client_impl::on_packet_received` is the match server's first packet byte; it is NOT in the PDB's `lobby_server_message_types_enum` (#303 review). Likely member of `vostok::match_server_message_types_enum` (type attested via `survarium::network_packets_orderer<match_client_message_types_enum, match_server_message_types_enum>` template args; enumerators unextracted). Name is a guess pinned to the immediate; recover when the survarium-side orderer gets matched. |
| punt | `assembly_patterns.md` needs restructuring (too many patterns, noisy for agents) | `docs/binary_matching/assembly_patterns.md` | #303 | sushi: agents reading through see a lot of noise; structure it better (group by family: inline-vs-call walls / macro eaters / lowering idioms / LTCG-linker artifacts, plus an index). Punted from #303: a doc-wide reorg conflicts with in-flight PRs appending patterns (e.g. #289) - do it as a standalone PR when the open structure sweeps land. |
| done | `udp_match_connection::m_last_receive_time_in_ms` (atomic32 member) | `network_core/udp_match_connection.h:202` | #288 | RESOLVED: pdb-parser now renders cv-qualifiers (LF_MODIFIER volatile + pointer-attribute volatile) in the generated structure headers. A full-tree sweep against the regenerated target structure confirms the PDB records `volatile long` for `m_last_receive_time_in_ms` (the inference was right) and ALSO for `m_last_send_attempt_time_in_ms` (was plain `long` in ours - fixed to `threading::atomic32_type`). game_core has zero volatile members in the PDB; network_core's only other one (`udp_match_packets_allocator::m_reference_count`) was already atomic32_type. |
| pairing-fixed | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` | #155 | ROOT CAUSE of the `None` found: the `playback_enum` param's enum was named `playing_type_enum` (with `playback_enum` a typedef alias) in `animation/mixing.h`, so the base symbol mangled to `...W4playing_type_enum@mixing...` while the target uses `...W4playback_enum@mixing...` — different names objdiff can't pair. The target NEVER emits `playing_type_enum` (0 occurrences vs 2 `playback_enum@mixing`); two base symbols carried the wrong tag (this fn + the `weapon_core_shotgun_reload_base_substate` ctor). FIX: renamed the enum tag to `playback_enum`, dropped the typedef (byte-neutral; every site already used the alias). Rebuild in flight to confirm the symbols now pair. Any residual after that is the body inline-vs-call class (setters out-of-line in target, inlined by our /GL), out of scope. |
| name-fixed, still None | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` | #155 | The `playback_enum` param's enum tag is now correct: `animation/mixing.h` tags it `playback_enum` (the target's real tag — the enum appears in exactly 2 target mangled names, this fn + the `weapon_core_shotgun_reload_base_substate` ctor, both `W4playback_enum@mixing`, ZERO `playing_type_enum`), making our base mangled name CHARACTER-IDENTICAL to the target's. This tag is **zero-cost**: clean builds of `playback_enum` vs the old `playing_type_enum`+alias on the SAME toolchain show 0 unit-level regressions (the "408 regressed" once blamed on it was June-7→June-8 delinker/tooling drift, not the rename — verified by rebuilding the pre-rename commit: 7952 matched, identical). HOWEVER the symbol STILL does not pair (stays `None`): the blocker is the BODY-level whole-program inline-vs-call of the inline-in-class `animation_lexeme_parameters` setters (target out-of-line, our /GL inlines them), an out-of-scope LTCG class. No zero-cost lever pairs this fn; left `STATE[None]`. See `game_core/get_weapon_lexeme_pair_impl.md`. |
| done | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` (`.bones_mask( 2 )`) | #155 | RESOLVED: `2` == `animation::body_part_hands_only` (= 0x0002 in `animation/type_definitions.h`; synonym `bone_mask_offset_only` also = 2). Chose `body_part_hands_only` to match the sibling offset_lexeme's `body_part_whole_body_but_hands`. Replaced the magic `2`. |
| done | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` (the `bool dummy` dead store) | #155 | RESOLVED: the lone `<0x4>` `mov byte[ebp-5],0` at target 0x72 (NO following lea/call) sits between the last main_lexeme_parameters setter and main_lexeme's construction — an unused `bool` local dead-stored under /Od, NOT an ASSERT (which adds lea+call = `<0xc>`). The reviewer's "dummy not in locals" doubt is consistent with a never-read bool: MSVC's PDB local table can omit a variable whose sole use is a dead store, yet /Od still emits the slot store. `bool dummy = false;` reproduces this single statement's bytes exactly; only the whole-function pairing was blocked (the enum-tag row above), independent of this line. |
| done | `legs_ik_processor::process` | `game_core/sources/legs_ik_processor.cpp` (the `process()` STRUCTURE DIFF, `ONLY base` rows) | #159 | RESOLVED: NOT a structure bug. Pulled the target asm, decoded the full four-branch `s_ik_adjust_hip_position` ladder (`.2`=both legs / `.5`=`+=right_delta` when left is_on_ground / `.8`=`+=left_delta` when right is_on_ground / `.9`=`left.is_on_ground()?right.is_on_ground()`) — base/target control flow is one-to-one. The operand-aware byte diff (`pdb_fetch --view diff`) is 100% `~` register/slot renames with ZERO `<`/`>` (missing/extra) instructions, so the three ONLY-base rows are an LCS aligner mis-pairing of the four textually-identical `process_leg` blocks, not an extra branch. Residual = the m_hip_bone pointer spill (0x4 frame, slot-rename cascade). |
| done | `s_ik_*_cc` dynamic initializers | `game_core/sources/legs_ik_processor.cpp` | #159 | RESOLVED: pulled both base (`??__E...`) and target (`dynamic initializer for...`) init asm. cc_float (foot_capsule) is BYTE-IDENTICAL to target. The three cc_bool inits carry correct arg values (default filter `general`→target ecx=1 for debug_draw; `execution_filter_early`→ecx=0 for rot_axis/adjust_hip); they differ from target only in the register-vs-stack call convention (LTCG: target passes `this` in esi + regs eax=1/ecx=filter, base uses thiscall + stack pushes). No mis-written init. STUB markers promoted to DONE; report.json stays `None` (universal cc name-pairing artifact). |
| paired (supersedes rows 17/30) | `get_weapon_lexeme_pair_impl` | `game_core/sources/weapon_lexeme_pair.cpp` | #282 (structure sweep b12) | The symbol now PAIRS at **61.19%** (current objdiff/delinker; the playback_enum tag fix holds). `STATE[None]` rows 17/30 are stale. Structure-diff: source statements 1:1; the 21-vs-11 record count is the line-table artifact of the setter inline-vs-call wall (embed in source). Marker updated to `STATE[61.19%\|PARTIAL]`. |
| progress (row 28) | `ASSERT_U` audit | `game_core/sources/{weapon_core,pistol_weapon_core,double_barreled_weapon_core}_{idle,aimed_idle}_state.cpp` | #282 (structure sweep b12+b13) | All 6 suspect `ASSERT_U( weight_driving_animation )` sites CONFIRMED as `VOSTOK_UNREFERENCED_PARAMETERS` (target eater pushes the param with NO leading assert_untyped enum dword; fixed, 85.65 -> 87.52 each, ASSERT row closed byte-exact). b13 verified the 4 fire-state weapon_and_hands_expression siblings carry a plain 0x12 non-evaluating ASSERT that already matches - no swap there. Row 28 master audit (other ASSERT_U sites) still open. |
| open | `threading::interlocked_or`/`interlocked_and` out-of-lined in target | `core/sources/resources_query_result.cpp` (`set_flag`/`unset_flag`); root cause in `threading_functions_win_inline.h` | match/core-query-result | The target **calls** `vostok::threading::interlocked_or`/`interlocked_and` (real out-of-line, ~59/~50 call sites engine-wide); our base inlines them to `lock or`/`lock and` because `_InterlockedOr`/`_InterlockedAnd`/`_InterlockedXor` are listed in `#pragma intrinsic(...)` in `threading_functions_win_inline.h` AND given inline `interlocked_or`/`interlocked_and` bodies under NDEBUG. In MSVC8 x86 those three were NOT intrinsics (x64-only / later compiler), so the original likely (a) omitted them from the pragma and (b) had no inline body, falling through to the `VOSTOK_CORE_API` out-of-line declarations in `threading_functions.h` — which currently have NO definition in our tree (a missing/un-reconstructed threading TU). This caps `set_flag`/`unset_flag`/`set_result_iterator`(17%)/`add_referrer`/`increase_quality_to_target`/`translate_query_if_needed`/`update_quality` (the set_flag-bearing ones) plus every other `set_flag`/`unset_flag` consumer across the engine. Engine-wide threading-header + missing-definition change; defer to the core-header audit. Note: `interlocked_increment`/`decrement` (`_InterlockedIncrement`/`_Decrement`, real x86 intrinsics) correctly inline as `lock xadd` in BOTH (observe_resource_destruction=100%), so the fix is scoped to or/and/xor only. |
| open | `resources_query_result.cpp` log category prefix (`core:` vs none) | `core/sources/resources_query_result.cpp:36` (`add_referrer`'s `LOGI_INFO`) + `resources_manager_new_queries.cpp:50,60` | match/core-query-result | Target `add_referrer` filter+initiator strings are both `"resources:manager:"` (no `core:` prefix); our base emits `"core:"`/`"core:resources:manager:"` because `VOSTOK_LOG_MODULE_INITIATOR` is baked `"core"` in `logging/extensions.h` under `VOSTOK_CORE_BUILDING` and `LOGI_INFO("resources:manager",...)` prepends it. The only string-form that reproduces BOTH target strings is `#undef`+`#define VOSTOK_LOG_MODULE_INITIATOR "resources:manager"` near the top + `LOG_INFO(fmt,...)` (no initiator arg). TESTED: this redefine REGRESSED add_referrer (66.24->65.19) and triggered broad ICF fold-rep churn from changing the shared string literal, while the dominant residual (the target inlines the `g_log_callback` boost::function callback ctor that our base out-of-lines) is unaffected. Reverted — speculative (no matched sibling TU confirms the redefine form) and net-harmful. Recover the original initiator handling for the resources subsystem before re-attempting. |
| progress | `sound_scene` portal-graph reconstruction residuals | `sound/sources/sound_scene.cpp` + `sound_scene.h` | match/sound-scene | The target portal-graph variant and its 0x270 layout are now present; the current Sound TU emits the matching `sound_scene_5` member record, while the unsuffixed and `_1`..`_4` base headers are stale records from other not-recompiled TUs. This campaign reconstructed the typed proxy factories, proxy freeing, graph-position traversal, graph heuristics, exact 39-statement `get_portal_nearest_point`, and several listener/channel/scene paths; the final review also removed the four target-absent legacy generic proxy factories plus callerless `set_listener_properties(float4x4)` and `calculate_hdr_audio`. Remaining source-shape work is concrete: the ctor is 36/37 statements (93.7%, environment-tree/effect-chain/default-environment differences), `find_path` is 3/4 (87.4%), and `free_sound_instance_proxy` is 2/2 but 67.9% due LOG/delete call expansion. The three free functions (`fill_x3daudio_vector` x2, `closest_point_on_segment`) are emitted but remain target-only/base-only because the delinker represents their names differently; see `patterns/overloaded-free-fn-demangled-name-unpairable.md`. Do not use older duplicate structure records as evidence that the class layout is still the earlier variant. |

## Provisional idiom candidates (NOT definitive — DO NOT promote to `assembly_patterns.md` yet)

Both were drafted from `game_core/get_weapon_lexeme_pair_impl`, which is **unverified** (`STATE[None]`
— it doesn't pair, so the whole-function match can't be confirmed). They differ in confidence:
**#1 is likely right** — its asm reproduces byte-for-byte at that single arg site, which is checkable
locally regardless of the function pairing — while **#2 (the `bool dummy`) is the one questioned in
review** above. Kept here (not in `assembly_patterns.md`) so the future matcher has both in hand;
promote #1 once any paired function exercises it, and resolve #2 with the further matching it needs.

**1. Conditional-pointer arg `val != u32(-1) ? &obj : NULL`**  *(likely right — reproduces byte-for-byte at the site)*
```
push 0                  ; (an unrelated arg pushed first)
mov  edx, [ebp+20h]     ; val (a u32)
sub  edx, 0FFFFFFFFh    ; edx - (-1) == edx + 1
neg  edx                ; CF set iff edx+1 != 0  (i.e. val != -1)
sbb  edx, edx           ; edx = (val != -1) ? 0xFFFFFFFF : 0
lea  eax, [ebp-0E0h]    ; &obj
and  edx, eax           ; (val != -1) ? &obj : NULL
push edx
```
Source: write the ternary `val != u32(-1) ? &obj : NULL` (a pointer-or-NULL select on an unsigned
`!= -1` test) — NOT hand-rolled asm. MSVC /Od lowers `!= u32(-1)` to `sub r,-1; neg; sbb r,r` (mask
= all-ones iff non-equal) and ANDs it with the address. Candidate site: the offset lexeme's
`time_driving_animation` arg = `time_synchronization_group != u32(-1) ? &main_lexeme : NULL`.

**2. Lone 4-byte `mov byte[ebp-N],0` (no lea/call) = unused `bool b = false;`, not an ASSERT**
A structure statement of size `<0x4>` whose only instruction is `mov byte ptr [ebp-N],0` with NO
following `lea eax,[ebp-N]; call <empty_stub>` is a plain unused `bool` local dead-stored under /Od.
DISTINGUISH from a compiled-out `ASSERT` (`<0xc>` = the byte-store PLUS `lea+call`). Writing
`ASSERT(UNKNOWN_EXPRESSION)` for a `<0x4>` slot over-produces the lea+call. The 4-vs-7-byte disp is
just small-vs-large `[ebp-N]` offset (allocation noise). Candidate site: `get_weapon_lexeme_pair_impl`
L40, target `<0x4>` `mov byte[ebp-5],0`. **Caveat:** only valid if the surrounding match is correct
— here it is not yet (see the `not done` row).
| open | get_weapon_lexeme_pair_impl | sources/vostok/game_core/sources/weapon_lexeme_pair.cpp:46 | #307 | lone `mov byte[ebp-5],0` (<0x4>, no PDB local): NOT an unused bool (fabricated `bool dummy` removed); likeliest a materialized bool temp or split-eater record - identify the real origin |
| open | `weapon_core_shotgun_reload_state::weapon_and_hands_expression` | `game_core/sources/weapon_core_shotgun_reload_state.cpp:100` | #369 | Target `--view structure` records ZERO named locals; the byte-100% form fabricated two (`state`/`current`). Flipped to the chained 0-local form (source-faithful, SIZE -0x6 / ~90% — the target materializes 2 stack temps our build elides). Open: recover the exact temp shape via vostok-versions; the structure verdict does not yet compare locals — a follow-up PR adds locals name+count to the tool + worker. Pattern: `current-state-cast-forward-materialized-locals.md`. |
| open | `weapon_core::reset_fire_queue` | `game_core/sources/weapon_core.cpp:805` | #372 | The min's 2nd operand is an INLINE ACCESSOR (proven by experiment: a helper returning `m_ammo_in_magazine + ( m_is_round_chambered != 0 )` lands 99.81% MATCH / 0xbc==0xbc vs 85.8% for the direct expression — the return-temp `[ebp-2]→[ebp-6]` double-store is byte-required). Real name/signature unknown (inline, no standalone symbol), so NOT fabricated — kept as the direct expression. Restore the accessor once identified (a consumer's asm / a standalone symbol / another build). Pattern: `inline-accessor-return-materializes-call-operand.md`. |
| open | `free_fly_camera::build_view_matrix` | `game/sources/free_fly_camera.cpp:145` | game-camera | The z-component of `new_angles_zxy` is `angles_zxy.z * <offset>` where `<offset>` is a float .rdata constant the delinker labels `offset` (value unresolved). `0.75f` is a guess; it is the sole residual byte vs target (rich-index 97.9%). Confirm the real value (vostok-versions / a resolvable .rdata symbol). |
| done | `free_fly_camera::on_activate` | `game/sources/free_fly_camera.cpp:188` | game-camera | RESOLVED: the re-orthogonalisation reconstructed as `m_inverted_view_matrix.j.xyz() = float3(0,1,0); m_inverted_view_matrix.i.xyz() = math::cross_product(j.xyz(), k.xyz())` (verified: cross_product(j=(0,1,0),k) = (k.z,0,-k.x) matches the inlined asm exactly). 38.77% -> 79.75%, STRUCTURE MATCH (3 vs 3 stmts). Residual is non-steerable: base materialises the float3(0,1,0) temp on the stack then copies into j (target writes 3 components direct); the cross row differs only by xmm register-allocation order. |
| open | `free_fly_camera::on_keyboard_action` | `game/sources/free_fly_camera.cpp:46` | game-camera | Structurally faithful (calls `key_binder::get_binded_action`, switch on game_action_id, console execute / toggle_pause). Earlier "depressed % = get_binded_action stub" theory is WRONG: get_binded_action is now bodied yet on_keyboard_action stayed 78%. Real residual is non-steerable optimizer codegen on this /Ox module: (1) the base tail-merges the two identical `console_commands::execute` case bodies (kSERIALIZE/kDESERIALIZE differ only by string literal) into one shared push/call while the target emits each its own call (3 extra target stmts); (2) the target CSEs `get_game_scene().get_game()` into one slot reused for get_binded_action + toggle_pause (base re-loads), shifting register allocation. Both are compiler heuristics, not source shape. |
| open | `lobby_menu::update_level_loading_progress` | `game/sources/lobby_menu_ui.cpp:457` | game-lobby-menu-ui | The level-name localization key `L"st_loading_level"` is a LENGTH-matched guess (16 wide chars => the 4x movq + word the target copies from rdata into `w_text`); content is loaded from rdata and does not affect this function's bytes, but the exact key is unverified. Structure faithful; byte % (~61%) capped by the scaleform flash_value/flash_movie /Od inline wall. |
| open | `lobby_menu::on_match_message_arrived` | `game/sources/lobby_menu_ui.cpp:1630` | game-lobby-menu-ui | The `L"joined"`/`L"in_queue"`/`L"team"` wcsstr prefix literals are length-matched guesses (relocated rdata addresses, do not affect this function's bytes), exact keys unverified. The two `joined`-prefix searches (joined+left blocks) read the same prefix in the target. Structure faithful (3 message blocks, 31 stmts == target); byte % (~50%) capped by the scaleform flash /Od inline wall. |
| done | `lobby_menu::on_keyboard_action` (query_info_types) | `game/sources/lobby_menu_input.cpp:55` | game-misc-states | Resolved: `query_info_types` enumerator set recovered from the target PDB structure dump (`lobby__query_info_types.h`); value 0 = `lobby::q_client_state`. Cast replaced with the named enumerator. |
| done | `lobby_camera::on_mouse_key_action` (mouse_button 0x152) | `game/sources/lobby_camera.cpp:62` | game-stub-lobby-camera | Resolved: the shipped `input::mouse_button` enum carries virtual-key-style codes (0x151..0x158, `mouse_button_count = 8`), recovered from the target PDB structure dump. `input/mouse.h` updated; the cast replaced with `input::mouse_button_right` (0x152). |
| open | `player::apply_input` (float2) look_pitch clamp upper bound | `game/sources/player.cpp:429` | match/game-stubs-2 | The look_pitch is `clamp_r( pitch + rot.y, -1.f, <upper> )` (the target's 3-branch clamp body: if v<=-1 ret -1; if v<=hi ret v; ret hi). Lower bound is the literal -1.f; the upper bound is the delinker's unresolved float-pool symbol `clear_value` (same UNRESOLVED-float-pool-const family as game.cpp's stats_graph defaults). Guessed 1.f. Recover the real float (vostok-versions / a sibling that references the same pool entry) and replace. Also the clamp_r ASSERT(min<=max) emits where the optimized target folds the constant assert — confirm whether the source used clamp_r or a raw inline min/max. |
| open | `weapon::update_dispersion_visual_representation` statics | `game/sources/weapon.cpp:32` | match/game-stubs-5 | Recovered the body (default_vertical_fov is now a real global). Three TU-local statics needed: `s_hide_crosshair_on_aim_value` (bool, gates the crosshair via the `hide_crosshair_on_aim` cc_bool), `s_dispersion_gui_scale_coef_value` (float, scales crosshair size by dispersion), and `cc_float dispersion_magic_coef_cc` registering the latter (its dynamic initializer is `dispersion_magic_coef_cc` in the target — the cc_float ADDRESS-TAKE is REQUIRED: without it MSVC folds the 1.0 default and drops the coef load). cc_float min/max (0/10000) recovered from the initializer asm (`__real@461c4000`). UNKNOWN: the cc_float command-name STRING (guessed "dispersion_magic_coef"), and both static SEED values — all data-section, do not affect this function's bytes. Recover the name string + seeds (vostok-versions / the cc_float dynamic-init rdata). |
| done | `lobby_menu` server-callback enum labels (`query_info_types` / `friendship_actions_enum`) | `game/sources/lobby_menu.cpp` (on_client_status_received / on_friendship_status_recivied / casts in query_lobby_info / query_account_data) | match/game-lobby-menu | Resolved: `query_info_types` (`lobby__query_info_types.h`) and `friendship_actions_enum` (`messaging__friendship_actions_enum.h`) enumerator sets recovered from the target PDB structure dump. Numeric `case N`/`(lobby::query_info_types)N` casts replaced with the named enumerators (q_client_state..q_player_reputations); friendship action names corrected (add_friend/remove_friend/add_ignorable/.../update_friends_status). Codegen-invisible. |
| open | `lobby_menu::lobby_menu` GFx scale-mode slot | `game/sources/lobby_menu.cpp:86` | match/game-lobby-menu | `movie->SetViewScaleMode( flash_movie::SM_ExactFit )` maps to the target GFx slot `[+0xC0](2)`; the flash_movie wrapper around that GFx call is unverified. Confirm the wrapper/enumerator spelling. |
| open | `lobby_menu::on_stats_message_arrived` | `game/sources/lobby_menu_ui.cpp:1741` | match/game-lobby-menu-ui-r2 | The wcsstr prefix literals (`L"player_id"`/`L"player_exp"`/`L"match_count"`/`L"player_count"` + the `L" ="`/`L"="` terminators) are length-matched guesses (relocated rdata, do not affect this function's bytes); exact keys unverified. The `query_client_status(7/8)` enumerator names are now resolved (`q_account_money`/`q_player_skills`, recovered from the target PDB structure dump). Structure faithful (chat_handler now headered, the parse + two add_message + player_count else-block recovered); byte % (~73%) capped by the scaleform flash /Od inline wall (set_games_online Invoke + flash_value ctor/dtor) and LTCG scheduling. |
| open | `lobby_menu_external_handler::callback` | `game/sources/lobby_menu_ui.cpp:188` | match/game-lobby-menu-ui-r2 | The flash external-interface method-name dispatch (15 `strings::equal` branches) is structurally faithful (83 stmts == target). Two unverified/wall items: (1) the first 3 branches (`leave_queue`/`play_button_clicked`/`profile_changed`) INLINE the strcmp in the target while the rest call `vostok::strings::equal` out-of-line — an MSVC inline-budget heuristic we can't steer per-site (inline-vs-call wall); (2) the `args[N].GetUInt()/GetBool()/GetString()` reads inline to the GFx Value union field at `[+8]` under LTCG (scaleform Master Gold /GL) — the GFx-Value-internal layout is not headered, so that inlining + the `start_friend_message` flash_value[2] / `unlock_perks` vectora-build scheduling is the byte residual (~75%). The method-name string literals are matched from the target rdata. |
| open | `lobby_menu::fill_inventory_labels` | `game/sources/lobby_menu_ui.cpp:742` | match/game-lobby-menu-ui-r2 | Walks the file-scope `survarium::lobby_labels` ui_label table (loop count 0x49=73). `lobby_labels` is declared `ui_label* lobby_labels = lobby_labels_data;` to match the target symbol mangling `?lobby_labels@survarium@@3PAUui_label@1@A` (a pointer, not an array); the 73 `{name,label}` pairs are a fabricated placeholder (strings are read at runtime = data only, zero bytes in this function), exact pairs unrecoverable (same data wall as `login_menu::fill_labels`). Structure faithful; byte % (~57%) capped by the scaleform flash /Od inline wall (CreateObject/SetMember/SetStringW + flash_value ctor/dtor). Recover the real 73 pairs from the lobby_labels data table. |
| open | `player::insert` (flash_text_manager::create_text_w out-line) | `game/sources/player.cpp:230` | match/game-player-stubs | The full body is reconstructed (see the claude@NOTE) but cannot link: `flash_text_manager::create_text_w(wchar_t*)` is an INLINE stub in `game/flash_text_manager.h` AND has an out-of-line body in scaleform `movie.obj`, so instantiating it in player.cpp gives LNK2005 (multiply defined). Cure: declare `create_text_w` out-of-line in `flash_text_manager.h` exactly like `flash_text::set_visible`/`set_color` already are (resolve to the single `movie.cpp` definition), then player::insert links + pairs (capped on the GFx DrawText inline + active-object intrusive_ptr wall). Scaleform-glue header change, not a player.cpp concern. |
| open | `text_translator::load_text_localization` | `game/sources/text_translator.cpp:31` | match/game-small-cooks | The line-29 STR_JOINA config path uses placeholder `""` for the middle `char*` arg (the recorded tuples ctor is `<char const*, char*, char const*>`, so the path is `STR_JOINA( requests[0].path, "resources/localization/", <runtime language string>, ... )`). The language variable's source is unknown from the available corpus, so the exact literals/byte image are not recoverable here. Structure matches (2 stmts); byte % (~81%) capped by the unknown middle string. Recover the language string source and replace the `""` placeholders. |
| open | `step_manager::on_step` | `game/sources/step_manager.cpp` | match/game-small-cooks | LTCG custom-convention residual: the target promotes `world`->ecx and `position`->eax (no slots), passes `a` as the only `[ebp+]` stack arg, and dispatches add_decal/play_particle through the bullet_manager_engine sub-vptr (`[world+0xC4]`). The 6-argument `emit_and_play_once` API is now present; the source call uses its three default arguments. Structure faithful (17 stmts, lines 34-67); byte % (~38%) is LTCG/arg-passing + optimized line granularity. The remaining bullet_manager_engine accessor belongs to game_world. |
| done | `resources::queries_result` header is 8 bytes too large (cross-module layout) | `vostok/resources_queries_result.h` | match/game-lobby-scene | DONE: removed the spurious `uid_object<queries_result>` base, added the target's cancellation field, and locked the target size with `STATIC_SIZE_ASSERT(queries_result, 0x50)`. |
| open | `animation_space_graph::max_speed` (m_max_speed mutable vs const_cast) | `game/sources/animation_space_graph.h:66` | match/game-anim-space-2 | `max_speed() const` writes `m_max_speed` (lazy-cache: the asm proves the const method stores to `[this+0x110]`). Declared the member `mutable` to allow the write; `mutable` vs an in-body `const_cast< animation_space_graph* >( this )->m_max_speed` is byte-identical codegen so this is a style guess. Confirm the original spelling (vostok-versions / a sibling lazy-cache idiom). |
| done | `player_input::deserialize` + `player::deserialize` (re-spell post-noinline) | `game_core/sources/player_input.cpp:26` + `game/sources/player.cpp:1523` | match/game-deserialize-noinline-fixup | INVESTIGATED: the original premise (target out-of-lines `r<T>`, so re-spelling aligns) is FALSE for these two. Both TUs are shipped `/Ox` and the target INLINES `r<T>` (`r<math::float2>` has NO standalone target body; in player::deserialize every `r<T>` expands to raw `m_pointer` reads). Our base is `/Od` (game_core/game.ninja carry no `/O` flag) and out-of-lines, now compounded by the `noinline`. So these regressed for the OPPOSITE reason from the +25 winners: their targets inline `r<T>` while the winners out-of-line it. player_input is already STRUCTURE MATCH (3 stmts); manual-inlining the read (tested) drops to 57.7% AND breaks the 3-stmt structure. player::deserialize also diverges on inlined `item_in_slot().c_ptr()` + intrusive_ptr refcount (TRGT_ONLY lock-xadd), a pre-existing `/Ox`-vs-`/Od` gap unrelated to noinline. Residual is a header-owned (`packet_reader.h`, out of these TUs' scope) + `/Ox`-vs-`/Od` codegen wall, non-steerable from these TUs. Faithful source kept; documented in `claude@NOTE`s. Scores: player_input 58.27%, player 26.41% (= post-noinline baseline; comment-only edit). |
| open | `empty_hands::selected_animations` (m_random.random in a const method) | `game/sources/empty_hands.cpp:57` | match/game-small-leftovers-2 | `selected_animations() const` mutates `m_random` (the asm stores the new LCG seed to `[this+0x154]`). Wrapped in `const_cast< empty_hands* >( this )->m_random.random(...)` to compile under the const method; `const_cast` vs a `mutable m_random` is byte-identical codegen so this is a style guess. The header (empty_hands.h) is 100% with `m_random` non-mutable, so the const_cast is the more likely original spelling, but confirm (vostok-versions / a sibling const-method-mutates-rng idiom). |
| done | `human_npc_cook::on_npc_options_received` (L85 sound + L99/L119 temporaries) | `game/sources/human_npc_cook.cpp` | match/game-small-leftovers-2 | DONE: PDB/assembly prove L85/L86 assign `sound_world_user` and `sound_scene` in `brain_unit_cook_params`; the inferred `register_receiver` call was removed. Request construction is complete and the post-setup CFG agrees. The residual PDB local/line difference is caused by target keeping creation-data `c_ptr` and allocator `strip_pointer` out-of-line while base inlines them. |
| done | `human_npc_cook::on_subresources_loaded` (L127/L129 + resource set_* grouping) | `game/sources/human_npc_cook.cpp` | match/game-small-leftovers-2 | DONE: restored the two-step project-config extraction, brain resource at slot 0, target setter order, direct managed-animation copy, typed resource casts, and the two-argument memory-usage construction. Target/base now have the same eight PDB locals and the same line-record count; the residual is the documented resource-pointer cast/RVO and helper-inline boundary. |
| open | `weapon_sound_events_handler_state_cook<T>` resource bodies (create_resource / on_subresources_ready / new_state x6) | `game/sources/weapon_sound_events_handler_state_cook_inline.h` + `_cook_specializations.h` | match/game-weapon-sound-bodies | Cluster-wide PARK (~60 template instantiations, ONE shared cause). The accessor/ctor/link walls are now CLEARED (weapon_core accessor map recovered, emit_hud_sound 100%, handler ctors/init/finalize byte-correct). Remaining: new_state's pinned_ptr time-scale arg is fed by a `cubic_spline_skeleton_animation` keyframe-table duration read that isn't decoded yet; create_resource (90-stmt config parser) + on_subresources_ready (38-stmt installer) need their own structure recovery. Decode the cubic_spline keyframe-duration read first (unblocks new_state), then body create_resource/on_subresources_ready from target asm. The handler initialize/finalize/ctors pair at full % once new_state is bodied (currently objdiff-unpaired ICF fold-rep). |
| open | `n_ary_tree_transition_tree_constructor` anchor-friend + change_animation reachability | `animation/sources/mixing_n_ary_tree_transition_tree_constructor.h` + `anchor_animation_cloner.cpp` | match/anim-nary-transition-tree | TEMPORARY codegen-neutral `friend void ::vostok::anchor_animation_cloner()` added so the anchor can call the private `change_animation()` root, pinning the constructor's real call graph (new_animation -> add_operands -> the new_*_transition node leaves) out-of-line. The leaves are reached through their TRUE callers (add_operands/change_animation), not enumerated in the anchor. Drop the friend + the anchor's change_animation() call once the real mixer::set_target call graph reaches change_animation. change_animation itself is bodied minimally (PARKED at ~3%, STATE[STUB]) - reconstruct its 71-stmt multiplication-tree build (asm 0x6daeb0, lines 1244-1404) to structure-match. |
| open | `base_player::get_animation_playback_state` (reconstructed forwarding helper name) | `game_core/sources/base_player.cpp:131` | match/game-core-base-player | The base default forwards `(object, mask, result-by-value)` to a 4-arg cdecl free fn that COMDAT-folded to a bare `ret` whole-program, so the real callee NAME is unrecoverable. Reconstructed as `survarium::query_animation_playback_state( pcvoid const, u32 const, animation_playback_state )` (empty body, re-folds to the same `ret`). The forwarding `call rel32` is byte-identical regardless of the helper's name; the function pairs at ~17% with correct structure, residual is the `/Od`-vs-`/O1` frame wall (game_core base compiles `/Od`, target Master Gold `/O1`). Identify the true callee (vostok-versions / a sibling forwarder idiom) to retire the base-only `query_animation_playback_state` symbol. |
| open | core `R_ASSERT` behavior under MASTER_GOLD | `vostok/debug/macros.h:85` (consumer: `core/sources/game_resman.cpp:78`) | match/core-game-resman | `VOSTOK_MASTER_GOLD` is now correctly derived from `MASTER_GOLD`, so `LOG_DEBUG`/`LOGI_DEBUG` elide like the target. The remaining issue is separate: the target's `R_ASSERT` still logs failure (preserving `base_resource` and five statements), while the base MASTER_GOLD macro fully elides and DCEs the asserted expression. Recover the shipped gold assert macro behavior. |
| open | `threading::interlocked_or` / `interlocked_and` noinline+Oy-off HACK | `vostok/threading_functions.h:87-90` (decl) + `core/sources/threading_functions_win.cpp:21-34` (def) | chore/core-interlocked-noinline | HACK (recovers target CODEGEN, not original source): the out-of-line `interlocked_or`/`interlocked_and` were re-inlined by our `/GL` whole-program build into ~97 caller sites as inline `lock or`/`lock and` (base had ~55+42 inline; target out-lines, keeping only 5 `or`+2 `and` inline), and the standalone symbols stayed unpaired at 15B custom-cc vs target 25B standard-frame. Applied THREE levers on the decl+def: (1) `__declspec(noinline)` forces callers to emit `call interlocked_or/and` (matches target) - the sanctioned packet_reader::r<T> precedent; (2) `DISABLE_LINKER_GL` (= `__declspec(dllexport)`, the engine's OWN anti-/GL idiom, faithful - cf. `math::mul4x3`) restores standard `__cdecl` (15B->23B); (3) `#pragma optimize("y",off)` restores the frame pointer that core.vcproj's global `OmitFramePointers="true"` strips (23B->25B == target). RESULT: base now has 0 inline `lock or`/`lock and` and 58+43 `call interlocked_or/and` (target 56+47); standalone bodies are byte-correct (only a `push esi` schedule diff, 85.7%) but still report unpaired because the COMDAT lands in `threading_functions_win.cpp.obj` while the TARGET's is attributed to `threading_policies.h:128/131` (the `operator_or`/`operator_and` instantiation point - i.e. the original likely emitted these as header-instantiated COMDATs, not from a .cpp). To make them PAIR, move the defs into a header included by the policy consumers (deferred - risk/ODR vs dllexport). Net +fuzzy; matched-count dip is ICF fold-rep churn. TODO-to-remove if a faithful cause (build flag / original `#pragma` / header placement) is found. interlocked_increment/decrement (lock xadd) deliberately left inline (target keeps them inline too). |
| done | physics file-scope `dynamic initializer/atexit` pairing and omitted smooth-position command | `physics/sources/bullet_character_controller.cpp` + `physics_entry_point.cpp` + `animated_rigid_body.cpp` | match/physics-target-only | RESOLVED: `vostok derive` now canonicalizes unique target/base static-init identities across the PDB spelling gap, including function-local scopes and unequal emitted sizes. Direct target image-base/section validation disproved the prior stale-PDB verdict for `s_cc_smooth_positions_count_cc`; its real target code, string, backing value `3`, bounds `1..100`, and serializable flag were reconstructed in source. The sole residual is `calculate_bt_joint_size`: its faithful body has no shipped caller and the candidate compiler eliminates the standalone symbol. |
| open | `ai::predicate_types_enum` has 3 extra predicates vs the shipped target | `ai/sources/predicate_types.h:13` (consumers in `ai/sources/behaviour_domain.cpp`) | match/enum-const-divergences | [values] divergence: the target ships only 12 enumerators (0x0..0xb ending `predicate_type_is_at_position`); our reconstruction added `predicate_type_animation_playing` (0x9), `predicate_type_sound_playing` (0xb) and `predicate_type_is_moving` (0xe), shifting the tail. Matching the target means REMOVING those 3 AND every consumer (behaviour_domain.cpp `predicate(...)` preconditions, the 3 `add_predicate` blocks, and the `brain_unit::is_playing_animation`/`is_playing_sound`/`is_moving` predicate binds) plus the `predicates_captions`/`world_state_properties_tuples` rows - a behaviour-logic [stmt] change spanning AI files, not an enum-only fix. Parked. Recover whether the shipped behaviour domain truly lacks these 3 predicates (vostok-versions) before deleting live binds. |
| open | `resources::class_id_enum` deleted/renamed class block | `vostok/resources_classes.h:208` (consumers in core/sound/editor/game cooks) | match/enum-const-divergences | [values] divergence: 11 enumerators (`config_class`..`sound_player_editor_class`) are absent from the shipped target's class_id_enum but our tree still has resource cooks registering them (`config_class`->configs_binary_config_cook.cpp; `spl_class`->spl_cook.cpp; `sound_stream_class`->sound_*_cook*.cpp; `animation_controller_class`->rtp/editor; `game_project_simple_class`->project_cooker_simple.cpp). The 4 alias entries (`platformed_raw_data_class`/`converted_model_class`/`binary_config_class`/`inherits_binary_config_class`) ARE fixed (now alias onto 0x3/0x3/0x22/0x23 to match the target). Removing the 11 deleted classes needs parking/removing those cooks (several in skipped sound/editor modules) - out of the enum-only scope. Block anchored at 0x20a so it doesn't collide. Parked. |
| investigate | pdb-parser collapses competing same-name `LF_ENUM` records to one | `pdb_parser` / `pdb_divergence` (external tool) | match/enum-const-divergences | After edits to 8 enums, a fresh build's objects contained only the new enumerator strings, while the linked PDB still exposed old and new same-name enum variants and `pdb_parser` warned that it was "keeping first". This proves the one-record normalization is lossy; it does not make first, last, or most-complete a generally correct selector. Extend raw variant-set reporting to top-level enums and preserve every type index. Then use module `S_UDT`/consumer evidence where available before deciding which variant describes a given compiland. Do not claim whole-enum source fidelity merely because one selected record or one object agrees. |
| open | `animation_lexeme_parameters` setter-family `noinline` fallback | `vostok/animation/mixing_animation_lexeme_parameters.h` | animation-to-100 | Retail calls every observed use of `playback_type`, `time_synchronization_group_id`, `weight_synchronization_group_id`, `bones_mask`, and `time_calculator` out-of-line, while the reconstructed `/GL` build absorbed every body into callers. The scoped `__declspec(noinline)` restores those real calls, pairs all five functions in the correct header unit, and materially improves nearly every caller. Residual: the emitted standalone bodies use standard `thiscall`, while retail uses LTCG custom register conventions (`this` in EAX and the value/reference in ECX). Remove the fallback if the faithful cross-library/compiler-context cause is recovered. |
