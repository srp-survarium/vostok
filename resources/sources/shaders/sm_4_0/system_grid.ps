#include "common.h"

uniform texture2D	t_grid;
uniform sampler 	s_grid;

float4 main( v2p_T vin) : SV_TARGET
{
	float mask = t_grid.Sample( s_grid, vin.Tex0).x;
	
	clip( mask - 0.5);
	
	return float4(0,0,0,0);
}