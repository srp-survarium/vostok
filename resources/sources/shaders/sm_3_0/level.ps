#include "common.h"

f_deffer 	main	( p_flat I )
{
  f_deffer	O;

  O.Ne          = half4(normalize((half3)I.N.xyz), 1);
  O.position    = half4(I.position.xyz, 0);
  O.C			= half4(0.43, 0.75, 0.68, 0);

  return O;
}
